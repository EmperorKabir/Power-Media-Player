# Cross-examination + conflict resolution (Phases 6+7) — 2026-07-04

All three canonical findings files read IN FULL by the main session (reasoning 1510L, context7 404L, superpowers 480L).

## Inter-lens agreement (strong consensus, no arbitration needed)
- AudioDelayProcessor missing onFlush (3 lenses; PROJECT_RULES §4 contract violation; 3 concrete scenarios).
- PodcastSyncWorker KEEP policy freezes stale constraints for upgraders (lens2 doc-proof + lens3 commit-pair 50c19a6→fc3fb29).
- podcast_episodes: zero indices, unindexed audioUrl lookup on the track-change hot path, unbounded rows (lens1-B5 + lens3 B-6; the MediaOverrideRepository "indexed" comment is FALSE).
- OAuth drive_* cache never trimmed (audit-5.11 landed on the SAF twin only) (lens1-B5 + lens3 B-10).
- DataStore: ~120-138 bare data.map flows, zero distinctUntilChanged → every write fans out to all live collectors (lens1-B5 + lens3 B-9).
- proguard blanket -keeps neutralise R8 on the biggest libs; defensive batch origin aa5e431, no crash lineage (lens1-B6 + lens3 B-17).
- DiagLog formats before the enabled check (KDoc false) + shared SimpleDateFormat race (lens1-B6 + lens3 B-15/16).
- CastRelayServer faststart heads accumulate per cast session (lens1-B4 + lens3 B-1; eviction must spare the CURRENT item's head and keep tokens mapped — passthrough downgrade).
- Direct-Boot LOCKED_BOOT receiver reads CE storage (lens2 crash-grade, lens3 refined: silent no-op probable, crash device-dependent — wiring wrong either way).
- Dead code: CastButton.kt file, MediaSelectionComponents.kt file, MediaMetadataHelper.kt file, PlayerViewModel 6 members + 3 funcs, PaletteHelper 2 fns, LastPlayedViewModel 2 fns, LibraryViewModel 2 fns (playFolder KEEP-dormant), pairHueBridge, SourceChooserDialog+pickerRoots plumbing, reverbLabel, ControlButton, dead params/imports. All grep-verified by their finders.
- NUL byte in MediaOverridesPopup.kt:80 sentinel literal (breaks grep/CI tooling).
- PodcastsSection un-remembered Room flows (lens1-B3 + lens3 VERIFIED) + per-row SimpleDateFormat.
- EqualizerViewModel loadPresets N+1 collector leak.
- 23-30 plain collectAsState sites vs the app-wide WithLifecycle convention (ctx7 F-SWEEP-1 + lens3).

## CONFLICTS RESOLVED (Phase 7)
1. SettingsViewModel 78-flow combine: lens1-B2 wants a single data.map flow; lens3 A/B demand typed sub-combines and REJECT re-aggregation of the per-key architecture; PROJECT_RULES bans the indexed shape but the mitigations (distinctUntilChanged+conflate+WhileSubscribed) are real. RESOLUTION: the full refactor is HIGH-CHURN (10 consumer sites, 78 fields) and needs a full settings-matrix retest → NOT applied this pass; recorded as a gated work item with the agreed target shape (typed per-section sub-combines, retain the operator pair; do NOT re-aggregate SettingsDataStore per-key flows). The independent safe subsets ARE applied: ThemeSection switches to the existing dedicated flow; the B5-12 source-level distinctUntilChanged helper is RECORDED pending the write-as-event consumer sweep (lens3's own gate).
2. Slider per-delta DataStore writes (AvSync/BT/cast offsets, reverb wet mix, override sliders, font scale): lens1 marked defer-to-drag-end safe. PRESERVATION GATE KILLS the family: commit 92c2846 "BT video offset now applies the slider IMMEDIATELY (was throttled to a crawl)" is a shipped fix whose defence is the per-delta write path (service applies FROM the store flow); reverb/override sliders live-apply the same way; font scale's live preview reads the same store. Deferral/debounce would re-break device-verified immediacy. NOT APPLIED; recorded as needs-design (in-memory live channel + deferred persistence) — outcome (b)/(c) cannot be argued today.
3. SmartPlaylistRail duplicate LibraryViewModel: lens1-B3 says duplicate (screen uses the Activity-scoped VM passed from AppNavigation; rail resolves entry-scoped); lens3 REFUTED assuming same owner. RESOLUTION at execution: verified AppNavigation passes its own hiltViewModel() into LibraryScreen → the rail's default hiltViewModel() resolves the nav-entry owner → two instances CONFIRMED possible. FIX applied is safe under BOTH readings: pass the screen's VM into the rail (also fixes lens3's state-coupling point).
4. wseemann.media proguard keep: lens3 says RETAIN (JNI boundary); lens1-B6 says the FFmpeg dependency does not exist (excluded per build.gradle comment; no AAR). RESOLUTION at execution: dependency-list grep decides; if absent → keep is a no-op for phantom classes → remove together with MediaMetadataHelper.kt as one "FFmpeg remnants" change. (Verified: no wseemann/FFmpeg artefact in dependencies → REMOVE.)
5. Direct-Boot fix shape: lens2 Tier-1 runCatching vs lens3 Fix-A (drop LOCKED_BOOT path). RESOLUTION: Tier-1 (runCatching + isUserUnlocked guard) applied now — purely additive, converts crash to the no-op that already happens, BOOT_COMPLETED re-arms after unlock. Fix-A/B (drop path vs device-protected store) is a user decision — recorded.
6. CoverFrost readback: lens1 says per-line-per-frame; lens3 downgraded to 5x-per-track-change main-thread full-layer readback, animation-coupled. RESOLUTION: lens3's evidence-based shape adopted. The agreed fix (single shared readback, per-line regional sampling from one software bitmap, debounced bounds) preserves the documented key set — but it rewires the delicate frost pipeline and demands on-device visual verification → RECORDED as gated, not applied blind.

## TIER-A — APPLY NOW (compile/unit-verifiable; preservation-gated; grouped commits)
G1 dead code: CastButton.kt, MediaSelectionComponents.kt, MediaMetadataHelper.kt (+wseemann keep), PlayerViewModel 6 members + 3 dead funcs, ControlButton, PaletteHelper 2 fns, LastPlayedViewModel 2 fns, LibraryViewModel createSingleMediaItem+unhideUri, pairHueBridge, SourceChooserDialog+pickerRoots plumbing (3 files), reverbLabel+unused imports, SyncedLyricsPanel onLineTap param, ChapterRow index param, InfoSheet dead scope, unused imports batch, unused deps kotlinx-coroutines-guava/-play-services, icons-core line, NUL byte → "".
G2 correctness/store: AudioDelayProcessor onFlush + unit test; PodcastSyncWorker UPDATE policy; BootCompletedReceiver runCatching+isUserUnlocked; podcast_episodes indices (entity + v22→23 migration, v20→21 pattern); BLUETOOTH_SCAN permission removal + comment fix; exportSchema=true + schemaLocation; CastRelay SecureRandom tokens; DiagLog ThreadLocal formatters (B-16 only; B-15 lazy-format recorded); PROJECT_RULES build-coordinates doc fix.
G3 perf/lifecycle: OAuth trimDriveCache parity; DriveTagEnricher temp try/finally + CancellationException rethrow; EqualizerViewModel delete 2 loadPresets calls; PodcastsSection remember(flow) x2 + SDF hoist + setStatus Main-hop removal; PlayerScreen controlsHidden derivedStateOf; CoverArtBackground composition side-effects → LaunchedEffect; sleep-timer adaptive cadence (500 ms kept in fade window + <30 s to boundary); PlaybackSessionCoordinator ticker gated on isPlaying; coordinator map-from-state-field (B4-CO-2); PlaybackService sender caches cleared in onDestroy (B4-PS-5); album-compare cached on transition (B4-PS-3); subtitle-delay re-prepare debounce mirroring the cast-offset fix (B4-PS-2); Spotify paused-poll 3 s cadence (burst rule already exists via startPlaybackPolling); CloudViewModel enrichedCovers+starredOfflineIds → WhileSubscribed(5000) (offlineDrivePairs stays Eagerly); writeArtRow atomic re-read merge; HueSection fetch-once guard; DiagLogPicker remember; OpenSubtitlesSection token remember; ThemeSection dedicated flow; SmartPlaylistRail shared-VM param; collectAsState→WithLifecycle sweep; 4 AutoMirrored icon swaps; F-COIL-5 dead cast; DownloadsScreen _selected update{}; SubtitleAutoFetcher ConcurrentHashMap.newKeySet; FullScreenAlarmActivity cursor-loop cap; HueEntertainment per-frame IntArray hoist; ArtworkCache uriFor setLastModified touch (true LRU).

## TIER-B — RECORDED, GATED (genuine external gates; NOT silent deferral)
- Device-cast-test-gated: CastRelay head passthrough-downgrade eviction (T302), cast/mediarouter version bump (edge-to-edge warning), NanoHTTPD posture beyond token fix.
- Release-build + device-smoke-gated: proguard blanket-keep narrowing (recipe in lens3 B-17).
- Device-visual-gated: CoverFrost shared-readback + bounds debounce; CoverArtBackground inSampleSize screen-edge decode; captureCoverFrost captured-write guard; MediaThumbnailFetcher SourceFetchResult; F-M3-3 NavigationSuiteScaffold removal; Compact/Expanded shared row extraction (bookmark-rename drift = feature-add needing user flag); F-CMP-2 lyrics derivedStateOf hoist.
- Evidence-gated (no-guesswork policy): WAKE_MODE_NETWORK; focus churn gate on playWhenReady; widget merged-metadata art.
- User/product decisions: USE_EXACT_ALARM removal + canScheduleExactAlarms flow (before Production); Direct-Boot Fix-A/B; §B3 inert metadata-handoff stub (delete or finish); podcast row pruning; 4 GB faststart cache Settings surface; Hue TLS TOFU pinning; ChapterPickerDialog track-mode index-vs-ms defect (ledger).
- Design-needed: settings combine refactor family (incl. dead huePairStatus + 4 BT legacy fields — only safe inside that refactor); B5-12 distinct helper after the write-as-event sweep; slider live-apply persistence split; PlayerUiState 8-field strip removal (with suppression test); syncEpisodes IN() batch (carry-forward invariant test); RSS streaming + episode cap; ReverseAudio LRU; crossfade paused StateFlow wait; HueDimmableDriver remainder sleep; HueProvider listAllLights TTL memo; ChapterCache filesDir move; enrichedCovers exists() memo; M4b duration threading; LocalTrackArtFetcher single-MMR; DiagLog lazy-format ring (B-15); Room v21 LIKE-index drops (fold into next unavoidable bump).
- Test gaps (recorded): M4b box-walk, computeNextTriggerMs, TextNormalizer heal-loop contract, ArtworkCache eviction, HueAudioAnalyser characterisation.

## Master KEEP-DO-NOT-TOUCH register (union of all three lenses — binding on execution)
Sticky-metadata owner gates + fallback chains (PlaybackConnection); lastReal composition write; bounded runBlocking seeds; GlobalScope Spotify pause; cold-start restore block; cast suppression windows; skip debounce; VideoSurface TextureView + binding stack; CoverFrost key set + capture-in-draw; CoverArtBackground allowHardware(false) + null-reset + Coil ByteArray bypass; deferred-seek sliders; Eagerly on player uiState + offlineDrivePairs + activeOverride (Main.immediate); positionUi tick isolation; per-key DataStore flow architecture; syncEpisodes carry-forward; durable filesDir storage + SHA-256 keys; mojibake repair at source AND display; enrich dedup/NO_ART grading + full-window sizes; MobileDataPolicy fail-open; Spotify poll/token optimisations + lyrics subsystem; worker cadence directive (CONNECTED); Hue single-teardown, cipher pin, tuning constants, isStreaming short-circuit; EQ hitboxes (#14) + restoreIfUntouched; T294 nav architecture; DiagLog channel/writer/fatalSync; 206-only guard + fd chains; BAL bridge; alarm §C6 snooze ids + §C12 UI; allowBackup=true.
