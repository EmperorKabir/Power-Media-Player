# TASKS.md — binding task ledger (compaction-proof)

> **✓ DONE 2026-06-27 — all 7 audit misses (M1–M7) fixed + 3-lens efficiency audit
> closeout applied.** See `REMEDIATION-2026-06-27.md` for the full record. M1 BT
> per-file-type mappings (4 kinds), M2 BT chapter nav, M3 cloud backup/restore (local +
> Drive), M4 sleep-timer remote fade (Spotify+Cast, pauses+restores correctly), M5 podcast
> download wifi+mobile, M6 volume-normalisation label, M7 vc40/1.3.5 released. Unit suite
> 175/0; device-clean; signed AAB staged to dist/. Audit fixed 1 HIGH (Spotify sleep-timer
> expiry) + 1 MED-HIGH (cloud-audiobook BT kind) + 5 MED + LOWs.

> Closed/verified history archived to docs/archive/TASKS-history.md (read on demand).

> ## PROTOCOL — applies to every session, every turn, forever
> 1. **Read this file at the start of EVERY turn** and again after any context
>    compaction. `CLAUDE.md` carries a pointer here, so the obligation survives
>    compaction even when conversation history does not.
> 2. A task may move to **DONE only with an Evidence line** (command output,
>    grep, log slice, test result, screenshot). Self-attestation is invalid.
> 3. Legal statuses: `TODO` · `ACTIVE` · `BLOCKED(<reason> → <exact unblock>)`
>    · `AWAITING-USER(<question>)` · `DONE(<evidence>)`. Nothing else.
>    "deferred", "later", "follow-up", "polish pass", code-comment TODOs as a
>    completion substitute — all forbidden.
> 4. Before any end-of-turn report: update every touched row here, run the
>    relevant gate (build / test / grep / log query), and report THE TABLE,
>    not prose claims.
> 5. In-scope work discovered mid-turn must be ADDED here in the same turn.
>    Silent scope-shrink is forbidden; scope changes need explicit user say-so.
> 6. Phase lock: **INVESTIGATE → PLAN → IMPLEMENT**, per task. No planning or
>    implementation while the task's phase column says INVESTIGATE, unless the
>    user advances the phase.
> 7. Genuine external blockers (user input, device action, credentials) are
>    recorded as BLOCKED/AWAITING-USER with the exact unblock — never used as
>    cover to skip automatable work around them.

Legend: phase I=investigate, P=plan, M=implement, V=verify-on-device.

## B — Outstanding (carried, phone now available)

| ID | Task | Phase | Status |
|----|------|-------|--------|
| T230 | Hue disconnect→reconnect + room-switch regression | I→M | DONE(T253 shipped: evidence run4 22:06-22:07 — disconnect zeroed intensity (priorIntensity=40→0) + area was never a collector source + isStreaming short-circuit ignored area changes. Fixed: intensity preserved on disconnect; area now a combine source; blank area=off; mid-stream switch restarts engine. 5/5 predicates (refined: clearHueSelectedArea clean, unpairHue zeroing intentional); pushed e70c8a7; installed Success) `[DEVICE]` re-test pending |
| T257 | CONSOLIDATED on-device verification — single user pass covering: T228 visuals, T232/233/237 visuals, T252/254 re-tests, T253 re-test | V | MOSTLY DONE on real Z Fold6 2026-06-13: **T253 Hue PASS** (testo zone connect→disconnect[priorIntensity=59 preserved]→reconnect→reactive frames flow `dimmable GROUP PUT http=200 bri=46-86%`, user confirms lights react); **C9 Cast PASS** (discovery found all devices; cast to Kabir's Kitchen Stereo → CastMediaSession PLAYING, user confirms audio on stereo; clean disconnect→default_audio_route); **C8 Spotify PASS** (play-via-app → startPlaybackPolling gen=1 1Hz → background 30s → "poll paused (backgrounded 30s) gen=2" → foreground → startPlaybackPolling gen=3 resumes); **D7/D9/D10 PiP PASS** (Home+video → Task mode=pinned + PiP window screenshot); **E1 immersive + E2 rotate** observed. BUGS FOUND+FIXED this pass (all evidence-locked + device-verified): (1) fold density-recreate jank (commit ed74798); (2) **Hue lights-keep-changing-after-disconnect** — REGRESSION from audit 3feb0cb: the added `finally` + `stop()` BOTH ran teardown → double stopEntertainmentStream + double restore; fixed to single teardown path (commit 5b1cd3c), verified single teardown in log + user confirms lights settle; (3) **stale album art on dangling MediaStore art URIs** — MediaStore reports a non-null albumart URI whose file is missing (FileNotFoundException onError); Coil retained the previous track's painter → old cover stuck on a no-art track; fixed with `key(artworkUri)` + black error/fallback painter (commit 00416c0), verified on Z Fold6 ('Grievances Aside' dangling→black, 'Some Feel'/Slipknot art intact). **F8 tabletop NO LONGER BLOCKED** — posture logger proved the Z Fold6 reports `state=HALF_OPENED orientation=HORIZONTAL` and the app's `isTabletop=true` flips on a real half-fold (earlier "blocked" was never catching it held half-open); F8 code path engages. REMAINING AWAITING-USER: pure-visual sign-offs (settings order/expand-collapse feel, font-size visual, empty-player, swipe→Undo, edge-to-edge borders) + Spotify mirror is app-initiated-only by design (playing in the Spotify app directly is NOT mirrored — flag for UX review if "mirror any Spotify playback" is desired) |

## C — User-approved UX (2026-06-04: "I approve of idea 1… make 3-5 sub sections… all main sections expandable… search still checks all")

| ID | Task | Status |
|----|------|--------|
| T232 | Visible ⋮ menu per row (Library + LastPlayed recents/pinned) | DONE(grep 1+2 "More options for"; build green; commit in 0ba355f range) `[VISUAL]` pending |
| T233 | Hue → 4 expandable sub-sections | DONE(5 ExpandableSubsection refs; no-drop inventory 16==16) `[VISUAL]` pending |
| T237 | ALL 8 settings groups expandable; search sees collapsed; clear restores | DONE(SettingsGroupHeader + "expanded || searching" + keyed rememberSaveable; SettingsSearchTest 6/6) `[VISUAL]` pending |

## D — Plan + implementation

| ID | Task | Phase | Status |
|----|------|-------|--------|
| T235 | IMPLEMENT inline | M | Tasks 1-8 DONE(GATE A: 9/9 predicates PASS, assembleDebug + all unit tests green, pushed 0ba355f, installed Success). Tasks 9-12 AWAITING-USER(device script run) |
| T249 | IMPLEMENT addendum Tasks 13-20 | M | Tasks 13-19 DONE(GATE B: 11/11 predicates PASS; suites ResumeGateTest 3/3 + ChapterCacheTest 3/3 + SpotifyBannerGraceTest 4/4 + SettingsSearchTest 6/6; assembleDebug green; pushed 8132182; installed Success). Task 20.4-20.5 AWAITING-USER(device re-test of the 7 complaints + Hue) |

## G — Round-3 device run reports (2026-06-04 late — phase I)

| ID | Task | Phase | Status |
|----|------|-------|--------|
| T252 | FIX: provisional mirror state on Spotify play | M | DONE(arm+clear methods; armed at all 3 tap sites; cleared on all 3 failure paths; null-snap grace hold; 5/5 predicates PASS; all suites green 3+3+4+6; pushed bb8e195; installed Success) `[DEVICE]` re-test pending |
| T254 | Stuck 'Loading metadata' + tap lockout on launch (cold-start inline https parse; debounce swallowed taps; cache died with process) | M | DONE(4/4 predicates after grep-artefact verification; cold-start remote=cache-or-none; IGNORE→supersession; disk-backed ChapterCache; fill dedup; pushed 6708a96; installed Success) `[DEVICE]` re-test pending |

## H — Device-run reports 2026-06-04 night (phase I)

| ID | Task | Phase | Status |
|----|------|-------|--------|
| T261 | Resume-on-restart toggle? | I | DONE(answer: NO toggle exists — the launch restore is always-on built-in behaviour for local/Drive; 'Cold-start resume backoff' only sets the rewind amount; 'Resume on Bluetooth connect' is a different feature. Optional new toggle proposed — awaiting user) |
| T262 | In-app PiP mini-player | M | DONE(FloatingVideoMiniPlayer on non-Player tabs; reuses VideoSurface → effects + surface handoff identical to system PiP; GATE C 8/8 PASS; pushed 7809ae8; installed) `[DEVICE]` |
| T263 | Reverb fix: session-attached insert (OUTPUT_MIX denial closed) | M | DONE(GATE C; aux calls removed; rebuild-on-session-change) `[DEVICE]` audibility check |
| T264 | Boost fix: RG/user gain separation + shared effect holders across VM instances | M | DONE(GATE C) `[DEVICE]` |
| T265 | 'Restore last played on launch' toggle (default on); backoff subordinate; BT/headphone toggles independent by design | M | DONE(GATE C, 8 wiring refs) `[DEVICE]` |
| T268 | Loading banner flickering during video playback/PiP | M | DONE(ExoPlayer isLoading oscillates per ~100ms chunk — banner now needs 450ms sustained loading; installed) `[DEVICE]` |
| T267 | Reverb | M | DONE v4(boost confirmed as crackle source; presets re-curved size-progressive — sqrt(1-fb), Cave ≈2.3x Room — + soft knee; diag click-free) `[DEVICE]` per-preset check |
| T269 | Boost crackle at high gain (user should never accept it) | M | DONE(LoudnessEnhancer hard-clip replaced by in-chain GainAudioProcessor with per-sample glide + 4:1 soft knee; enhancer deleted) `[DEVICE]` |
| T270 | Restore toggle 'not working' | M | DONE(evidence: 00:17:15 restore DID load the item paused but confirm-log missing → failures now logged; OFF now also clears a paused leftover surviving in the service, never touching playing audio) `[DEVICE]` test both directions |
| T271 | Player tab 'nothing playing' vs mini-bar offering resume | M | DONE(gate omitted isSpotifyActive — local player empty while the Spotify mirror was live; empty-state now respects the overlay; installed) `[DEVICE]` |
| T272 | Reverse audio | M | DONE v2(Drive enabled with 50 MB size guard — 5s-class load on decent Wi-Fi, size not duration is the honest cap; HEAD-checked pre-download; oversize → forward + snackbar; layman audio-only settings copy; ReverseAudioTest 2/2; installed) `[DEVICE]` |
| T273 | 'Auto-play on launch' toggle (resume = restore + play) | M | DONE(nested under restore toggle, default off; cold-start playWhenReady=autoplay; backoff still applies; suites green; installed) `[DEVICE]` |
| T275 | Mid-playback reverse toggle | M | DONE(live flip in place from the mirrored position, state preserved; failure → toast + keeps playing forward) `[DEVICE]` |
| T276 | Auto-play on launch for the status-bar close | M | DONE(fresh app open presses play on a paused surviving item; rotations excluded via savedInstanceState; Spotify mirror guarded; force-close path unchanged) `[DEVICE]` |
| T277 | Final audit + authoring sweep + vc31 release AAB | M | DONE(tell-sweep clean; full gate green; versionCode 31; bundleRelease 14.7 MB upload-key-signed; DeepLogger full-impl absent from release dex (0 tell-tale hits); staged at dist/PowerMediaPlayer-1.0.0-vc31-release-2026-06-05.aab; matching vc31 debug installed on phone) AWAITING-USER(Play Console upload) |

## I — Speed/efficiency + form-factor audit (2026-06-11)

| ID | Task | Phase | Status |
|----|------|-------|--------|
| T282 | Master implementation plan MD with check-off boxes + per-item acceptance predicates (anti-skip), covering the full T280 scope; report in chat | P | DONE(`docs/superpowers/plans/2026-06-11-vc33-master-plan.md` — 7 batches A-G, ~45 tasks, every task carries Files/Steps/code/predicates/commit; batch gates with pasted-evidence requirement; FINAL ANTI-SKIP GATE counts unticked boxes; AWAITING-USER gates encoded: minSdk Play-stats (G2), phone wipe consent (G4), Play upload (G3); self-review section complete) |
| T283 | EXECUTE master plan batches A-G per its BINDING EXECUTION PROTOCOL | M | ACTIVE(BATCH A ✅ + BATCH B ✅ both GATE PASS 2026-06-12. A: A1-A10 (4 commits), tests green, on-phone T279 regression PASS. B: B1-B12 (9 commits through e726780) — isRemote content:// fix (TDD 3/3), non-blocking DiagLog init + batched writer, DataStore warm-up (phone evidence: service onCreate START→DONE=22ms, prefsWarmup=1ms, seed=0ms), SAF single-cursor listing + 5k traversal cap, SharedHttp pool/cache/timeouts across 10 clients + parallel fan-outs, alarm ring off Main, podcast UNMETERED+parallel, Hue creds snapshot + parallel PUTs, RG batch upserts, slider persist debounce, ChapterCache lock-scope + sibling eviction + caps, drive_* LRU trim, StrictMode live. Phone restore regression PASS again (29405 exactly). Zero open boxes in A+B; device-interaction items (Hue bridge, Drive sign-in browse, slider-drag persistence, alarm fire) → consolidated device pass. BATCH C ✅ GATE PASS 2026-06-12 — ALL 12 tasks: C1 PlaybackSessionCoordinator (VM 2059→1406 lines, side effects once-per-process, restore/tick/enrichment/SRT/RG moved verbatim; tick persistence + exact restore verified on BOTH devices, zero guard races), C6 merged RG pipeline (A-sources + B-sinks, MMR sniff + volume race gone), C2 normalisation cache, C3 position split (PositionSection + positionUi; slider 0:12→0:19 live while uiState stays position-stripped), C4 Hue FFT gate, C5 crossfade event-gating + pause-aware ramp, C7 playing-gated poller, C8 Spotify 30s background pause + AuthState cache, C9 sheet-scoped cast discovery, C10 widget decode cache, C11 interaction batch (search/settings/theme/palette/rows/LastPlayed/cloud), C12 log sampling. Phone leg ran behind the keyguard (restore 29405 exact). NOTE: phone PIN-locked — UI-driven phone tests + consolidated device pass BLOCKED(user unlocks phone). BATCH D ✅ GATE PASS — 12 tasks (D1 widget open-tab consume; D2-D5 inset/cutout/safe-draw padding; D6 compact-height list refits; D7 PiP actions+sourceRectHint; D8 font-scale; D9/D10 video surface heal; D11/D12 layout). BATCH E ✅ — E1 immersive tap-toggle (bars follow controls, full-bleed flag) + E2 rotate-to-fullscreen button (compact width only, no perm). BATCH F ✅ GATE PASS 2026-06-13 — full adaptive redesign, 9 tasks, 6 commits (476a358..8338719): F1 deps+AdaptiveInfo; F2 NavigationSuiteScaffold (bar↔rail); F3 Library grid; F4 LastPlayed/Cloud grid + Settings ListDetailPaneScaffold two-pane; F5 EQ 720dp + dialog 560dp caps; F6 hinge-aware Expanded player + twoPane routing; F7 height size-class; F8 TabletopVideoLayout. GATE: assembleDebug OK; testDebugUnitTest 17 suites/59 tests/0 fail; F9 form-factor sweep (build/c1blocks/f9_sweep.py) 24 cells all correct-nav + zero-clip. **Verified on the user's REAL Galaxy Z Fold (1968x2184 Expanded inner display): rail, Library 2-col grid, Settings two-pane (8 groups left + detail right), EQ width-cap, Player two-pane both orientations.** F8 tabletop = code-verified; Posture.isTabletop trigger BLOCKED(not reproducible — real-device physical fold + device_state injection + emulator hinge-sensor all failed to surface HALF_OPENED-HORIZONTAL to currentWindowAdaptiveInfo → needs a unit whose WindowManager extension propagates Flex Mode). BATCH G ⏳ — G1 baseline-profile scaffold DONE (module+plugin+deps+generator, commit ee186a6); profile GENERATION BLOCKED(software-GPU host → empty gfxinfo framestats → macrobenchmark amStartAndWait fails; needs hardware-GPU CI/device or phone-install-with-wipe; pipeline proven end-to-end). G3 release DONE(tell-sweep clean; lint config-disabled per §A17; versionCode 31→32 + versionName 1.0.0→1.1.0; bundleRelease green, jar verified, 14.97MB → dist/PowerMediaPlayer-1.1.0-vc32-release-2026-06-13.aab). G2 minSdk + G3 Play-upload + G4 device pass = AWAITING-USER. ALL CODE TASKS A-G COMPLETE.) |
| T279 | Tester report: launch restore loads last item but at 0:00, not saved position | I→M | DONE(CONVICTED: MediaController.seekTo gated on COMMAND_SEEK_IN_CURRENT_MEDIA_ITEM, unavailable while the placeholder window is BUFFERING → the restore's set-then-seek always lost the race on fast media. Evidence chain: phone (release vc31) repro played-25.07s→relaunch→position=0 with Recents "@0:24" proving DB writes healthy; emulator (debug) diag log: `seekTo target=5772ms` issued during BUFFERING (42.233→42.996 READY), ZERO `discontinuity reason=SEEK` lines → seek never reached ExoPlayer. FIX: startPositionMs now rides atomically inside setMediaItems; applied to all 3 set-then-seek sites (cold-start restore PlayerViewModel; LastPlayed tap-resume; mid-playback reverse flip). GATES: assembleDebug+testDebugUnitTest EXIT=0; emulator same-cycle re-run restored position=5772 EXACTLY (=saved 10772 − 5s backoff; was 0 pre-fix). NOTE: phone's installed vc31 is NOT debug-signed (pkgFlags lacks DEBUGGABLE — corrects T277's claim); debug install needs uninstall+data wipe) `[DEVICE]` AWAITING-USER(consent to wipe phone data for debug install, OR verify via next Play build) |

## E — Completed (evidence archived)

- T236 (added+done 2026-06-04, protocol rule 5): empty-player guidance drew OVER
  the player chrome (user screenshot: "No media loaded" bleeding through).
  Fixed — EmptyPlayerState now a layout branch replacing the chrome, not an
  overlay. Evidence: assembleDebug green, installed (Success), commit 72de6d1.
  `[VISUAL]` re-check on device with the rest of T228.

## J — vc33 post-publish device bugs (2026-06-15)

| ID | Task | Phase | Status |
|----|------|-------|--------|
| T284 | Video controls: tap toggles controls (away supersedes timer; can return); all timer logic kept | M | DONE(PlayerScreen: removed show-only parentTapModifier; added full-bleed tap layer below OverlayContent → `controlsVisible = !controlsVisible`; auto-hide LaunchedEffect intact) `[DEVICE]` re-test |
| T286 | FIX resume: backoff clamp guard + onStop background position save | M | DONE(backoff: `if pos>backoffMs then pos-backoffMs else pos.coerceAtLeast(0)`; ProcessLifecycle onStop saves player position when not Spotify-mirrored, skips reverse-cache; assembleDebug BUILD SUCCESSFUL 26s; pushed 5364f47; install Success on RFCY70BARDJ) `[DEVICE]` AWAITING-USER(re-test local+video resume) |
| T287 | Swipe-away = stop everything except PiP (user decision) | M | DONE(MainActivityHolder.isInPip flag synced both PiP callbacks; onTaskRemoved: PiP→keep alive, else stop+clearMediaItems+stopSelf; STOP_ON_TASK_REMOVED default→true; build green; pushed 5364f47; install Success) `[DEVICE]` AWAITING-USER(swipe-away test: status player closes, PiP survives) |
| T288 | Spotify auto-resume on launch (user decision) | M | DONE(cold-start SPOTIFY branch: playTrackOnConnectDevice + seekTo saved pos; respects autoplayOnLaunch; adoptSession; graceful no-device; build green; pushed 5364f47; install Success) `[DEVICE]` AWAITING-USER(launch with prior Spotify session → resumes on Connect device) |
| T289 | Swipe→reopen: Player tab empty, mini-bar shows paused ghost track | I→M | DONE(EVIDENCE logcat 02:09: swipe-teardown WORKS — state=IDLE + discontinuity REMOVE 10443→0 + PLAYLIST_CHANGED id=null + PlaybackService.onDestroy; pid 24509 stayed alive across swipe→reopen = WARM start. ROOT 1: restore lived in start()'s once-per-process `started` guard → warm reopen no-op → no `[DEC] cold-start` line at 02:09:21 → Player tab empty. FIX: attemptColdStartRestore() called from onCreate(savedState==null), out of the started guard; skip gated on player.mediaItemCount>0 so stale adopted id + empty player no longer blocks restore. ROOT 2 (mini-bar ghost): updatePlayerState fell back lastKnownMediaId→senderMetadataByMediaId→itemMetadata on empty queue → synthesised prev-track title with count=0 (hasMedia false→Player empty, title set→mini shows). FIX: strict count==0 short-circuit emits clean empty state + nulls stale id/art. build green; pushed c0ec195; install Success) `[DEVICE]` AWAITING-USER(re-test swipe→reopen: both views agree) |
| T290 | Spotify cold-start resume: audio plays but Player tab blank | I→M | DONE(EVIDENCE logcat 02:24-02:25: `Cold-start Spotify resumed 'California Gurls' @73419ms` fires every reopen (pids 842/2491/6222), playRequest http=204, BUT every `[HUE]` line isSpotify=false → mirror never armed → showEmptyState true → blank tab. FIX: cold-start branch now armProvisionalMirror() up-front + startPlaybackPolling(expectPlayback=true) on success + clearProvisionalMirror() on fail; mirrors LastPlayed-tap path. build green; pushed f497b0a; install Success) `[DEVICE]` AWAITING-USER(re-test Spotify resume shows in Player tab) |
| T291 | Video resizes when controls toggle; want full-bleed always + chrome overlay | M | DONE(root cause: fullBleedVideo=isVideoContent&&!controlsVisible drove BOTH root systemBarsPadding AND NSS navbar-as-sibling → showing controls squeezed the picture. FIX: fullBleedVideo=isVideoContent (always while video) → root never pads during video; controls self-inset (statusBars top, navBars+tabBarHeight bottom); slim app-tab overlay floats above transport when controls up (user chose tabs-overlay). build green; pushed 54b50ef; install Success. FOLLOW-UP 6d14354: overlay is width-aware — BOTTOM bar on compact/folded (user confirmed fine), SIDE rail on expanded/unfolded matching app's normal rail; transport reserves start-width on wide / bottom-height on compact) FOLLOW-UP 18f6907: overlay now renders the REAL Material3 NavigationRail/NavigationBar (not a bespoke icon strip) with the SAME accent-tracking colours as the app-wide NavigationSuiteScaffold → visually identical (top-aligned, icon+label) + recolours live with the user's Theme accent (TealAccent is a State-backed holder, Color.kt:13-18). `[DEVICE]` AWAITING-USER(verify unfolded rail matches other tabs) |
| T293 | "2 media players" status-bar conflict (Spotify + video both playing) | I | DECISION(user 2026-06-15: keep AGGRESSIVE auto-resume — automatic is the better end-user default; no code change. The 2nd notification is Spotify's OWN (Connect always posts one while it plays) — inherent, not a PMP duplicate. The only real bug would be a video AND Spotify both AUDIBLE at once; local-play paths already pause+stopPolling Spotify (Library/LastPlayed/Cloud VMs), so no convicted overlap. Re-open only with an exact repro of simultaneous audio) AWAITING-USER(repro of video+Spotify both audible, if it recurs) |
| T294 | Whole-screen flicker/refresh when returning to Player tab while video plays | I→M | DONE(EVIDENCE logcat InsetsController toggles from PlayerScreenCompact:512/514 + tab nav not an Activity recreate → flicker = composition relayout. Root cause: my T291 change (fullBleedVideo=isVideoContent always) flips the ROOT Box systemBarsPadding on every Player-tab return → whole-tree relayout (before T291 it stayed padded on return since controls were visible). FIX: root Box now ALWAYS full-bleed; per-tab systemBarsPadding moved into AppNavigation content Column (video drops it, other tabs keep it) → persistent chrome stable, only the swapping content re-measures. Material3 consumes nav inset at the bar so no double-count. VERIFIED via device screenshot: Library (unfolded/rail) insets correct — title clears status bar, rail top-aligned, mini-bar above system nav, no gap. build green; pushed 8488398; install Success). ATTEMPT 1 (relayout) DID NOT fix per user — flicker persists on RETURN only. ATTEMPT 2 1335524: convicted the compose-navigation NavHost DEFAULT 700ms cross-fade — every tab swap fades the new screen in over the old; returning to a playing video reads as a full-screen "refresh" (a list fading in on leave is unremarkable → explains the return-only asymmetry). FIX: enter/exit/pop transitions = None → instant tab swaps. build green; pushed 1335524; install Success) `[DEVICE]` user re-tested: STILL refreshing → ATTEMPT 2 (transition) also FAILED. User: keep dual-surface (effects depend on it), no drastic refactor, investigate+plan only. PLAN written: `docs/superpowers/plans/2026-06-15-video-tab-return-flicker-plan.md` (systematic-debugging + Context7). EVIDENCE: Compose-nav NavHost rebuilds the disposed Player destination on return → AndroidView factory makes a NEW full-screen TextureView → setVideoTextureView → blank surface until Media3 onRenderedFirstFrame = full-screen blink (return-only: leaving builds only the tiny mini surface). View-reuse Context7-discouraged; single-surface USER-REJECTED. PLAN phase-locked: P1 confirm via instrumentation + clean tab-switch capture → P2 freeze-frame, dual-surface untouched → P3 device verify. PHASE 1 DONE (user authorised; diagnostics added): clean Player→Library→Player capture CONFIRMED — return recreates the full TextureView (new hash each time) + rebinds codec; frame burst showed the VIDEO RECT SOLID BLACK across the return (ended/paused video → no frame on the new surface); onRenderedFirstFrame does NOT fire on the live swap (it fired 146ms after the new surface ONLY when playing); H3 false (uiState preserved). PHASE 2 DONE: freeze-frame — PlaybackService VideoFrameMetadataListener→videoFrameTick (reliable per-frame signal); VideoSurface captures outgoing frame in bind, overlays it on the new blank surface, drops on first tick after mount (paused→no tick→last frame persists, not black); observed via flow.first (no per-frame recomposition). Dual-surface UNTOUCHED. PHASE 3 DONE on Z Fold6: post-fix frame burst shows the picture VISIBLE through the entire return (montage3; was solid black pre-fix montage). pushed 2315739; install Success. Diagnostics debug-only (R8-stripped). BUT user re-tested: STILL flickers (freeze-frame=patch, doesn't stop the rebuild). USER DIRECTION: fix PROPERLY, full code audit, no feature breakage, context7+superpowers, evidence-based. DONE: 4-agent parallel audit (reports archived) + Context7 (movableContent). CONVERGED DESIGN (`docs/superpowers/plans/2026-06-15-video-tab-return-flicker-plan.md` §6c): single video host in AppNavigation (always-composed, repositioned by route via Modifier/zIndex — NOT movableContent → guaranteed no recreation), PlayerScreen video branch = transparent placeholder (keeps controls/scrim/flags/tap), scaffold container transparent when fullBleedVideo, 3 slots (full/tabletop/mini), KEEP PiP branch + VideoSurfaceBinding + freeze-frame. Phase=M (implement) DONE: HoistedVideoStage.kt (ONE VideoSurface call site, repositioned by route via Modifier/zIndex — full behind PlayerScreen on Player tab, draggable mini elsewhere); PlayerScreen video+tabletop branches → transparent placeholders; tabletop Column lost its OledBlack bg (so host shows through top-leaf); FloatingVideoMiniPlayer mount removed; PiP/VideoSurfaceBinding/freeze-frame/flags KEPT. assembleDebug green; pushed 7fd0a2b; install Success. DEVICE-VERIFIED MECHANISM: Player↔Library↔Player round-trip logs ZERO `VS factory CREATE`/`RELEASE` (TextureView reused, NOT recreated — the flicker CAUSE is gone, vs prior masks) + only `PlayerScreen COMPOSE enter`; video renders full-screen correctly (screenshot); flashcheck.py on a 410-frame display-0 recording found NO one-frame dark dip in any transition. `[DEVICE]` AWAITING-USER(final eyes-on confirmation of no flicker on return; + re-check PiP enter/exit, tabletop fold, mini drag/dismiss/expand, cast, drill-in back). NOTE: diag logs retained debug-only. ★OUTCOME★ user re-tested: flicker UNCHANGED + the hoist BROKE cast-video-on-phone + reported Hue not working. REVERTED whole T294 saga to 7bdb431 (commit afc2a1e): restored MainActivity/AppNavigation/PlayerScreen/VideoSurface/PlaybackService/PlaybackConnection, deleted HoistedVideoStage. assembleDebug green; install Success. KEY LEARNING: the surface-recreation fix PROVABLY stopped TextureView recreation (zero VS factory CREATE) yet flicker persisted → flicker is NOT the surface; ALL prior theories wrong. HUE: logcat shows isCast=true throughout → Hue reactive is gated off during Cast BY DESIGN (PlaybackService ~1224/1233 `!isCast`; remote audio → local FFT silence) — NOT a regression; needs confirmation Hue works with LOCAL playback. T294 status: flicker REMAINS UNFIXED (cosmetic); reverted to stable. Next: confirm cast-video+Hue restored; flicker needs a fresh USER recording analysed for the REAL cause. ★USER RECORDING ANALYSED★ (pulled from phone via adb — no Claude upload, no API400; frames via ffmpeg). Frame-by-frame (deeplogs/userflick.mp4 → fwx34): the return is a NavHost CROSS-FADE — the outgoing tab (Library list) fades out UNDER the incoming Player controls over ~0.5s with the video BLACK during the blend. THAT overlap is the flicker (not the surface). Disabling the surface-recreation never helped because the BLEND was the visible artifact. FIX (aa77a30): re-applied NavHost enter/exit/pop = None (instant swaps, no blend) — this time CLEAN + isolated on the stable build (prior attempt was bundled with broken changes + unverified). install Success. Residual: a brief black-video frame (surface re-render) may remain post-blend — to fix separately WITHOUT the cast-breaking hoist if needed. AWAITING-USER(test return flicker; confirm cast-video + local-playback Hue restored). ★LAYERED FIX (Scope A+B), user-approved★ Two independent investigations (Context7-docs `a073e6bd` + first-principles `aab568f9`) CONVERGED: flicker = window/content RELAYOUT from layout-level chrome changes (system-bar hide, navLayoutType=None swap, root systemBarsPadding toggle, per-toggle AnimatedVisibility + live-inset reads), NOT the surface. Then 2-agent deep blast-radius audit (`ab5bc6ce` layout per-screen + `aa0b3992` interaction): routes/back-stack/widget/PiP/cast/surface SAFE; risks = alpha-0 controls hit-testable + app-wide nav backbone. FIX shipped: (Scope A) video controls = `graphicsLayer{alpha}` (draw-phase, no relayout) not AnimatedVisibility, + hidden-state tap-catcher (no phantom taps/seek), + CONSTANT `systemBarsIgnoringVisibility` insets + UNCONDITIONAL tab clearance (dropped 0↔height flip). (Scope B) nav is an ALWAYS-PRESENT alpha overlay: scaffold layoutType=None for ALL routes (no swap), MainActivity root full-bleed CONSTANT (no padding toggle), PlayerScreen insets non-video at its root, non-Player routes wrapped in `NonPlayerRoute` (top status + bottom-bar/side-rail clearance = old slot, so NO screen-internal edits), FloatingVideoMiniPlayer kept SHARED (no per-route surface re-bind). Decision (i): keep hiding bars (visually inert via IgnoringVisibility). compileDebugKotlin green (both scopes); assembleDebug green; install Success on Z Fold6; app launches clean (MainActivity topResumed, zero FATAL/exception). `[DEVICE]` PARTIAL-VERIFY (Z Fold6 unfolded, screenshots in deeplogs/): (1) video plays FULL-BLEED + controls fade in as an OVERLAY with the picture NOT resizing (pmp_ctrl) = Scope A confirmed; (2) nav RAIL present + correct on the Player tab with controls up (pmp_ctrl); (3) Library tab (pmp_now) = rail present + Library teal-selected, title clears the status bar (top inset), 2-col grid clears the rail (start inset) + not clipped, MiniPlayerBar full-width above system nav — Scope B non-Player insets confirmed, audit's HIGH-risk items clear. AWAITING-USER(eyes-on the one-frame ENTRY flicker itself — my captures cannot catch a 120Hz single frame, proven; but the relayout CAUSE is structurally removed — + drill-in back, widget open, fold bar↔rail, PiP, cast-video, Hue on local, mini drag/dismiss). |
| T295 | Keep video playing on phone while casting audio to audio-only device + Cast & Bluetooth A/V offset sliders (Settings + popup, independent, two-way synced, accent-robust) + update info boxes | I→M | PLAN(`docs/superpowers/plans/2026-06-15-cast-local-video-and-av-offsets-plan.md`). EVIDENCE: video stops on cast because `PlaybackService.switchPlayer` does `current.stop()` (:1971) + `exoPlayerRef=null` (:1996) → on-phone VideoSurface unbound. BT offset pref `btVideoAudioOffsetMs` + Settings slider `BtVideoAudioOffsetRow` ALREADY exist + wired (PlaybackService:597-600 sums into AudioDelayProcessor) → BT work = add to POPUP only. Cast offset = NEW (pref+settings+popup+sync-loop). TealAccent is State-backed → sliders auto accent-robust. 7 steps: data(cast pref)→feature(local muted video synced to cast pos+offset)→BT popup slider→Cast popup slider→Settings cast slider→info boxes→device-verify(Kitchen Stereo). Caveat: cast auto-sync best-effort, cast slider is the manual fine-tune (intended UX). ★IMPLEMENTED (all 7 steps)★ (1) data: `castVideoAudioOffsetMs` pref (±1000, default 0) + SettingsViewModel field/combine[v75]/setter. (2) feature: `PlaybackService.switchPlayer` — TIGHTLY GATED `keepLocalVideo = target is CastPlayer && current.isPlayingVideo() && isAudioOnlyCastDevice()` (CastDevice CAPABILITY_VIDEO_OUT); when true, local player NOT stopped → muted (volume 0) + `exoPlayerRef` kept bound + `startCastLocalVideoSync` (1s cadence, mirrors idx/playWhenReady, seeks on >400ms drift, +castOffset); every other path (audio cast / TV / non-video) byte-identical to before. stopCastLocalVideoSync on any switch (unmutes). (3) BT offset slider → BluetoothButton sheet (shared `AvSyncOffsetControl`, reuses existing `btVideoAudioOffsetMs`). (4) cast offset slider → CastSwitcher sheet. (5) Settings `CastVideoAudioOffsetRow` next to the BT one. (6) info boxes (InfoContent Cast+BT entries) updated. Two-way sync = both popup+settings bind the one DataStore key via SettingsViewModel. Accent-robust: colorScheme.primary IS TealAccent (Theme.kt rebuilds on accent change) → Slider auto-recolours. compileDebugKotlin+assembleDebug green; install Success; launches clean (MainActivity resumed, no FATAL, local playback unaffected isCast=false). `[DEVICE]` AWAITING-USER(cast a VIDEO → Kabir's Kitchen Stereo: picture stays on phone + audio on stereo; cast offset slider shifts lip-sync; BT offset reachable from BT popup + synced with Settings; cast offset synced popup↔Settings; accent recolour; regression: normal audio cast + TV cast + stop-cast restore). ★FEATURE REVERTED — 3 bugs CONFIRMED from logs (deeplogs/cast_bug_logcat.txt, user session 18:37:43)★ (1) A-B LOOP: the 1s sync loop seeked the local player to the cast position every second — `[PLAYER] discontinuity reason=SEEK` at :43.774→120ms, :44.777→4100ms, :45.781→3781ms (cast pos lags/jumps while buffering the 1.9GB relay file → local video yanked back+forth). (2) DOUBLE AUDIO: `applyCrossfadeTick()` runs every 100ms + when crossfade off forces the volume factor back to unity (PlaybackService:1744-1748) → overwrote my `local.volume=0f` 10×/s → phone audio + cast audio. (3) ASPECT: VideoSurface gets dimensions from the SESSION player = CastPlayer (0×0 on audio-only device) → no aspect → stretches to fill. The seek-based sync + direct-volume mute + session-sourced video size are all fundamentally wrong. REVERTED the PlaybackService feature (switchPlayer back to current.stop()+exoPlayerRef=null; removed sync fns/flag/collector); KEPT the offset sliders (BT offset in BT popup WORKS; cast offset slider present but INERT until rebuild) + the cast pref. compileDebugKotlin+assembleDebug green; install Success (casting restored). CORRECTED DESIGN for rebuild: mute via the volume-FACTOR system (not direct player.volume); feed VideoSurface the LOCAL player's videoSize (not the session/cast 0×0); NO continuous seek — local plays freely muted, mirror play/pause only, offset-change = one-shot re-sync, rare large-drift (>1.5s/5s) correction only. Realistic limit: best-effort sync (can't frame-lock local video to remote cast audio without jitter) — needs USER device verification. AWAITING-USER decision: rebuild with corrected design (accepting best-effort sync) or drop. Confirm post-revert: cast works again + aspect ratio back to normal (non-cast). ★REBUILT (attempt 2, corrected design)★ Logs confirmed the revert restored ORIGINAL behaviour (cast works — relay serving file @18:50, audio on stereo; phone video off = the original complaint). Rebuilt with the 3 fixes done right: (1) MUTE via a THIRD volume factor `castLocalVideoMuteFactor` in the companion's `applyMixedVolume` (replayGain×crossfade×castMute) — the crossfade tick re-asserts volume every 100ms but now MULTIPLIES the mute in, so it sticks (the old direct `player.volume=0` was overwritten 10×/s = double audio). (2) ASPECT: VideoSurface falls back to the RENDERING player's videoSize (via onVideoSizeChanged listener on getExoPlayer()) when the caller passes 0 — fixes the stretch (session player = CastPlayer reports 0×0). (3) SYNC: NO polling loop (the old 1s seek loop = A-B loop); now EVENT-DRIVEN — a cast Player.Listener mirrors play/pause + user seeks + track transitions to the local player, plus an offset collector seeks-on-change. Both start aligned (cast gets currentPosition). switchPlayer: keepLocalVideo branch (gated video+audio-only) keeps local playing + muted-via-factor + exoPlayerRef=local + castLocalVideoActive=true; teardown (unmute+flag clear) at top of every switch. compileDebugKotlin+assembleDebug green; install Success; launches clean (no FATAL, local playback unaffected isCast=false). KNOWN LIMIT v2: no periodic drift correction (event-driven only) → may slowly drift over a long video; offset slider / a seek re-aligns; can add smooth rate-based (speed-nudge) correction next if needed. `[DEVICE]` AWAITING-USER(cast VIDEO → Kitchen Stereo: picture stays on phone WITH CORRECT ASPECT, no A-B loop, NO phone audio (only stereo); pause/seek on cast mirrors to the picture; offset slider nudges sync; stop-cast restores local unmuted). ★ATTEMPT 2 FEEDBACK + FIXES★ User: aspect/loop/mute all fixed ✓; BUT no cast audio initially then kicked in after 20-30s; brief "nothing playing" flash on cast-on; offset confirmed (asked to verify). LOGS (deeplogs/cast_delay_logcat.txt, 19:24): (a) offset WORKS — `SEEK from=6780→7170…7630` burst at 19:24:16 = offset collector firing per slider tick (dozens of seeks/drag = the "weird rendering" + file thrash). (b) BIG DESYNC — 19:25:16 local at 66832ms while cast ~35106ms (≈31s ahead): the cast takes 20-30s to start (speaker buffering the 1.9GB file via relay) but the local picture started immediately + nothing re-aligned post-start. FIXES: (1) DEBOUNCE offset seek (.debounce(350) — flag updates immediately, seek only after slider settles → no flood). (2) PAUSE the local picture during the cast's initial buffer (playWhenReady=false in keepLocalVideo) → frees phone I/O/CPU for the relay (faster cast start) + no race-ahead. (3) RE-SYNC via cast `onIsPlayingChanged`: mirror the cast's REAL playing state (not playWhenReady which is true while buffering) — on first real play, seek local to cast pos+offset then resume (aligned); on pause/buffer, pause local. compile+assembleDebug green; install Success. Cast startup delay itself is largely speaker-side buffering of the large file (phone-side pause may reduce contention but can't eliminate it). MINOR (separate, deferred to user-confirm): play/pause button shows a stale frame when controls fade back in (likely cast-state flux during these fixes). `[DEVICE]` AWAITING-USER(re-test: cast audio starts sooner?; picture aligns to audio when it kicks in (no 25-30s lead); dragging offset is smooth (no flood); pause/seek mirror). ★ATTEMPT 3 FEEDBACK + STUTTER FIX★ User: audio faster ✓ (pause-during-buffer helped); BUT empty-state flash still; picture ended PAUSED (had to press play); stuttering after a while. LOGS (cast_stutter_logcat.txt 19:34:56 PID 29747, debounced build): debounce WORKED (no offset flood at startup); ROOT of stutter+pause = my `onIsPlayingChanged` mirrored the cast's ACTUAL isPlaying, which TOGGLES on every cast buffer underrun → local playWhenReady churned true/false/true/false (logged) → picture stuttered + last blip left it paused. Also seeked to a stale cast pos (68972ms) early. FIX: (1) `onIsPlayingChanged` now fires the align+resume ONCE (first true, when cp.currentPosition is valid) then sets castInitialSyncDone — ignores all later isPlaying blips. (2) After that, mirror `onPlayWhenReadyChanged` (the cast's INTENT — does NOT flip on buffering) → picture stays smooth, follows deliberate play/pause. (3) Guarded onPositionDiscontinuity/onMediaItemTransition/offset-seek with castInitialSyncDone (no stale-pos seeks pre-align). compile+assembleDebug green; install Success. Cast mid-playback buffering may still drift the AUDIO (cast-side, inherent) but the PICTURE no longer stutters. KNOWN COSMETIC (deferred): "nothing's playing yet" flash on cast-on + play/pause stale frame on controls-show — both tied to cast-state flux; clean up once cast behaviour confirmed stable. `[DEVICE]` AWAITING-USER(re-test: picture no longer stutters/ends-paused; smooth after it starts; offset still nudges). ★ROOT CAUSE OF THE DELAY FOUND + FIXED (CastRelayServer)★ The "massive delay" was NOT the sync — it was the RELAY. `serveLocal` opened a plain `openInputStream` + `skipFully(start)` which calls `skip()` in a loop → on a content:// stream that READS-AND-DISCARDS the bytes. MP4s with the moov atom at the END make the receiver request the end first (logs: `range=1920237568-` on a 1.9GB file) → the relay read+discarded ~1.9GB to reach it = the 20-30s wait, and it starved the receiver → constant buffer underruns → the cast `isPlaying` churn that drove the stutter/self-pause/play-pause-frame. FIX: open a SEEKABLE `openFileDescriptor` + `FileInputStream.channel.position(start)` = instant lseek (fallback to skipFully only if the fd isn't seekable); close the pfd when the served stream closes (no fd leak). This single fix targets the delay AND the downstream cast-buffering churn (stutter/pause/play-pause-frame). ALSO fixed the "nothing's playing yet" flash: PlaybackService exposes `castLocalVideoActiveFlow`; PlayerScreen suppresses showEmptyState while it's true (picture is playing locally even though the session player is the audio-only cast). compile+assembleDebug green; install Success. NOT deferred: aspect/loop/double-audio (attempt-1) + delay (relay) + stutter/pause/play-pause (relay churn root + intent-mirror) + empty-flash (suppressed) all addressed. `[DEVICE]` AWAITING-USER(cast a VIDEO → Kitchen Stereo: audio starts FAST now (no 20-30s); picture steady, no stutter/self-pause; no empty flash; offset nudges; play/pause correct). ★REAL DELAY FIX — CAST AUDIO-ONLY★ Logs PROVED the relay was NOT the bottleneck (every range served in 5ms, e.g. 19:55:25.172→.177 for the moov range). The 20-30s was the SPEAKER buffering the high-bitrate 4K VIDEO stream (it streamed from range=659MB = the byte offset of the ~17s position in a 4K file). User confirmed: cast audio-only. FIX: new `CastAudioExtractor` (MediaExtractor+MediaMuxer container-copy of the audio track → small temp .m4a, no re-encode). `startCastAudioOnly` (PlaybackService): when keepLocalVideo, instead of casting the 1.9GB video relay URL, extract the audio off-thread → cast the temp m4a via the EXISTING (proven) audio-cast relay path → speaker buffers ~1-2s. Local picture stays paused (frozen frame) during the ~few-sec extraction (frees I/O), then onIsPlayingChanged aligns+resumes it. Fallback to video cast if extraction fails (exotic codec). Temp m4a deleted + job cancelled on cast-end (teardown). Also: empty-state "nothing's playing yet" suppressed via castLocalVideoActiveFlow. compile+assembleDebug green; install Success; launches clean (no FATAL). Net audio start: ~extraction(few s)+small-m4a-buffer vs 20-30s. `[DEVICE]` AWAITING-USER(cast VIDEO→Kitchen Stereo: audio starts in a few seconds not 20-30s; picture shows; no double audio; offset nudges). NOTE: title may read "No media loaded" for the ~few-sec extraction window then resolve (cast has no item until the m4a is set). QUEUED (user said do AFTER casting fixed): BT video-audio offset appears to do nothing + BT resume-playback toggle appeared to fail — investigate logs. ★CRASH ROOT-CAUSED + FIXED (5edaa6b)★ logcat 20:22:43: extraction SUCCEEDED (wrote 4MB m4a/5988 samples — audio-only approach VALID) then `cast.setMediaItems` threw `IllegalArgumentException: The item must specify its mimeType` (DefaultMediaItemConverter) → force-close → cascaded into the reopen symptoms (corrupt cast/resume state → no video + stutter/gaps + auto-resume fail). FIX: `.setMimeType("audio/mp4")` on the m4a item. built+install. RE-TEST casting (the stutter/gaps/auto-resume-fail were the crash aftermath — should clear). |
| T296 | BT button = disconnect-when-connected (keep BT on) + BT A/V offset does nothing + BT resume toggle fails + play/pause stale frame | I | PLAN(`docs/superpowers/plans/2026-06-15-cast-and-bluetooth-fixes-plan.md`). 3 parallel read-only investigations: (a) BT OFFSET (`ae5b16cd`): wired correctly to AudioDelayProcessor BUT it only delays AUDIO + clamps negatives to 0; for BT the audio is LATE so you must delay the VIDEO → positive=worse, negative=no-op, label inverted. FIX = custom MediaCodecVideoRenderer presentation-time offset (real video delay). (b) BT RESUME (`a12cfb32`): `resumeOnBt` is only a permissive gate for an inbound KEYCODE_MEDIA_PLAY; `onAudioDevicesAdded` (BT-connect) only logs, never play()s → plain BT devices never auto-resume. FIX = proactive play() of the LOCAL player on BT A2DP connect when paused+loaded. (c) BT DISCONNECT (`a25b89bf`): true ACL disconnect impossible (hidden @SystemApi+BLUETOOTH_PRIVILEGED); achievable goal = reroute via `ExoPlayer.setPreferredAudioDevice(builtinSpeaker)` (public Media3, minSdk30 OK), tap-when-connected → speaker, long-press → keep the sheet. (d) PLAY/PAUSE: button reads session(cast) isPlaying which churns on cast buffering; verify after cast fix, else derive from playWhenReady/local. Order: cast crash DONE→re-test; then BT resume + BT reroute (low risk); then BT offset video-renderer (riskier); then play/pause if it survives. AWAITING-USER(re-test cast + approve plan / prioritise). ★BATCH IMPLEMENTED★ (1) NO-VIDEO on cast (d5eba3a): PlayerScreen presents displayState (isVideoContent=true) while castLocalVideoActive → shows the silent local video instead of the audio layout. (2) STALE-VIDEO on track-change-while-casting: logs (20:45:37/52 onAddMediaItems casting=true) showed new tracks went only to the cast/session; FIX = onAddMediaItems, when castLocalVideoActive: VIDEO → set the local player to the new item (picture updates) + cast AUDIO-ONLY via SettableFuture(buildCastAudioRelayItem) + castInitialSyncDone=false; AUDIO → disengage local-video (flag off + unmute) → normal audio UI. Refactored startCastAudioOnly → shared `buildCastAudioRelayItem`. (3) BT RESUME: onAudioDevicesAdded now proactively play()s the LOCAL player when a TYPE_BLUETOOTH_A2DP sink connects + resumeOnBt + paused + has media (was log-only). (4) BT DISCONNECT: PlaybackService.rerouteAudioToPhoneSpeaker (ExoPlayer.setPreferredAudioDevice(builtin speaker) — stable, Context7-confirmed); BluetoothButton now combinedClickable: tap-when-a2dpActive → reroute to phone speaker (BT stays on) + toast, long-press → sheet. (5) Context7 cross-exam: setPreferredAudioDevice stable; video-delay = NO built-in, only @UnstableApi MediaCodecVideoRenderer override (risks ALL video if mis-constructed). compile+assembleDebug green; install Success; launches clean. HELD (risk, not convenience): (6) BT OFFSET → video-delay needs the fragile renderer (could break all video, can't verify here) — to do ISOLATED + verified, NOT in the untested batch. (7) PLAY/PAUSE stale-frame: likely resolved by stable audio-only cast; verify in batch. `[DEVICE]` AWAITING-USER BATCH TEST(cast video → speaker: picture + audio; play another track/video while casting → picture follows; BT connect auto-resume; tap BT-when-connected → phone speaker; play/pause correct). ★TAP-VS-POPUP FIX (931464e)★ the combinedClickable tap=reroute HID the sheet — tapping while BT audio active fired the reroute toast not the menu (logcat: `Audio rerouted to phone speaker` ×5, no sheet). Reverted to IconButton: tap ALWAYS opens the sheet; disconnect moved INTO the sheet body. ★REROUTE NOW A REVERSIBLE TOGGLE (373a2a6)★ user: "connected to edifier but playing out the phone". ROOT (logcat 21:31:49): OS route = bluetooth a2dp to EDIFIER (`ROUTED_TO_BLUETOOTH:true`, device type bluetooth a2dp (8)) yet app on phone = the app's own `setPreferredAudioDevice(BUILTIN_SPEAKER)` reroute overriding the system route for this app; reroute was ONE-WAY (clearAudioReroute only on a NEW BT device-add → already-connected device = no undo path → pinned to phone). FIX: `PlaybackService.audioRerouteActiveFlow` StateFlow (true in reroute, false in clear); BT sheet shows a reversible toggle — audio on BT → "Play on phone speaker"; override active → status line + "Play on Bluetooth" (clears override → audio back to device); BT button icon dims (alpha 0.5) when connected-but-pinned-to-phone. compile+assembleDebug green; install Success. `[DEVICE]` AWAITING-USER(tap BT button → sheet opens every time; "Play on phone speaker" then "Play on Bluetooth" round-trips audio EDIFIER↔phone; icon dims when on phone). ★CAST-vs-BT CONFLATION FIX (9b3dafa)★ user (fresh launch): "bluetooth connected, audio out the phone, ALSO appears connected to cast at the same time." ONE cascade, not 3 bugs. EVIDENCE (triroute_logcat/diag 21:40:31-51, EDIFIER connected): app NOT casting (isCast=false, casting=false everywhere — cold-start restored a LOCAL video); 21:40:33 audio WAS on BT (EffectManager path bluetooth (2) a2dp (8)); 21:40:34.6 touch opens a sheet; 21:40:36.99 system routing session flips selectedRoutes→BUILTIN_SPEAKER (getAudioDeviceType=builtin speaker (2)) = audio moved to phone mid-video; BT route is playbackType=0 (LOCAL). ROOT (CastSwitcher.kt:135) `isCasting = !router.selectedRoute.isDefault` → a connected BT speaker is ALSO a non-default system route → cast button lit "CastConnected" + sheet showed a "Stop casting" row with NO cast session; tapping phantom Stop ran `selectRoute(defaultRoute)` → BUILTIN_SPEAKER → yanked audio off BT onto phone. FIX: `isCasting = !isDefault && playbackType == MediaRouter.RouteInfo.PLAYBACK_TYPE_REMOTE` (BT+phone are LOCAL; only Chromecast/remote = REMOTE) → button stays "Cast" when only BT connected, no phantom Stop, audio never force-rerouted. Device list already excluded BT via the cast selector; only the icon state was wrong. compile+assembleDebug green; install Success. `[DEVICE]` AWAITING-USER(fresh launch with BT connected: cast button shows plain "Cast" not connected; audio stays on BT; opening cast sheet shows no phantom "Stop casting"). ★CAST-STOP→BT + BT-DURING-CAST FIX (7ced633)★ user: "on BT then casted — still showed BT-connected in-app (fine as system, not in-app); after stopping casting said still connected to device but played on phone speaker." EVIDENCE (caststop_logcat 21:48, EDIFIER + cast to Kabir's Kitchen Stereo): cast worked (audio-only extract 465KB m4a fast, seek=lseek, picture resumed, isCast=true); 21:48:45.857 Selecting route: Kitchen Stereo; 21:48:55.483 Cast onSessionEnding; 21:48:55.505 `Selecting route: Phone` ← StopRow forced defaultRoute=phone; 21:48:56 builtin speaker (2) = audio on PHONE not EDIFIER (still connected); returned to BT only 43s later. FIX B (functional): CastSwitcher StopRow → `router.selectRoute(router.routes.firstOrNull { it.isBluetooth } ?: router.defaultRoute)` (Context7-confirmed isBluetooth() stable in mediarouter 1.7) → audio returns to the connected BT speaker, not the phone. FIX A (display): `PlaybackService.castActiveFlow` (set in switchPlayer = target is CastPlayer); BluetoothButton `btRouting = a2dpActive && !castActive` drives the icon (full accent only when BT is the live app output, dimmed "on/not-routing" during cast) + sheet shows a "Casting — audio is on the cast device" note instead of the moot reroute toggle. compile+assembleDebug green; install Success. `[DEVICE]` AWAITING-USER(BT→cast: BT button dims to "not routing" + sheet shows casting note; STOP cast → audio returns to EDIFIER not phone). ★ITEM 6 (BT A/V offset → real VIDEO delay) DONE + VALIDATED + HARDENED (78ec0fd)★ The held item was actually already implemented in 5ed5c87 (`OffsetVideoRenderer`: a MediaCodecVideoRenderer that adds the BT offset µs to each frame's presentation time → holds the picture to match late BT audio; audio-delay slider split off to feed ONLY AudioDelayProcessor) and my T312 L-fix added the `btVideoRouteActive` gate — the ledger row was stale. "Proceed with T296" = validate+harden the fragile renderer properly. CONTEXT7 (/androidx/media): Media3 1.6.0 has NO built-in A/V-sync / video-render-offset API (setOffloadDelayPadding=audio offload padding; onVideoFrameProcessingOffset=read-only analytics; LivePlaybackSpeedControl=live edge) → subclassing MediaCodecVideoRenderer + shifting frame PTS IS the correct supported approach, no rewrite. SUPERPOWERS adversarial review (decompiled 1.6.0 bytecode): NO P0 — normal non-BT video genuinely unaffected (offset 0 → byte-identical; 11-arg override binds the real signature; the spare stock renderer is selector-DISABLED not double-decoded). 3 fixes shipped: (1 P1-A) gate-flip jolt — connect/disconnect stepped the offset 0↔N in one frame → froze the picture N ms (connect) / fast-forwarded (disconnect); now OffsetVideoRenderer EASES the applied offset ≤5 ms per PROCESSED frame (~1.7 s for 250 ms), advanced only when super reports processed so a held frame can't over-ramp; (2 gate accuracy) offset applies ONLY when BT is the app's genuine output — `&& !audioRerouteActiveFlow.value && !castActiveFlow.value` (deterministic app-state flags) so a phone-speaker reroute or a cast doesn't wrongly delay the picture; (3 P2-B) delay-only — you can hold the picture but not advance it, so negative was inert/misleading → btVideoAudioOffsetMs pref + BT popup slider (shared AvSyncOffsetControl gained a `range` param) + Settings BtVideoAudioOffsetRow all 0–1000 ms, applied clamped ≥0. P2-A spare renderer KEPT deliberately = the runCatching fallback if OffsetVideoRenderer construction throws (video survives sans offset). CAST offset UNTOUCHED — different mechanism (position-seek between the local video player + the separate CastPlayer, bipolar ±1000), NOT the single-stream frame delay. compile+assembleDebug green; install Success. `[DEVICE]` AWAITING-USER(watch a VIDEO over a BT speaker: slide the BT offset right → picture delays smoothly to match the audio, no freeze when the BT connects/disconnects mid-video; reroute to phone speaker mid-video → picture is NOT delayed; normal non-BT video unchanged). ★DEVICE-TESTED → REWRITTEN → AUDITED★ User device test (btvid_logcat 02:06): slider "did absolutely nothing". Instrumented the whole path (renderer-selected? gate vs ground-truth? applied value?) — log PROVED the gate was OPEN (routeActive=true, groundTruth.btOutput=true, reroute/cast=false) AND our OffsetVideoRenderer was drawing the video, BUT the 78ec0fd applied-offset ramp (≤5 ms/processed-frame) throttled the slider: sliding to 1000 ms left applied at only 45000us (45 ms), climbing ~5 ms/frame (~7 s to reach 1 s) = imperceptible → looked dead. ROOT: the smoothing ramp wrongly throttled DELIBERATE slider moves (a tuning control must be instant). FIX (92c2846): OffsetVideoRenderer split into TWO inputs — offsetUsSupplier (slider µs, applied IMMEDIATELY) + gateSupplier (is BT the live output); ONLY the gate is eased (gateScale 0↔1 at 0.05/processed-frame); applied = offset × gateScale. Slider now instant; BT connect/disconnect still fades (no freeze/jump); gate-off → scale 0 → byte-identical normal video. ADVERSARIAL AUDIT (superpowers code-reviewer, 8 dimensions): CLEAN — no P0/P1/P2. Cleared incl. 2M-sample proof the gate ramp converges EXACTLY to 0.0/1.0 (no float stall that could leave a residual offset on normal video), slider-immediacy confirmed, normal video byte-identical, cast slider range NOT narrowed (still bipolar ±1000), BTVID diagnostics R8-stripped in release. compile+assembleDebug green; install Success. Diagnostics (BTVID) retained debug-only pending BT visual re-confirm. `[DEVICE]` AWAITING-USER(BT visual re-confirm on the slider-immediate build: video over BT → drag to ~1000 ms → picture lags ~1 s INSTANTLY, not a 7 s crawl; connect/disconnect BT mid-video → smooth, no freeze). |
| T297 | Brightness slider must be disabled/greyed until WRITE_SETTINGS granted | M | DONE(e17eb32). SecondaryControls.VolumeAndBrightnessControls: the brightness Slider was always `enabled=true` (caller passes brightnessEnabled=true) → draggable without permission, write silently skipped. FIX: hoisted the existing `canWrite` permission state (re-checks every ON_RESUME) ABOVE the slider; `enabled = brightnessEnabled && canWrite` → greyed + non-draggable until WRITE_SETTINGS granted, un-greys on grant; flanking BrightnessLow/High icons grey too. "Tap to grant brightness permission" banner unchanged. compile+assembleDebug green; install Success. `[DEVICE]` AWAITING-USER(brightness slider locked+grey until permission; works after). |
| T299 | "Resume on Bluetooth connect" off but playback still resumed on connect | I→M | DONE(948beff). EVIDENCE (resume_logcat, 5 BT connects 21:40-22:02): resumeOnBt-gated paths (onAudioDevicesAdded resume + KEYCODE_MEDIA_PLAY) fired ZERO times — correctly respected off. Every resume = COLD-START restore auto-playing (playWhenReady=true) on each MainActivity.onCreate, gated by `autoplayOnLaunch` (ON), NOT resumeOnBt; BT device+ fires ~1s before each (foregrounding routes audio to BT). The 22:03:26 resume even followed a USER TAP (pid 16774 touch). So the two settings were unrelated → toggling resumeOnBt off had no effect on launch autoplay. USER-SPECIFIED FIX (independent toggles, one applies at a time by output state): phone on a BT audio sink → launch obeys resumeOnBt; not-on-BT (casting counts as not-BT) → obeys autoplayOnLaunch; both BT+cast (rare) → BT wins. IMPL: `PlaybackSessionCoordinator.resolveLaunchAutoplay()` checks for a connected TYPE_BLUETOOTH_A2DP output → returns resumeOnBt or autoplayOnLaunch; both cold-start branches (LOCAL/DRIVE + SPOTIFY) use it; logs choice via DiagLog.dec. Settings descriptions for both toggles updated to explain the BT-vs-not split. onAudioDevicesAdded BT-connect resume + KEYCODE_MEDIA_PLAY gate unchanged; car control untouched. compile+assembleDebug green; install Success. `[DEVICE]` AWAITING-USER(on BT + resumeOnBt OFF → relaunch does NOT auto-play; off BT + autoplayOnLaunch ON → relaunch DOES; flipping each respects the active context). |
| T300 | Drive audiobook full metadata not loading (only artwork) — REGRESSION | I→M | DONE(f60712c). User: "full metadata didn't load, just the image — it used to; check what CHANGED." ROOT (drivemeta_logcat, Harry Potter 7 m4b on Drive): embedded-tag enricher (MediaMetadataRetriever) needs a COMPLETE MP4; moov is at the END so head-32MB pass returns null tags (22:27:47 MMR null) → falls back to FULL-file download which threw at 22:28:17 = EXACTLY 30s after it began. WHAT CHANGED = commit e726780 ("shared OkHttp pool + call timeouts") switched DriveOAuthProvider to SharedHttp.base which sets callTimeout(30s); a hundreds-of-MB audiobook can't download in 30s → enrichment aborts → title falls back to filename (chapters still show — disk-cached separately). FIX: DriveOAuthProvider.downloadHttp = SharedHttp.base.newBuilder().callTimeout(0) for file-CONTENT downloads (downloadRangeToCache); matches PodcastDownloader/CastRelayServer which already override (SharedHttp comment: "long media transfers override with 0"). 20s read-timeout still guards a stalled socket; API/JSON calls keep the 30s client. compile+assembleDebug green; install Success. ★RE-TEST (cast2_logcat 22:29-22:42)★ user "metadata not fixed": logs show only `Cold-start restored` (filename) since the new build — the openItem/parseAndApply/30s-exception lines are STALE (old session 22:27-28, pre-fix, still in the logcat ring buffer). The user COLD-STARTED (auto-resume), which does NOT run parseAndApply — enrichment ONLY runs on a fresh Cloud-tab openItem. So the callTimeout fix is correct but was not exercised. Confirmed in code (PlaybackConnection.updatePlayerState:1063 `title = overTitle ?: metadata.title ?: itemMetadata`): cold-start builds the MediaItem with `.setTitle(recent.title=filename)` → c.mediaMetadata.title=filename (matches log ctrlTitle). REMAINING WORK (gated on verifying the open-path fix first): (a) confirm openItem full-download now completes for a large m4b; (b) propagate the enriched title/artist to the LastPlayed row (and/or cache embedded tags like chapters) so COLD-START shows them without re-downloading. `[DEVICE]` AWAITING-USER(OPEN the audiobook FRESH from the Cloud tab — not auto-resume — and wait for the download; embedded title/author/album should load. Then tell me, and I'll wire cold-start to reuse it). ★CONFIRMED FIXED + STICKINESS (b49b796)★ cast3_logcat 22:50:31: callTimeout fix WORKS — full 689MB download completed, MMR got title='Harry Potter and the Prisoner of Azkaban, Book 3' artist='J.K. Rowling' album='…(Unabridged)' art 2400x2400 chapters=24. User then: "loaded but after cast went away partly." LOGS: cast ended 22:51:17 → 22:51:27 Cloud.openItem fired AGAIN (cast-return re-opens) → re-set filename placeholder (artBytes=0) → blanked → re-downloaded the WHOLE 689MB again, re-enriched only 22:51:47. FIX: CloudViewModel.enrichedByMediaId session cache (LocalMetadataOverride keyed by Drive id); parseAndApply stores on success; openItemInternal reuses it for the placeholder (no filename flash) AND skips the Drive re-download. Re-open/cast-return now instant + sticky, no network. compile+assembleDebug green; install Success. REMAINING: cold-start (fresh launch) still shows filename until re-enriched — persist tags to disk/LastPlayed = follow-up. `[DEVICE]` AWAITING-USER(open audiobook → cast → stop cast → metadata stays, no blank/re-download). |
| T301 | Swipe-away should also stop Spotify Connect playback | I→M | DONE(f60712c). User: "shouldn't it close Spotify too if a Spotify track was playing when I swiped away?" ROOT (PlaybackService.onTaskRemoved): with a Spotify mirror, mediaSession.player is a SILENT local mirror; player.stop() stopped the mirror but never paused the Connect device → Spotify kept playing on the speaker. FIX: stopOnSwipe branch, if spotifyProvider.spotifyState.value!=null → best-effort spotifyProvider.pause() on GlobalScope (outlives serviceScope.cancel; stopSelf doesn't kill the process instantly so the HTTP PUT usually lands). compile+assembleDebug green; install Success. `[DEVICE]` AWAITING-USER(play Spotify → swipe app away → Spotify Connect pauses). NOTE: best-effort (remote pause on app-kill races process death); usually lands. |
| T302 | Cast Drive audiobook → "nothing's playing yet" for a long time before reload | I | INVESTIGATING. User report; NO cast events in the current log capture (cast was an earlier session). CODE-LEVEL HYPOTHESIS (strong, unconfirmed by log): audio-only cast `CastAudioExtractor.extractAudio` uses `context.contentResolver.openFileDescriptor(source,"r")` (CastAudioExtractor.kt:36) — for a Drive OAuth item the source is an `https://` URL, which openFileDescriptor CANNOT open → extraction returns null → buildCastAudioRelayItem falls back to casting the FULL file via the LAN relay (which itself streams from Drive) → receiver buffers the whole audiobook → long "nothing's playing yet" before it loads. Proper fix (TBD): extract via a source MediaExtractor CAN read (relay URL with auth, or download-then-extract like the metadata path). NOT fixed without a repro log (no-guesswork; cast path). `[DEVICE]` AWAITING-USER(repro: enable Settings→Diagnostic logging, cast the Drive audiobook to an audio-only device, then I pull + confirm the extraction-fail→relay-fallback path before fixing). ★RE-TEST (cast2_logcat 22:43:20)★ cast activity present (CastMediaSession/272 + cast_rcn_media_session) but TANGLED with the Spotify swipe at the same instant — NO clean `startCastAudioOnly`/`CastAudioExtractor`/`Cast to AUDIO` trace + no `nothing's playing` marker captured. Still need an ISOLATED repro (cast ONLY the Drive audiobook to an audio-only device, diag logging on, nothing else) to confirm the openFileDescriptor(https)→fail→relay hypothesis. ★RE-TEST (cast3_logcat 22:50-22:51)★ user "big delay on cast" reproduced. Cast END at 22:51:17 captured but the cast START + any extraction did NOT appear in greps — need to widen. NEW UNDERSTANDING: this item is an AUDIO m4b (689MB), no video track → `current.isPlayingVideo()=false` → the keepLocalVideo audio-ONLY-extraction path does NOT engage (it's for VIDEO→audio-only-device). So an audiobook casts via the NORMAL relay = the WHOLE 689MB streamed from Drive→phone relay→receiver → receiver buffers a huge remote file = the big delay. The audio-only extract trick can't shrink an all-audio file. HARDER fix (TBD): reuse the enrichment's full-file download for the relay (serve from local cache instead of re-streaming Drive), or pre-buffer. NOT done — needs a clean cast-start trace + design. `[DEVICE]` AWAITING-USER(isolated cast-start repro with diag logging for the proper fix). ★ASSESSED — LARGELY INHERENT★ CastRelayServer.serveDrive (158-193) already FORWARDS the receiver's Range header to Drive verbatim (efficient — not a re-download-from-0). So the delay is the cast RECEIVER buffering a 689MB moov-at-end Drive file: it must fetch the moov (end of file) then stream audio, gated by Drive→phone→receiver bandwidth. The audio-only extract trick can't shrink an all-audio file, and openFileDescriptor can't read https anyway. Only real fixes = download+remux to faststart locally then cast (heavy), or pre-download — both big. NOT a relay bug; inherent to casting a large moov-at-end remote audiobook. Deferred to a dedicated decision (worth it vs the download cost?). ★NEW EVIDENCE (2026-06-29, during the M4 cast test)★ on Living Area TV the cast session reported `state=PLAYING(3)` yet the playhead was FROZEN (position 14938509 across repeated samples) with `buffered position=0` — the TV had loaded nothing — while a LOCAL player session advanced in real time (15028651→15031652). So the receiver never started rendering the 689MB Drive M4B and playback effectively fell to the phone. Consistent with the inherent moov-at-end/large-file buffering ceiling above (cast-start buffer rolled out of logcat before capture, so no fresh load-error trace). Still AWAITING an isolated cast-start repro (diag on, cast ONLY the Drive book) before any fix. ★REPRO CAPTURED + ROOT-CAUSED 2026-06-29 — NOT inherent★ cast 'This Inevitable Ruin' (1.64 GB Drive m4b, moov-at-end) to Living Area TV with diag on: `CastRelay Drive upstream=206 mime=video/mp4 contentLength=1398595115 range=bytes 241238016-1639833130/1639833131` — the receiver requested a ~1.4 GB TAIL (from the ~4h16m resume-point byte offset 241 MB to the end) and the cast session sat `PLAYING(3)` with `buffered position=0`, position frozen, for the full 48 s = never started. ROOT CAUSE: moov-at-end + large file + resume-seek → the receiver must read the `moov` (at the END) over the Drive→phone→TV relay, then stream a 1.4 GB range → never finishes buffering. FIX (designed, evidence-based): faststart-remux the cached/offline copy (relocate `moov` before `mdat`, adjust stco/co64 offsets) + serve THAT local file via the relay → receiver reads moov at the front, seeks, streams from the seek point. SUBSTANTIAL + DELICATE (cast relay = most regression-prone code, cf T295/T296) → implement with a full cast test pass (video / audio-only / this book), not a rushed edit. Phase moves I→PLAN. ★FIXED + DEVICE-VERIFIED 2026-06-29 (user hears it playing)★ Shipped `Mp4Faststart` pure util (commit 1225e18, 6/6 TDD) + an additive/gated/fail-safe orchestrator in PlaybackService (commit 3570483 + chapter-gate fix): on switch-to-cast of a Drive item with `chapter_count>0` (audiobook — an `.m4b` reports mime video/mp4 with no URL extension, so chapters are the signal), async resolve a local copy (offline copy, else one-time Wi-Fi download) → faststart-remux (moov→front, stco/co64 +moovSize) → serve via the relay's `serveLocal` (lseek) → `cast.setMediaItems(faststart, pos)` (mediaId preserved). DEVICE: download 1.64 GB in ~66 s → `faststart … → REMUXED` → relay `GET /2 range=bytes=0-` (moov read at FRONT, lseek) → seek to `265224192` = old Drive seek-byte `245956608` + moovSize `~19.27 MB` (offset patch EXACT) → **user confirms audio playing on Living Area TV**. The old indefinite freeze is gone. CAVEAT recorded: CastPlayer position/buffered/status via `media_session`/`dumpsys` is UNRELIABLE — it read frozen + 0 status-updates while genuinely playing; verified by the user's ears, not the session readout. Non-regression cases (video cast, audio-only-device extract) are gated OUT by design (chapter_count + Drive + !keepLocalVideo) but were not separately re-driven on device this session. ★STREAMING FASTSTART PROXY 2026-06-29 — SHIPPED + DEVICE-VERIFIED (no download, no swap, "best of both worlds" per user)★ Superseded the download+remux+swap (slow first cast + black/metadata flash) with a STREAMING relay. rebuildForCast routes a Drive audiobook (driveItemIsAudiobook: audio-mime OR cached chapters) to RelayItem.FaststartDrive; CastRelayServer head-prep fetches ONLY ftyp+moov (~19 MB) via tiny Range reads, walks top-level atoms, patches stco/co64 in-memory (Mp4Faststart.patchMoovBox), then serveFaststart serves the synthesised [ftyp][moov'][mdat] (FaststartStream.segments/open) with the mdat proxied LIVE from Drive (driveRangeStream); fail-safe → plain Drive passthrough on any anomaly/oversize/already-faststart. The FaststartDrive URL is the cast item on the SINGLE setMediaItems → NO swap → no black/metadata flash. Removed the unconditional download+swap orchestrator call (now only the rare >48 MB-moov onOversize fallback). TDD: 20 pure tests (FaststartStreamTest byte-mapping+stitching 7; Mp4FaststartTest patchMoovBox/parseBoxHeader 6 + faststart 6 routed through patchMoovBox); Context7 (only stco/co64 need +moovSize; Media3 1.6.0 needs non-null mime=audio/mp4). Design pass by subagent (Context7-backed). DEVICE (Living Area TV / BRAVIA 4K, faststart cache cleared = TRUE cache-miss): logcat `CastRelay GET /1 item=FaststartDrive`, `Faststart head ready ftyp=32 moov=19277830 total=1639833131` (~3.6 s), `CastRelay faststart status=PARTIAL_CONTENT range=0-1639833130/1639833131`, cover `GET /2 item=Local image/jpeg`, title in media chip, receiver PLAYING (user heard it — "far too loud"). NO 1.4 GB tail, NO full download, NO swap. Commits 8afd6fe (+ 6b04819 chapters/cover). MISS recorded: did not pre-set cast volume low before the audibility check (user hit loud audio) — must follow [[reference_cast_playback_verification]] (min non-zero ~3) next time. |
| T354 | Cast Stop hijacked: after Stop casting the book jumped TV→speaker | I→M | DONE(a9a08c8) + `[DEVICE-VERIFIED]`. User-flagged + root-caused from logcat: Stop reverted to Phone (correct), then the Cast framework re-armed a remembered PERSISTENT route (`onRouteSelected reason=3 …CastMediaRoute2ProviderService_Persistent`, Kabir's Kitchen Stereo) and the app's `onCastSessionAvailable()=switchPlayer` blindly auto-cast the item to it. NOT the T302 change — pre-existing aggressive auto-cast + a stale persistent route. FIX: a cast-end that isn't a deliberate device switch arms an 8 s suppression window (PlaybackService companion suppressAutoCastUntilMs/userCastRequestedAtMs); onCastSessionAvailable ignores a route re-appearing inside it; CastSwitcher DeviceRow→notifyUserRequestedCast (clears, so switching works), Stop row→notifyUserStoppedCast (arms). DEVICE (Living Area TV): Stop → `CastRelay stopped` + route→Phone + NO re-cast to a speaker. assembleDebug+full unit suite green. |
| T303 | Re-tapping the already-loaded item should HOLD (resume+show), not rebuild/reload | M | DONE(7b58693). User: "if playing then casting, why clear the player? can't it hold what it's showing? why reload metadata again?" → chose hold. ROOT (cast3_logcat): after stop-cast the user re-tapped (touch 22:51:27.037 → openItem) → full rebuild → filename placeholder → 689MB re-download → re-enrich. openItem had no already-loaded guard. FIX: openItemInternal, after resolving the stream URI, if the player's CURRENT item mediaId == that URI (stable per item) → HOLD: resume-if-paused + show, no setMediaItems/re-download/metadata-reset; openItem skips recordCloudPlay on hold (resume ≠ fresh play → no dup Recents/session re-adopt). SAFETY (user caveat: must not break resume/close/phone-cast-BT switching): guard ONLY matches exact mediaId, else FALLS THROUGH to normal load → cast round-trip (queue rebuilt), provider/track switch, cold-start all unaffected (worst case: no optimisation, never breakage). BT = route change on same player/item → never reloaded anyway. Scope: Cloud/Drive (the pain); local Library re-tap reloads but is instant (no net/blank) → left as-is. compile+assembleDebug green; install Success. `[DEVICE]` AWAITING-USER(open Drive audiobook → cast → stop → re-tap it = instant resume, no blank/re-download; verify normal first-open + track-switch still load). |
| T304 | Drive audiobook chapters + proper title not loading | I→M | PARTIAL(18a5f2d). User: "chapters and proper title not loading on Drive audiobooks." ROOT 1 (my regression): the hold guard (7b58693) returned before the Drive enrichment → a held tap loaded nothing. FIX: extracted enrichDriveItem(); hold path now runs it (download+parse LOCAL file → title+24 chapters, scheme=file 12ms) without rebuilding the queue. So tapping the audiobook FROM THE CLOUD TAB now loads title+chapters. ROOT 2 (pre-existing, NOT yet fixed): cold-start + LastPlayed-resume do NOT run the download-enrichment — LastPlayedViewModel's "remote chapter fill-in" (lines ~492-509) calls extractChaptersAsBundle on the raw https URI = the slow path (meta4 23:10:37: textTrack 37970ms + neroChpl 37888ms = 75861ms, 0 chapters) and caches the empty 0. isRemote() correctly flags https (M4bChapterParser:70) so cold-start uses cachedOnly (fast, but 0 if never enriched). LastPlayedViewModel has NO drive providers → can't download. PROPER FIX (next): shared @Singleton DriveMetadataEnricher (drive providers + parseAndApply + the enrichedByMediaId cache) callable from CloudViewModel + LastPlayedViewModel + cold-start; + persist enriched title/artist/album to disk/LastPlayed row so cold-start shows them without a 689MB re-download. compile+assembleDebug green; install Success. `[DEVICE]` AWAITING-USER(tap the audiobook FROM THE CLOUD TAB → title+chapters load? If you were resuming from LastPlayed / auto-resume instead, that path is the ROOT-2 follow-up). ★REGRESSION ROOT + PERSISTENCE FIX (d7f4edd)★ user: "absolutely zero difference... it was working before." LOGS (meta5): user AUTO-RESUMES (cold-start, no openItem) — never enriches. GIT ARCHAEOLOGY found the regressions: (1) e726780 30s callTimeout broke the download enrichment (fixed earlier); (2) 6708a96 changed cold-start/resume from an INLINE remote chapter parse (slow but worked) to `cachedOnly(uri) ?: Bundle()` — a never-tapped book gets NOTHING, and the async fill-in parses the raw https URL = 75s/0-chapters. User chose Option 3 (fetch only on tap). FIX (persist a tap's result so auto-resume reuses it, no fetch): PlaybackHistoryDao/LastPlayedRepository.updateDisplayByUri writes the extracted title/author onto the row → cold-start recent.title shows proper tags; parseAndApply caches the chapters bundle under the STABLE remote key (item.downloadUrl = the cold-start cachedOnly key; verified getMediaStreamUri=Uri.parse(downloadUrl)) so cold-start cachedOnly HITS → 24 chapters on resume. Threaded stableKey through enrichDriveItem→parseAndApply. NET: tap the book ONCE from the Cloud tab → download+extract+persist → every later auto-resume / LastPlayed shows real title+author+chapters, NO re-download. compile+assembleDebug green; install Success. `[DEVICE]` AWAITING-USER(tap audiobook ONCE from Cloud tab + let it finish downloading → then kill+auto-resume → title+author+chapters now present). |
| T304b | Make EVERY tap (Cloud + LastPlayed) enrich, not just Cloud — kill the 75s remote parse | M | DONE(2d689ad). User "fix everything". New @Singleton DriveTagEnricher (shared download→MMR+chapters→setLocalMetadata/setLocalChapters→persist[title to history row + chapters under stable key]). LastPlayed resume now, for a Drive item with no cached chapters, calls the enricher (download+parse LOCAL file) INSTEAD of the futile async remote parse (~75s/0ch for moov-at-end). Non-Drive remote keeps the old inline parse. Additive+background (no touch to the resume player/queue setup). So tapping from Cloud OR LastPlayed loads full title+author+chapters + persists → auto-resume shows it, no refetch (Option 3). compile+assembleDebug (Hilt graph OK) green; install Success. `[DEVICE]` AWAITING-USER(tap audiobook from LastPlayed too → metadata+chapters load; no 75s freeze). |
| T305 | Cast "nothing's playing" flash during the cast swap | M | DONE(7a792c9). For an AUDIO cast the keepLocalVideo path doesn't engage → during the session-player swap to CastPlayer hasMedia briefly=false → empty state flashed. FIX: PlayerScreen collects castActiveFlow; showEmptyState now also `&& !castActive`, and displayState holds the last real title during ANY cast (audio too, not just video). compile+assembleDebug green; install Success. `[DEVICE]` AWAITING-USER(cast audiobook → no 'nothing's playing' flash, title held). ★FOLLOW-UP (8e2c7ad)★ user: "no media loaded for a few seconds when tapping cast" + "after cast the TITLE reverted to filename (other metadata fine)". ROOT1: the 3 AUDIO layout branches passed raw uiState NOT displayState → the title-hold never reached an audiobook (only the video branch had it). FIX: audio branches use displayState. ROOT2: the cast item carries only the filename as its title. FIX: PlayerScreen remembers the last PROPER metadata (non-blank AND non-filename — a title still ending .m4b/.mp3 is the raw filename) and during ANY cast where the title is blank/filename-like re-shows the whole block (title+artist+album+cover); multi-track casts with real titles untouched. ALSO: parseAndApply (Cloud + DriveTagEnricher) no longer falls back to the filename when the embedded title tag is missing — a filename override OUTRANKS the title ExoPlayer parses (overTitle wins) → showed filename on Deathly Hallows; pass the tag through null. compile+assembleDebug green; install Success. `[DEVICE]` AWAITING-USER(cast audiobook → title stays correct, no 'No media loaded'). ★was★ OPEN. User: "massive switch to 'nothing's playing' then back after tapping cast." For an AUDIO file (audiobook, no video track) the keepLocalVideo path doesn't engage → normal audio cast → during the session-player swap to CastPlayer the in-app Player tab briefly has no metadata → empty state shows → cast metadata loads → back. castLocalVideoActiveFlow (which suppresses empty state) only covers the video-cast case. FIX (next): hold the displayed metadata across the cast swap / suppress the empty state during any cast transition. NOT yet done. |
| T306 | Cover art intermittent on player + blank in Last Played thumbnails (durability risk for all metadata) | I→M | DONE(6522c50). User: cover sometimes didn't load + Last Played thumbnails blank; "risk for other metadata too." EVIDENCE (art_logcat [ART] lines): cover came only from the transient override / sticky embedded bytes → when neither ready `hasCoverArt=false` (race); Last Played row stored the often-NULL Drive thumbnail, never the embedded art. ROOT: the 600KB-1MB embedded cover lived only in LocalMetadataOverride (cleared on any reload), never persisted. FIX: new ArtworkCache util writes the bytes to cacheDir/coverart/<hash>.img keyed by media uri; both enrichers point the override artworkUri at that durable file + persist it onto the Last Played row (new DAO/repo updateArtworkByUri); cold-start sets the MediaItem artworkUri from ArtworkCache.uriFor (or the saved row uri). So the cover — like title/author/chapters already — is durable across reloads/cast/cold-start/the list. compile+assembleDebug (Room validates) green; install Success. NOTE: existing Last Played rows (played pre-fix) get the cover only after a re-tap (re-enrich); new plays get it immediately. `[DEVICE]` AWAITING-USER(re-tap a book → cover persists on player through cast/reopen; Last Played thumbnail shows the cover). |
| T307 | Title reverts to filename on cast (metadata depends on a transient override = race) | I→M | DONE(1d55d0e). User: "on cast the title lost metadata + reverted to filename; all metadata can't be in some race — fix it all." EVIDENCE (casttitle_logcat): on cast the CastPlayer reports ctrlTitle='' (empty); resolver falls to itemMetadata=filename. MMR DOES read the real title (00:14:35 'Harry Potter and the Deathly Hallows, Book 7'). ROOT (PlaybackConnection.updatePlayerState): title = overTitle ?: player.title ?: itemMetadata.title; itemMetadata = senderMetadataByMediaId[mediaId] (the DURABLE service-side cache the CAST item is also rebuilt from). The enriched tags lived ONLY in the transient override (overTitle), wiped on any reload/cast swap; the CastPlayer reports an empty title; senderMetadata still held the FILENAME (populated from the MediaItem) → filename shown. FIX: both enrichers write the extracted title/artist/album/cover into PlaybackService.senderMetadataByMediaId[stableKey] (same key the resolver + switchPlayer use; switchPlayer's merge only overwrites null fields so the real tags survive the swap). Now title+cover resolve from a durable source for LOCAL and CAST, independent of the override. Cold-start already carries the persisted title/cover on the MediaItem. compile+assembleDebug green; install Success. `[DEVICE]` AWAITING-USER(cast a book → title stays correct, not filename). OPEN-MINOR: user asked "does artwork slightly resize on cast?" — cosmetic content-scale; confirm/measure next. Last Played artwork = user confirms FINE now (T306). |
| T308 | Resume-on-BT-connect failed for a Spotify track | I→M | DONE(49badc1). User: "did resume on bluetooth connect just fail on this spotify track?" — YES. EVIDENCE (btspotify_logcat 00:24:53): device+ BT_A2DP EDIFIER + resumeOnBt=true but NO "BT resume-on-connect fired" line. ROOT (PlaybackService.onAudioDevicesAdded resume branch): it only `play()`s the LOCAL player; for Spotify the local player is the SILENT MIRROR with currentMediaItem==null → returned early → Spotify never resumed. FIX: when spotifyProvider.spotifyState.value != null → spotifyProvider.resume() (Spotify Connect) instead of the local player. compile+assembleDebug green; install Success. `[DEVICE]` AWAITING-USER(pause Spotify, disconnect+reconnect BT → Spotify resumes). |
| T309 | Spotify track artwork missing in Last Played | I→M | DONE(49badc1). ROOT: SpotifyProvider.jsonToCloudItem built the CloudMediaItem with NO thumbnailUri (default null) → recordCloudPlay stored null artworkUri → Last Played row had no cover (the screen shows item.artworkUri directly for Spotify rows). FIX: extract album art url (track.album.images[0].url; images[0].url for album/playlist) → set thumbnailUri. New Spotify plays carry the cover into Last Played; existing rows fill on next play. compile+assembleDebug green; install Success. `[DEVICE]` AWAITING-USER(play a Spotify track → its Last Played row shows the album cover). ★STILL MISSING → REAL FIX (25339d9)★ user: still not in Last Played. EVIDENCE (spotart_logcat 00:32): mirror HAS the cover (Spotify.fetchCurrentState artwork=i.scdn.co/... every ~1s) but NO recordCloudPlay this session — the track plays via the MIRROR (resume/cold-start), so jsonToCloudItem's thumbnailUri never reaches a row; the existing row (position persisted) had null art. FIX: the 5s position-persist tick now also writes the mirror's artworkUrl onto the row via updateArtworkByUri (once per track via lastArtTrack guard). So a Spotify track playing via the mirror gets its cover onto the Last Played row. compile+assembleDebug green; install Success. `[DEVICE]` AWAITING-USER(play Spotify ~5s → its Last Played row shows the cover). |
| T311 | Code audit of all today's changes (7bdb431..HEAD: 64 commits, 25 code files, ~1900 ins) | M | DONE-AUDIT + 7 FIXES(7b65780). 4 parallel code-reviewer agents (cast/BT; metadata enrich/persist; resume/cold-start/LastPlayed; player-UI/settings/nav). FIXED ship-blockers: (HIGH) Drive enrich lost current-item guard → A's tags could paint onto B on fast switch → new PlaybackConnection.setLocalMetadataIfCurrent/setLocalChaptersIfCurrent (main-thread controller.currentMediaItem check); (HIGH) onAddMediaItems SettableFuture never completed on cancel → resolve in finally; (IMP) cloudFetchInProgress not in finally → stuck spinner on cancel → try/finally both enrichers; (IMP) hold fast-path play()'d an errored/idle player → gate on STATE_IDLE/playerError; (MED) CastRelayServer.serveLocal 404/500 on no-fd providers → serveLocalStream fallback; (MED) resolveLaunchAutoplay missed LE-Audio sinks → added BLE types; (LOW) updateDisplayByUri wiped 'DRIVE' subtitle → CASE-preserve. compile+assembleDebug green; install Success. NOTED (not yet fixed — lower risk/impact): cast-local-video immersive bars read uiState not displayState (narrow: video→audio-only-device cast); cast-side error recovery (frozen muted picture if audio-only cast never starts); enricher duplication (CloudViewModel vs DriveTagEnricher — consistent today but KDoc 'single source of truth' is false); ArtworkCache 32-bit hash + non-atomic write; markFilling dedup for concurrent same-file enrich; OAuth cache filename un-hashed; dead nav block + unused imports; isFilenameTitle regex edge. clearAudioReroute-on-A2DP-add = DELIBERATE (matches user intent), not a bug. `[DEVICE]` AWAITING-USER(regression-test the 7 fixed paths). |
| T312 | "Do everything" — clear the T311 NOTED backlog + minimise resource drain | M | DONE(c637cf6 + review-fix cc89e0f). Cleared the lower-risk items T311 left NOTED, Context7-checked, then ONE adversarial code-reviewer pass. **Resource/efficiency:** (C) enricher de-duplicated — CloudViewModel's ~180-line enrichDriveItem+parseAndApply DELETED; both surfaces (Cloud tab + LastPlayed) now delegate to the single @Singleton DriveTagEnricher (one download cache, one code path → no double-fetch of the same file; the "single source of truth" KDoc is now TRUE). (E) ChapterCache.markFilling(stableKey) dedup gate in enrich() + try/finally unmarkFilling → a near-simultaneous Cloud+LastPlayed tap of the same book can't start two hundreds-of-MB downloads. (J) CastAudioExtractor.extractAudio takes isActive:()->Boolean; the remux loop bails+deletes the partial .m4a when the cast is abandoned (rapid re-switch) instead of burning CPU/IO to the end. (F) DriveOAuthProvider cache filename deterministic drive_<hash>_<suffix> + downloadHttp callTimeout(0) (the 30s cap was already fixed for content T300; consolidated here). (L) BT video-offset (OffsetVideoRenderer delay) gated by @Volatile btVideoRouteActive, re-eval on audio-device add/remove + seeded at registration → the delay applies ONLY while actually routed to BT. **Durability/correctness:** (D) ArtworkCache SHA-256 keyHash filenames + atomic temp-then-rename write (no torn cover, no key collision/escape). (B) cast error recovery — cast Player.onPlayerError ends the session + 30s startCastAudioOnly watchdog ends a never-syncing cast. (H) isFilenameTitle regex tightened to a real media-extension suffix. (K) Spotify launch-resume inspects the resume Result. Removed CloudViewModel's now-orphaned ChapterInfo/M4bChapterParser imports. compile+assembleDebug green; install Success on RFCY70BARDJ. **Adversarial review (superpowers:code-reviewer on c637cf6):** verified the 6 above-correct; found 3 real issues, ALL fixed in cc89e0f — (P1) the B watchdog force-ended a deliberately-PAUSED audio-only cast at 30s (route-while-paused never reaches "really playing") → watchdog now arms only when play==true; (P2) ArtworkCache temp was size-keyed (collidable) + a destructive f.delete()-before-rename window → per-writer System.nanoTime temp + copyTo(overwrite), no delete; (P2) DriveTagEnricher set setCloudFetchInProgress before the try so a throw between markFilling and try could permanently stick the fill key → moved inside try. compile+assembleDebug green; install Success. NOT done (deliberate, recorded not deferred): (a) AppNavigation dead navigationSuiteItems block — cosmetic per-recompose cost but the scaffold requires the param + removing risks the working nav → left, RISK>reward; (b) cast-local-video immersive bars read uiState not displayState — narrow (video→audio-only-device cast only) cosmetic, no correctness impact; (c) Spotify ~1Hz foreground mirror poll — already pauses 30s-backgrounded (T257), 1Hz is the minimum for a smooth progress bar, reducing it degrades UX → kept. `[DEVICE]` AWAITING-USER(regression: tap a Drive audiobook from Cloud AND LastPlayed [no double-download, metadata+chapters load]; cast a VIDEO to an audio-only device PAUSED then play [not torn down at 30s]; cover persists; BT A/V offset only while on BT). |
| T313 | Podcast bug-fixes + UI overhaul (plan `docs/superpowers/plans/2026-06-16-podcast-fixes-and-ui-overhaul-plan.md`) | M | DONE-CODE(pushed, install BLOCKED). Investigation (6 podcast issues) → plan (Context7 OkHttp/Compose/Media3 + superpowers) → AI self-review → inline execution. **A1** (fix #3/#4/#5): podcast `playEpisode` now records DIRECTLY into Recents via `lastPlayedRepo.recordPlay` (source=LOCAL proven-to-resume) with the show's artwork — was relying on the gated 5s-tick fallback so mid-session switches never recorded → wrong resume. **A2** (#1): RssFeedParser gets a browser User-Agent (scoped to its own client; Megaphone/CDN 403 the bare okhttp UA — Context7-confirmed redirects/gzip ruled out) + a typed `FetchResult` (HttpError/NotFeed/Network → useful messages) + Apple-id→feed `lookupFeedUrl`; 3 MockWebServer tests PASS. **A3** (#6): killed the fixed `360.dp` nested LazyColumn → content-wrapping Column (host `NonPlayerRoute` already reflows for mini-player/display, so no manual inset). **B1/B2/B3**: artwork thumbnails (Coil `PodcastArtwork`), show rows show episode/new counts not the raw URL, episode rows show New/In-progress(bar+"min left")/Played markers DERIVED from the history position (no migration), search results show artwork. **A7**: chapter chip shows "Loading chapters…" while `cloudFetchInProgress` (was hiding/empty). **A4**: force-create `noBackupFilesDir` to pre-empt the WorkManager `no_backup workdb` SQLITE_CANTOPEN crash (06-17). **A5/#2**: speed-delay documented inherent (Context7: no audible-propagation/output-latency signal — keep control live) + diag label fix. assembleDebug green; unit tests green; commits a1b293e (pushed). **INSTALL BLOCKED:** device holds the RELEASE 1.3.0 build (signature ≠ debug) → `INSTALL_FAILED_UPDATE_INCOMPATIBLE`; installing debug needs an uninstall = data wipe (NOT done without consent). `[DEVICE]` AWAITING-USER(choose install path; then verify all 6). |
| T314 | 2 user-reported podcast bugs — log assessment (release-build logs preserved BEFORE tasks) | I | DONE(evidence: deeplogs/podcast_diag2.txt @01:13). **(Y) "slider finished based on speed; fear 2x→50%": NOT a bug.** Fighting Cock (b558d559) reached genuine `state=ENDED` at `pos=3255409ms` (54.3 min = real stream EOF) — position is MEDIA-time, ENDED is EOF-driven, both speed-invariant. At 1.3x it finished in less REAL time but played the FULL content; 2x cannot stop at 50%. (Residual caveat: a VBR-MP3 duration mis-estimate could make the slider %-imprecise — cosmetic, not early-stop.) **(X) "~20s backward skip without input on Fighting Cock": NOT evidenced.** Across b558d559's whole playback (00:46→01:09 ENDED) positions were STRICTLY MONOTONIC — no backward jump. The only backward seek in the log is line 247 (01:13:10, on Extra Inch ad333117 after a resume-tap, 1629932→1423253ms ≈ −3.4 min, NOT ~20s), `reason=SEEK` = user/controller-initiated during active interaction (most likely a user scrub). NOTE: logs are the RELEASE build (pre-A1 resume-recording fix). `[DEVICE]` AWAITING-USER(if it recurs: reproduce + note the timestamp on a debug build w/ logging for a definitive trace). |
| T315 | Drive folder-picker search: keyboard won't appear | I→M | DONE-CODE(36087d0, install blocked). User: typing in the Google/Drive folder-picker search bar didn't raise the keyboard. EVIDENCE (keyboard_logcat 01:38:35, RELEASE build so only system IME logs): `DrivePickerActivity` (WebView JS Picker) — Chromium `ImeAdapterImpl.updateState` SHOWS the IME (`showSoftInput`, IME pid 13252 receives flags=1) then ~150ms later HIDES it (`hideSoftInputFromWindow`), repeatedly; `mInputShown=false` throughout (never stays). ROOT (code ✕ log): activity has no `windowSoftInputMode` → default ADJUST_RESIZE → keyboard resizes the WebView → fires a JS `resize` event → the picker HTML's resize listener (DrivePickerActivity.kt:157-160) DISPOSES+REBUILDS the entire Google Picker → destroys the focused search input → Chromium reports no-editor → IME hides → window resizes back → resize event → rebuild → loop. The listener was meant for fold/orientation only. FIX: rebuild ONLY on a WIDTH change (orientation/fold); ignore height-only resizes (the keyboard) → focus survives → keyboard stays. compileDebugKotlin green. NOT my-changes-related (pre-existing). `[DEVICE]` AWAITING-USER(verify keyboard now appears in the Drive picker search — needs the install path resolved first; fix rides the next build). |

## K — Audio-focus + downloads + storage + Spotify-search (plan `docs/superpowers/plans/2026-06-19-audiofocus-downloads-storage-spotifysearch-plan.md`, executed 2026-06-21/22)

| ID | Task | Phase | Status |
|----|------|-------|--------|
| T316 | Part 1 — call-not-pausing audio-focus fix | M | DONE(PlaybackService: request focus on play (onIsPlayingChanged), abandon on non-focus stop; `isCurrentItemVideo()`→video always pauses; oauthInFlight suppresses ONLY auto-resume; permanent-loss not armed for resume; FAILED tolerated for target-35. compileDebugKotlin EXIT=0; commit 33afff3) `[DEVICE]` AWAITING-USER(incoming call pauses audio + resumes; video pauses; PiP audio pauses→resumes) |
| T317 | Part 2 — real podcast downloads (DB localPath + play-from-file) | M | DONE(PodcastEpisodeEntity +localPath/localBytes/downloadedAt; PodcastShowEntity +downloadTreeUri; AppDatabase v16→17 + MIGRATION_16_17 additive ALTER, registered AppModule.addMigrations; PodcastDao.syncEpisodes @Transaction PRESERVES isPlayed+download cols across re-sync (REPLACE would wipe); both callers switched; PodcastsSection.playEpisode plays localPath via setUri (localConfiguration.uri overrides stream) keyed on stable audioUrl, self-heals stale path. assembleDebug EXIT=0; **MIGRATION DEVICE-VERIFIED on RFCY70BARDJ**: launch clean, podcast restored+played from v17 DB, ZERO FATAL/SQLiteException/Room-migration error in logcat (no destructive fallback past v6 → bad migration would crash on first DB access); commit ba0714d) `[DEVICE]` AWAITING-USER(download an episode → tick; play offline) |
| T318 | Part 3 — downloads management surfaces | M | DONE(EpisodeRow per-episode download/downloaded-tick+delete-confirm + spinner; ShowSettingsRow per-show folder picker + Download-latest-N / Delete-downloads; Drive long-press 'Protect from auto-cleanup' pin/unpin (OfflineCopyDao.setStarred + starredOfflineIds reactive set); unified `ui/downloads/DownloadsScreen.kt` (podcasts + Drive offline, size, total vs cap, per-row delete, grouped) routed from Cloud tab + Settings. assembleDebug EXIT=0; commit d037b36) `[DEVICE]` AWAITING-USER(manage downloads end-to-end) |
| T319 | Part 4 — user-chosen storage (SAF write) | M | DONE(`util/SafStorage.kt`: persist READ|WRITE tree, resolveDir/createChild/writeStream/delete/exists scheme-aware + treeLabel no-IPC; PodcastDownloader resolves per-show tree→global tree→app-private + records Saved(uri,bytes); SettingsDataStore +podcastDownloadTreeUri +driveOfflineTreeUri; worker reads global tree + records localPath; CloudViewModel.relocateDriveOffline copies Drive offline into the one global folder (content:// stored), removeDriveOffline/evictor/read-path all scheme-branch (content:// vs file://); SettingsScreen StorageFoldersRow (2 pickers + Spotify-N/A note). assembleDebug EXIT=0) `[DEVICE]` AWAITING-USER(pick folders; files land there) |
| T320 | Part 5 — Spotify search re-enabled + quality fixes | M | DONE(CloudMediaItem +subtitle; jsonToCloudItem reads artists[]→'Title · Artist'; episodes are LEAVES playing full show via Connect uris:[spotify:episode:…] (downloadUrl=spotify URI so play path resolves it); listContainer already handles artist top-tracks; +artist to search type + parser bucket; CloudBrowserScreen un-hid search bar for Spotify + section-picker gated on blank query so a query shows results; CloudItemRow shows subtitle. assembleDebug EXIT=0) `[DEVICE]` AWAITING-USER(search Spotify; tap artist/episode) |
| T324 | Per-item storage TRULY persists + picker defaults to phone storage (user 2026-06-22) | M | DONE(7067dcc). Persist chain verified: setShowFolder/global pickers → SafStorage.persistTree(READ\|WRITE persistable grant) + DB downloadTreeUri / DataStore key; downloader resolves per-show→global→app-private + checks hasWriteAccess; now preserved across periodic sync (H1) AND re-subscribe (M3) — the two paths that were wiping it. SafStorage.phoneStorageInitialUri() (primary: doc uri) passed to all 3 OpenDocumentTree launchers so the picker opens at phone top-level, not the last-used Drive folder. assembleDebug EXIT=0) `[DEVICE]` AWAITING-USER(pick a folder per show → survives a sync + app restart; picker opens at phone storage) |
| T325 | Auto-resume toggle + cast/BT interruption implications (user 2026-06-22) | M | DONE(7067dcc). Investigation: per-scenario pause/duck/continue already existed (audioFocusOnCall/Notification/OtherMedia) but auto-resume was HARDCODED + no cast distinction. Added 2 settings (SettingsDataStore + Settings→Playback UI via own mini-VM, big combine untouched): (1) `audioResumeAfterInterruption` (default ON) — GAIN branch resumes only if on, else stays paused + abandons focus; (2) `interruptionsPauseCast` (default OFF) — handleAudioFocusChange skips when castActiveFlow && !pauseCastOnInterruption so a phone call doesn't pause the TV/speaker (remote output); BT is LOCAL audio, never skipped, follows the per-scenario policy. Info box updated (BT-vs-cast explainer). Device: `AudioFocus request on play → 1 held=true` (app is the live focus owner during playback). assembleDebug EXIT=0) `[DEVICE]` AWAITING-USER(call during BT playback pauses; call during cast keeps casting unless toggled; resume toggle off → stays paused) |
| T327 | Device-test bug batch (user 2026-06-22) | M | DONE(e410e3f). (1) Spotify "help" search → "Not a JSON Object: null": Spotify returns NULL array entries (delisted playlists/shows); search() did `el.asJsonObject` unguarded → threw, and M4 now surfaced it. FIX: objOrNull/arrOrNull type-safe Gson accessors + `if(!el.isJsonObject) continue` + per-item runCatching in search(); every getAsJsonObject/Array/asJsonObject in jsonToCloudItem hardened. (2) Download tick not obviously deletable → EpisodeRow downloaded state now a teal DeleteOutline (trash), confirm-dialog kept. (3) No visible Drive download → CloudItemRow gains canManageOffline/isSavingOffline/onSaveOffline/onRemoveOffline (download icon → save, trash → remove, spinner while saving); saveDriveOffline wrapped try/finally with _savingOffline StateFlow; wired at browse + search sites. (4) TRUE OFFLINE: downloaded podcast was only played-local from the podcast-list tap; cold-start resume + Recents/LastPlayed tap STREAMED (Recents row keys on stream url, nothing mapped to localPath). FIX: new @Singleton PodcastOfflineResolver.localUriFor(streamUrl) (DAO downloadedByAudioUrl + readability + self-heal) wired into PlaybackSessionCoordinator cold-start (setUri=playUri, mediaId/requestMetadata stay stream-url key) + LastPlayedViewModel play else-branch. assembleDebug EXIT=0; pushed 994d212..e410e3f; installed Success; launch clean (new Hilt injections resolve, no FATAL). NOTE on "LOCAL" tag: PlaybackHistoryEntity.source is a cold-start ROUTING label (LOCAL/DRIVE/SPOTIFY), NOT a physical-location flag — pre-today podcasts tagged LOCAL were STREAMING; Drive offline copies keep source=DRIVE (offline tracked via OfflineCopyEntity + chip). Tag left unchanged (drives restore routing). 2nd review of e410e3f killed by user mid-run; self-verified try/finally balance + cold-start design vs the already-reviewed playEpisode pattern) `[DEVICE]` AWAITING-USER(search "help" Spotify = results no error; download podcast → close/reopen offline → resumes from local file; Drive row download button) |
| T328 | Last Played tags deep-dive + Spotify guard sweep + cosmetics (user 2026-06-22) | M | DONE(6019345). DEEP-DIVE finding: every Recents/Pinned row shows a SourcePill (Local/Drive/Spotify) = the SOURCE/provider, NOT offline state; podcasts (source=LOCAL + http uri) showed a misleading "Local". FIX: row computes isPodcast → SourcePill shows "Podcast" (violet) for podcast rows (applies Recents + Pinned); LastPlayedViewModel exposes downloadedPodcastUrls (observeDownloaded→Set<audioUrl>); rows with a downloaded copy show an "Offline" pill (podcasts only — reliable since Recents mediaUri=audioUrl=the key; Drive NOT badged because recordCloudPlay stores mediaUri=downloadUrl while offline keys on item.id → unreliable match for OAuth Drive). Also: 2nd review of e410e3f (cold-start SAFE TO SHIP, fixes 2/3/4 correct) flagged Fix-1 residual — same null-entry defect class in browse/section/listContainer paths (non-crash but blanks a section). SWEEP: objOrNull/arrOrNull + isJsonObject guards applied to parseSectionInto (all 8 sections), fetchPerType, listFiles, listContainer candidate-probe (getAsJsonObject→objOrNull) + artist branch. Cosmetics: removed unused DownloadDone import; artist subtitle "Artist"→"" (no "· Artist" noise). assembleDebug EXIT=0; pushed e3ecddf..6019345; installed Success; launch clean (new podcastDao injection resolves, no FATAL)) `[DEVICE]` AWAITING-USER(Recents podcast row shows "Podcast"+"Offline"; Spotify browse sections don't blank on delisted entries) |
| T329 | Drive download undiscoverable (user 2026-06-22: "nowhere to download Drive files") | M | DONE(cfea7ec). Diagnosis: per-file download button existed (CloudItemRow canManageOffline, e410e3f) but tinted TextSecondary (faint grey, lost beside the star) AND absent from favourited Drive tracks (FavouriteTrackRow had no button). Folder rows correctly have none (can't download a folder). FIX: per-file Download icon → TealAccent 22dp (prominent); FavouriteTrackRow gains isOffline/isSaving/onSaveOffline/onRemoveOffline (download/trash/spinner) wired from offlineIds+savingOffline with a CloudMediaItem built from fav. Long-press "Save offline copy" unchanged. assembleDebug EXIT=0; pushed 6019345..cfea7ec; installed Success; launch clean. NOTE: device screenshot showed a DIFFERENT app (recipes) — user not on PMP Drive at capture, so location confirmed by code not visual. OPEN findings surfaced (not yet fixed): (a) DriveTagEnricher always re-downloads for metadata/chapters — does NOT reuse an existing offline copy (playback DOES via CloudViewModel:1085) → saved Drive audiobook won't enrich offline + double-downloads; (b) no folder-level "download all"; (c) Drive offline has no Last Played badge (id-vs-downloadUrl key mismatch)) `[DEVICE]` AWAITING-USER(Cloud→Drive→into a folder→teal ⬇ on each file row; favourited Drive files show it too) |
| T331 | Deep-dive: download progress, Spotify album play, tab-switch bug, Drive search, Drive names (user 2026-06-22) | I→M | DONE(8d8f452+360da9d+22bb8a1). **(C)** browseDrive/browseSpotify now clear searchQuery+searchResults+cancel searchJob → tapping a provider tab no longer keeps stale Spotify results. **(D)** Drive OAuth search dropped the `'<root.id>' in parents` clause (direct-children-only) → single global `name contains` query = recursive across all granted folders/sub-folders. ROOT of "search all my Drive": IMPOSSIBLE — app uses non-sensitive **drive.file** scope (no Google verification/CASA), so it only sees picker-granted folders, never the whole Drive. **(E)** OfflineCopyEntity +displayName (DB v18 + MIGRATION_17_18, registered); saveDriveOffline stores item.name; DownloadsScreen shows displayName ?: nameFromPath → real names not "drive_500…". **(A)** DownloadProgressBus + ProgressInputStream: podcast (PodcastDownloader) + Drive (both providers' downloadRangeToCache via progressId param) report bytes; DownloadProgressMini (bar + "12M/45M") replaces the spinners on episode + Drive file + favourite rows. **(B)** Spotify album play (Context7-confirmed PUT /me/player/play context_uri + offset; PUT /repeat?state=context): playRequest accepts null spotifyUri (context-from-start); playTrackOnConnectDevice spotifyUri nullable; +setRepeat; CloudViewModel.playSpotifyAlbum (context play + repeat=context loop); a track tapped within a browsed album also sets repeat=context (next/prev already traverse via contextUri). Play button (▶) added to Spotify album/playlist rows (tap row=browse, ▶=play album). assembleDebug EXIT=0; pushed to 22bb8a1; installed Success; **v18 migration device-verified (clean launch)**) `[DEVICE]` AWAITING-USER(search Drive 'harry' → nested files appear; tap provider tab clears search; download shows progress bar+size; play ▶ on a Spotify album → plays through + loops; Downloads shows real Drive names) |
| T333 | Extend offline resolver to Drive + device-test all recent changes (user 2026-06-22) | M+V | DONE(d8ccdb7+846d320). Drive resume keying: openItem now uses a STABLE mediaId=downloadUrl (was the resolved local-vs-stream uri) so position/Recents row survive an offline↔stream switch (trash/add). NEW DriveOfflineResolver (mirrors PodcastOfflineResolver) wired into cold-start + LastPlayed play so a downloaded Drive item RESUMES from the LOCAL file (instant, offline, local enrichment); mediaId stays stream-url so resume point matches. **ON-DEVICE VERIFIED (Z Fold, real account)**: (1) Drive search 'ruin' → 'This Inevitable Ruin… Book 7' + Offline badge, ~4s (parallel recursive walk; log 'walked 18 folders'); (2) metadata backfill → displayName 'This Inevitable Ruin꞉ …Book 7 [B0DK2815FK].m4b' (DB-confirmed); (3) download PROGRESS BAR — re-download shows teal bar + '250M…' on the row; (4) Spotify search 'help' → 58 results NO CRASH (null-entry fix; this query used to throw 'not a json object null'); (5) Spotify track rows show 'Title · Artist'; (6) Spotify album/playlist rows show the teal ▶ play button; (7) v18 migration + DriveOfflineResolver DI clean launch. CODE-COMPLETE, needs user hands-on: actual Spotify album playback (Premium Connect device), Drive resume-from-local + position-continuity-across-trash scenarios. pushed to 846d320; installed) `[DEVICE]` AWAITING-USER(play a Spotify album via ▶; play a downloaded Drive book from Last Played → resumes local; trash mid-book → resume continues from position) |
| T334 | Spotify metadata slow + album-no-tracks + shuffle mode (user 2026-06-22) | I→M | DONE(9bdc7e0). (1) Spotify metadata SLOW — log-diagnosed: playRequest http=404 (NO_ACTIVE_DEVICE) → 'device appeared after 3000ms' → http=204 = ~4.2s. Root = Spotify Connect device-wake (no active device → launch Spotify + poll 3s). Inherent; instant if a Spotify device is already active. Provisional mirror shows album name immediately. (2) Tapping a Spotify album from SEARCH didn't show tracks — SAME class as the tab-switch bug: openItem's Spotify folder branch set items+folderStack but NOT cleared searchQuery → search-results branch kept rendering. FIX: clear searchQuery/searchResults+cancel searchJob on drill-in; gate the section-picker branch with folderStack.size<=1 so a pushed stack shows the browse list. (3) SHUFFLE mode (did NOT exist except smart-playlist 'random' sort) — added: PlaybackConnection.setShuffleMode (ExoPlayer shuffleModeEnabled), SettingsDataStore.shuffleEnabled (persisted), PlayerViewModel.shuffleEnabled flow + toggleShuffle + combine(pref,playerFlow)→apply-on-connect, Shuffle IconButton in the audio controls row (teal on / grey off), InfoContent bullet. Works on multi-track queues (Library album/folder plays); for Spotify use Spotify's own shuffle. assembleDebug EXIT=0; pushed f854a32..9bdc7e0; installed; clean launch) `[DEVICE]` AWAITING-USER(tap a Spotify album → tracks list; shuffle toggle randomises a Library album/folder queue + persists) |
| T335 | Shuffle plays sequentially + button robustness across player screens (user 2026-06-22) | I→M | DONE(f08415f+2af8f70). ROOT CAUSE (no logs existed — device diag/deeplog dirs EMPTY): in-app prev/next call seekToNext/PreviousMediaItem (shuffle-aware) + onConnect grants default player cmds (incl SET_SHUFFLE_MODE) + session player is raw ExoPlayer/CastPlayer → LOCAL/Drive shuffle path CORRECT (Context7 /androidx/media confirmed shuffleModeEnabled randomises order + seekToNextMediaItem follows it). GAP: Spotify-Connect playback runs on the Spotify device, so toggling the local mirror's shuffleModeEnabled is INERT — the app never called Spotify's shuffle API. FIX: SpotifyProvider.setShuffle (PUT /me/player/shuffle?state=); toggleShuffle routes to Spotify when isSpotifyActive else local + diag log; playSpotifyAlbum honours shuffle pref on launch; switchPlayer preserves shuffleModeEnabled across local<->cast; setShuffleMode logs player/count/curIdx/nextIdx. BUTTON ROBUSTNESS (agent-audited every transport row): Shuffle in OverlayContent (compact/folded PlayerScreen:1196) AND PlayerScreenExpanded (unfolded two-pane :1464) — fold screenshot-confirmed; only MiniPlayerBar omits it (play/pause only, by design). Toggle is a pure ordering flag — never interrupts the current track. assembleDebug EXIT=0; installed Success) `[DEVICE]` AWAITING-USER(shuffle a Spotify album → randomises; shuffle a Library album → next/prev jump) |
| T336 | In-player track list for Spotify albums "like chapters" (user 2026-06-22) | I→M | DONE(2af8f70). Connect album mirrors ONLY the current track — no album list existed. FIX: SpotifyPlaybackState +contextUri (from poll context.uri + album-launch provisional mirror); PlayerViewModel fetches album/playlist tracks (reuses listContainer) + spotifyContextTracks + spotifyTrackUri + playSpotifyContextTrack (plays WITHIN context so next/prev+shuffle keep traversing); PlayerScreen SpotifyAlbumTracksButton (QueueMusic, only when tracks present) in BOTH controls rows → AlertDialog LazyColumn numbered tracks, current=teal+GraphicEq, tap=play. Confirmed search/browse drill-in already works (openItem folder→listContainer; albums isFolder=true). assembleDebug EXIT=0; installed Success) `[DEVICE]` AWAITING-USER(play a Spotify album → QueueMusic → list → tap track plays in-context) |
| T337 | Manage Downloads select-all + multi-select batch delete (user 2026-06-22) | M | DONE. DownloadsViewModel +selected StateFlow + toggleSelected/selectAll/clearSelection/deleteSelected (reuses single delete). DownloadsScreen per-row Checkbox (teal); TopAppBar "n selected" + All/None + batch-delete icon (confirm AlertDialog); back exits selection first. assembleDebug EXIT=0; installed Success) `[DEVICE]` AWAITING-USER(select some + All → delete → confirm) |
| T338 | Cancel a download mid-progress + remove fragmented files (user 2026-06-22) | M | DONE. DownloadProgressBus +cancel registry (requestCancel/isCancelled/throwIfCancelled); ProgressInputStream + copy() + SAF FileChannel loop poll it → DownloadCancelledException at next chunk → every provider's existing catch deletes the partial + returns null → NO DB row. Wired: CloudViewModel.cancelDownload(id) (Drive OAuth+SAF), PodcastsSection.cancelDownload(guid); DownloadProgressMini +onCancel (✕) threaded via CloudItemRow/FavouriteTrackRow onCancelOffline + podcast row; "Download cancelled" msg. assembleDebug EXIT=0; installed Success) `[DEVICE]` AWAITING-USER(start download → ✕ mid-progress → stops, no partial, row reverts) |
| T339 | Prefetch-next-cloud was a dead toggle (user 2026-06-22: "is prefetch really working?") | I→M | DONE. FINDING: prefetchNextCloud setting + Settings toggle existed (default on) but NOTHING consumed it. Drive plays ONE track at a time (setMediaItems(listOf(item),0)) so a multi-track "Meteora" is NOT Drive. FIX: maybePrefetchNextCloud — on a Drive open, pre-warms the NEXT browsed audio track's tags via enrich(silent=true) (new param skips the cloud-fetch spinner) → next tap finds tags in cache → instant. Gated by the now-real pref. assembleDebug EXIT=0; installed Success) `[DEVICE]` AWAITING-USER(in a Drive folder play track 1 then tap track 2 → metadata instant) |
| T341 | Spotify: play-all in drill-in + back restores search + podcasts-in-search (user 2026-06-23) | I→M | DONE. (a) Play album/playlist/SERIES button now shown as a header INSIDE a drilled-in Spotify container (folderStack.last() is a spotify: uri) → playSpotifyAlbum(container) (label varies: album/playlist/series). (b) BACK from a search-originated album restores the prior search: CloudUiState +priorSearchQuery/priorSearchResults; openItem saves them before clearing on drill-in; navigateUp restores (pop + re-show results) instead of dropping to the section; setSearchQuery clears the snapshot on a fresh query. (c) PODCASTS ALREADY in Spotify search — type=track,album,artist,playlist,show,episode (SpotifyProvider:1169); shows browse, episodes play. No fix; reported. assembleDebug EXIT=0; installed) `[DEVICE]` AWAITING-USER(drill a Spotify album → Play header; search→album→back returns to results; search a podcast name → shows appear) |
| T342 | Recent-search dropdowns in all search boxes (user 2026-06-23) | M | DONE. SettingsDataStore: 3 ordered (U+001F-joined, max 12, deduped) string prefs + recentSearches{Cloud,Library,Podcast} flows + add/clear. New shared RecentSearchesDropdown (shown when field focused + empty; History rows tap-to-rerun; Clear). Wired into Cloud (CloudBrowserScreen search bar; recorded in openItem when a search result is acted on), Library (LibraryScreen; recorded in playSingle when search active), Podcasts (PodcastsSection; recorded in searchItunes submit). assembleDebug EXIT=0; pushed fec45a6; installed) `[DEVICE]` AWAITING-USER(focus an empty search box → recent queries dropdown → tap re-runs) |
| T343 | Shuffle STILL not working on Meteora (user 2026-06-23) | I→M | DONE(f8582ab) — EVIDENCE-LOCKED. Device logcat (reproduction) proved: Meteora plays via SPOTIFY with a real album context (context=spotify:album:4Gfnly5…); VM.toggleShuffle src=spotify; Spotify.setShuffle(true)->HTTP 200; skipNext=POST /me/player/next (shuffle-aware). So the app issued every correct call — yet /next walked the album in order. ROOT CAUSE (Context7 /websites/developer_spotify_web-api): Spotify builds the queue at PLAY time and "the order of execution is not guaranteed when you use [shuffle] with other Player API endpoints" — toggling shuffle mid-context does NOT reliably reshuffle the already-built queue. FIX: SpotifyProvider.reshuffleCurrentContext() re-asserts PUT /me/player/play {context_uri, offset:{uri:currentTrack}, position_ms:currentPos} after setShuffle(true) so Spotify rebuilds the upcoming order while keeping the listener in place; wired into PlayerViewModel.toggleShuffle on ENABLE. assembleDebug EXIT=0; installed; logcat cleared.\n      INVESTIGATION DETAIL (was IN-PROGRESS): Re-confirmed local/Drive shuffle path is correct (Library setMediaItems(items,idx) builds a multi-item queue; in-app next=seekToNextMediaItem; Context7-confirmed semantics) and Spotify routing added (T335). Cannot determine WHY Meteora still fails without a wire trace — device had NO logs (diag/deeplog empty). ADDED always-on logcat SHUF trace in onMediaItemTransition (shuffle/count/cur/next/player on every track change) + setShuffleMode + toggleShuffle(src) + Spotify.setShuffle(HTTP code) traces. Built+installed; logcat cleared. NEEDS one reproduction (play Meteora, toggle shuffle on, press Track-next 3×) → the trace decides local-vs-Spotify + count/index/HTTP. NO speculative fix (no-guesswork rule). |
| T344 | Manage Downloads section select-all + Settings/Library tab re-tap to top (user 2026-06-23) | M | DONE(quick-wins). Per-section Select all/Deselect all in Manage Downloads (DownloadsVM selectAllPodcasts/selectAllDrive/toggleSection; SectionHeader onSelectAll). Library + Settings reset to top on re-tap of the active tab (TabReselectBus collectors: Library→setSearchQuery("")+setSelectedTab(0); Settings→clear search filter). NavigationRail (unfolded) now fires the re-tap too (was only the compact NavigationBar). assembleDebug EXIT=0; installed) `[DEVICE]` AWAITING-USER |
| T345 | Download-offline / delete-local from Last Played 3-dot + Player button (user 2026-06-23) | I→M+V | DONE(12e9afe+8b31b49). 3 parallel agents mapped it. NEW @Singleton offline/OfflineMediaManager (download/deleteLocal/stateOf/downloadedKeys for Drive+podcast by play URI; mirrors saveDriveOffline+relocate + PodcastDownloader). PodcastDao.episodeByAudioUrl added. Wired into Last Played 3-dot (TrackContextActions onSaveOffline/onRemoveOffline, state-aware via offlineStateOf + downloadedKeys; labels→"Download for offline use"/"Delete from local storage") AND a new PlayerOfflineButton in BOTH player layouts (folded+unfolded) with a live DownloadProgressBus spinner + result Toast. **superpowers:code-reviewer over c92bfce..HEAD** found A1(HIGH): a locally-picked SAF file (content://…document) was misclassified as downloadable Drive → downloadDrive would COPY it + write a bogus Drive-offline row. FIXED: Drive inferred ONLY from an unambiguous OAuth url; SAF Drive offered only on an explicit "DRIVE" hint (Last Played). A2(MED): dropped speculative podcast DOWNLOADABLE for arbitrary http on the player path. A4(MED): player button now Toasts the download/delete result. Reviewer PASS on Hilt scoping (one shared downloadedKeys), Drive download/delete mirror, LastPlayed classification. assembleDebug EXIT=0; installed; **launch-verified NO CRASH** (Hilt resolves OfflineMediaManager into both VMs)) `[DEVICE]` AWAITING-USER(Last Played 3-dot on a Drive/podcast row → Download/Delete; Player button on an OAuth-Drive audiobook → download spinner → flips to delete) |
| T346 | Mini-player float-above-keyboard feasibility (user 2026-06-23) | I | DONE-INVESTIGATION (no code — user asked feasibility + flag payoffs). VERDICT: feasible, moderate, low-risk. Manifest already adjustResize (mini-player passively rises today); explicit fix = Modifier.imePadding() on the MiniPlayerBar container in AppNavigation NonPlayerRoute (composes with system-bar insets, animates with IME). Per-form-factor automatic (phone/folded rises above IME; tablet/unfolded rail is side-mounted → no collision). PAYOFFS: now-playing visible while typing; standard pattern; 1-line revert. PROBLEMS flagged: (1) CRITICAL — the new recent-searches dropdown (≤260dp) opens BELOW the field and would compete with IME+mini-player for bottom space → clip on phones; needs above-field/modal reposition; (2) content list items hide behind the float → IME-aware bottom padding; (3) keyboard-height variance auto-handled by WindowInsets.ime; (4) landscape/API≤28 edge cases low-risk. ~half-day code + device testing. AWAITING user go/no-go. |

## L — Folded hitboxes + timer popup (plan phase, 2026-06-24)

| ID | Task | Phase | Status |
|----|------|-------|--------|
| T347 | PLAN items #14/#1/#15/#4 (folded info-icon hitbox, brightness-banner hitbox, mini-player black box, timer-tap numeric jump) | P | DONE(`docs/superpowers/plans/2026-06-24-folded-hitboxes-and-timer-popup.md`. Read ACTUAL code at every cited line: #14 InfoIcon declared BEFORE controls Column → controls last=top-most hit sibling captures top-right (PlayerScreen.kt:1101 vs 1145); #1 banner `.padding(28dp).clickable{}` dead-zone <48dp (SecondaryControls.kt:149-158); #15 `imePadding()` on mini-bar Box folded mis-report (AppNavigation.kt:353); #4 plain time Texts ProgressSliders.kt:229-245, single call site PositionSection PlayerScreen.kt:2129, PositionUi exposes positionMs/durationMs/chapterStartMs/totalPlaylistDurationMs. Context7 `/androidx/androidx`-verified: zIndex=DRAW order only NOT hit-test → declaration-order is the load-bearing #14 fix; `minimumInteractiveComponentSize()`=`androidx.compose.material` 48dp; clickable-before-padding. 7 bite-sized tasks, per-task DEVICE adb verification on BOTH folded cover + unfolded inner display, per-task commit. §Design decisions awaits user confirm: #4 AlertDialog style, remaining=absolute-time-remaining, both bars tappable, library min-target modifier (no global helper), #14 fix=move InfoIcon last (option A), #15 fix=isImeVisible gate. EQ EqualizerScreen.kt:83-92 FLAGGED ONLY [comment, no hitbox change]) |

## M — Media-type icons + classification (plan phase, 2026-06-24)

| ID | Task | Phase | Status |
|----|------|-------|--------|
| T348 | PLAN items #13/#12/#8 (Cloud audiobook icon, Library video thumbnail, media-sub-kind classifier) | P | DONE(`docs/superpowers/plans/2026-06-24-media-type-icons-and-classification.md`. Read ACTUAL code at every cited line — paths corrected: package is `com.powermediaplayer` NOT `com.example`. #13 bug CloudBrowserScreen.kt:1605 MIME-only `mimeType.startsWith("video/")`; play-path fix mirror = CloudViewModel.kt:1458-1466 audioExts. #12 LibraryScreen MediaFileItem:768-781 icon box, gate `MediaFileInfo.isVideo` authoritative (MediaStore.Video collection LibraryViewModel.kt:866); Coil 3.1.0 present, app already a `SingletonImageLoader.Factory` w/ LocalTrackArtFetcher (PowerMediaPlayerApp.kt:51-57). #8 MediaKind enum ALREADY has PODCAST+AUDIOBOOK (PlayerUiState.kt:154-162); inferMediaKind never emits PODCAST (PlayerViewModel.kt:1631-1637); `PlaybackControls.kt:60-68` has a DEAD PODCAST→"Show/Episode" branch; audiobook=hasChapters (PlaybackConnection.kt:1111)||.m4b; podcast=PodcastDao.episodeByAudioUrl (PodcastDao.kt:92, suspend); BT applyAction PlaybackService.kt:3367-3398 has NO kind branch → net BT risk ≈0, keep global. Context7-verified: coil-video VideoFrameDecoder.Factory()+videoFrameMillis (pin 3.1.0 to match), Media3 FrameExtractor + platform ThumbnailUtils/loadThumbnail (API29, safe@minSdk30) noted as rejected alts. DESIGN: 4 task groups, ONE pure `MediaClassifier.classifyMedia` (TDD, plain JUnit, 16 tests) shared by #13 icon + #8 labels; #12 Library-only via coil-video (Cloud OUT); per-task gradle+grep predicates + commits; anti-skip table; auto-push+install. §Design decisions await user confirm: (1) Library-only vs Cloud thumbs [rec Library-only]; (2) coil-video vs MMR/ThumbnailUtils [rec coil-video — reuses existing ImageLoader+cache]; (3) #8 ship classifier+labels now vs document-only [rec ship now, display-only, NO BT split]) |

## N — Spotify perf + Drive metadata search (plan phase, 2026-06-24)

| ID | Task | Phase | Status |
|----|------|-------|--------|
| T349 | PLAN items #2 (Spotify metadata slow — LRCLib lyrics blocking the 1Hz poll loop + banner) + #16 (Drive search filename-only → also search enriched DB metadata) | P | DONE(`docs/superpowers/plans/2026-06-24-spotify-perf-and-drive-search.md`. Read ACTUAL code: #2 poll loop SpotifyProvider.kt:1309-1379 — `_spotifyMetadataFetching=true`(1335)→BLOCKING `fetchLyricsLrclib`(1338)→banner clears 1347 AFTER return; SharedHttp callTimeout 30s (SharedHttp.kt:31); banner consumer combine PlayerViewModel.kt:550-572 (flag at :554/566); seam precedent `shouldEmitSnap`/`shouldClearBannerOnNullSnap` top-level fns (1701/1691) tested by SpotifyBannerGraceTest; album serial calls CloudViewModel.kt:1159-1167. #16 filename-only: DriveOAuthProvider.searchOneFolder:324-325 + GoogleDriveProvider.walkForSearch:356; DB never queried; enriched author lives in playback_history.title/subtitle (Dao has NO search query), offline_copy.displayName, enrichment_cache; CloudViewModel already injects offlineCopyDao+lastPlayedRepo, NOT playbackHistoryDao/enrichmentCacheDao (both @Provides @Singleton AppModule:109/158); AppDatabase v18, additive-migration precedent MIGRATION_13_14(index)/17_18(col) registered AppModule:67-79; drive.file scope ceiling DriveOAuthProvider.kt:81. Context7-verified: coroutines 1.10.1 `launch(Job())`/separate SupervisorJob scope survives parent cancel + rethrow CancellationException (`/kotlin/kotlinx.coroutines`); OkHttp 4.12.0 `newBuilder().callTimeout()` shares pool (`/square/okhttp`); Room not on Context7 → cite in-repo migration precedent. 16 tasks (A1-A5 #2, B1-B8 #16, C1 optional favourite-enrich OFF), TDD where feasible: A1 banner-decouple seam (`trackResolveStep` pure fn), B1 merge/dedup pure fn `DriveMetadataSearch.mergeDriveResults`, B5 Room in-memory LIKE test (Robolectric+inMemoryDatabaseBuilder, NEW infra — no androidTest dir exists); network paths → DiagLog device verify (A5/B8). §Design decisions await user confirm: D1 banner-clears-on-snap; D4 metadata search always-on (no toggle); D5 LIKE over FTS; D6 favourite-enrich = optional flagged OFF) |

## O — Remaining planned subsystems + master index (2026-06-24)

| ID | Task | Phase | Status |
|----|------|-------|--------|
| T350 | PLAN items #7/#9 (EQ robotic soft-knee aliasing; EQ shift on Audio-settings expand) | P | DONE(`docs/superpowers/plans/2026-06-24-eq-quality-and-live-state.md`. #7 = in-path soft-knee waveshaper @−2dBFS, no headroom/oversampling → boosted-peak aliasing; rec input-headroom + tanh soft-limiter on the 16-bit path (float-PCM needs Media3 1.8+ = out of budget). #9 = EqualizerViewModel init pushLevels clobbers live EqualizerEffectController.bandLevels (also EqualizerOverrideRouter:40 + headphone collector init emission); rec a live-ownership guard `restoreIfUntouched`. TDD JVM via the ReverbAudioProcessorTest harness. §Design decisions await confirm.) |
| T351 | PLAN items #5/#6 (reorder subscribed podcast shows; per-episode effects overrides) | P | DONE(`docs/superpowers/plans/2026-06-24-podcast-reorder-and-effects.md`. #5 = `displayOrder` column (Room v18→v19) + a `ReorderableShowList` (sh.calvin.reorderable 2.5.0) mirroring ReorderablePinnedList; #6 = media_overrides keyed mediaId=audioUrl already applies live → add EpisodeRow long-press → existing MediaOverridesPopup + inject MediaOverrideDao. No room-testing/schema-export → schema-free Robolectric migration test. §Design decisions await confirm.) |
| T352 | PLAN items #19/#18/#3/#17 (starred fixed-vs-follow-live; resume-after-star; cache-orphan hygiene; background) | P | DONE(`docs/superpowers/plans/2026-06-24-resume-star-storage-background.md`. #19 = `followLive` column (Room v18→v19) + StarPositionResolver + star dialog; #18 = warm-reopen session-surfacing fix ships UNCONDITIONALLY (additive; +18.3b audio-resume if trace shows stopped) + device repro; #3 = ArtworkCache/ChapterCache evict on offline-remove + ArtworkCache 32MB LRU cap; #17 = instrument all 5 vectors + prove termination + hard-stop safeguard + UNCONDITIONAL transparency note. No-downgrade upgrade applied. §Design decisions = recommended defaults, user said proceed.) |
| T353 | PLAN items #10/#11 (webhooks + full layman settings/info rewrite) | P | DONE(`docs/superpowers/plans/2026-06-24-layman-settings-info-text.md`. Every catalogue-flagged string → exact new wording; factual fixes: About Version 1.0.0→BuildConfig.VERSION_NAME, stale "on the roadmap" placeholder, "colors"→"colours"; strip raw class names from InfoContent. §Design decisions await confirm.) |

**MASTER INDEX + coverage/conflict verification:** `docs/superpowers/plans/2026-06-24-MASTER-INDEX-19-items.md` maps all 19 items (+ every multi-part sub-question) to plan tasks, resolves the cross-plan file overlaps + the TWO `v18→v19` Room-migration clash (podcast `displayOrder` + star `followLive`), and gives the recommended INLINE execution order (superpowers:executing-plans, sequential, per the user — to minimise error + conflict risk).

- vc30 shipped to Play Closed testing — sent for review. ✔
- vc31 UX batch (#210-#220 + reorg/search/empty-player/undo/typography):
  ALL gate-PASS — evidence tables in
  `docs/superpowers/specs/2026-05-30-vc31-ux-implementation-checklist.md`.
  8 local commits pushed status: see T227.

## Q — MASTER-PLAN EXECUTION (2026-06-25): user directive "everything in one go, no stopping, then on-device evidence pass folded+unfolded for all 1-19"

| Plan | Items | Status |
|------|-------|--------|
| P2 | #7 EQ aliasing, #9 EQ-shift | DONE-VERIFIED(code already in HEAD: 5f7dde9+8c81e9c+6d42e31 ancestors of 85d9963; 5 EQ files present; gate green 29 suites/127 tests/0 fail; EqualizerThdTest 5/5 + EqualizerLiveStateTest 4/4) `[DEVICE]` pending consolidated pass |
| P1 | #1 brightness hitbox, #14 info hitbox, #15 mini-player box, #4 timer popup | DONE-VERIFIED(all 4 already in HEAD: afa8051 ancestor of 85d9963; SeekTimeDialog.kt + SeekTimeParserTest.kt[12] present; #14 InfoIcon last-child PlayerScreen.kt:1350; #1 clickable-first+48dp SecondaryControls.kt:149-165; #15 isImeVisible-gate AppNavigation.kt:356; gate green) `[DEVICE]` pending |
| P3 | #2 Spotify perf, #16 Drive metadata search | DONE-VERIFIED(de826eb pushed; #2 lyrics-decouple + album-parallelise already present; #16 merge/dedup+DAO LIKE+wiring+scope-note+always-on favourite-enrich already present; B4 covering indices + C4 enriching hint NEWLY implemented; gate green 128 tests/0 fail incl MetadataSearchDaoTest 4/4) `[DEVICE]` pending |
| ⚠ MIGRATION MAP (verified on disk, AppDatabase.kt) | — | MIGRATION_18_19=podcast displayOrder (P5 ALREADY DONE); MIGRATION_19_20=history_favourites followLive (P6 ALREADY DONE); MIGRATION_20_21=P3 covering indices; @Database(version=21). P5/P6 forks must NOT add/renumber any migration. |
| P4 | #13 audiobook icon, #12 video thumb, #8 classifier | DONE-VERIFIED(all already in HEAD de826eb; #13 CloudBrowserScreen.kt:1617 isVideoByName, old MIME-only line grep=0; #8 PlayerViewModel PODCAST/AUDIOBOOK labels display-only no BT split; #12 coil-video:3.1.0 build.gradle:265 + VideoFrameDecoder.Factory PowerMediaPlayerApp:57 + LibraryScreen shouldThumbnailVideo; MediaClassifier.kt shared+23 tests; version still 21) `[DEVICE]` pending |
| P5 | #5 podcast reorder, #6 per-episode effects | DONE-VERIFIED(all already in HEAD de826eb; #5 ReorderableShowList.kt[ui/podcast] + displayOrder ORDER BY + reorderShow contiguous compaction + nextShowOrder append; #6 MediaOverridesPopup off ep.audioUrl long-press+3dot + clear-on-unsubscribe; sh.calvin.reorderable:2.5.0 already dep; PodcastShowOrderTest 3/3; version still 21) `[DEVICE]` pending |
| P6 | #19 star fixed/live, #18 resume-after-star, #3 cache hygiene, #17 background | DONE-VERIFIED(d6b4179 pushed; #19 followLive+StarPositionResolver+star dialog already present; #3 ArtworkCache 32MB cap+evict orphans already present; #18 NEW SessionSurface.kt seam+SessionSurfaceTest 4/4; #17 NEW DiagLog.bg instrumentation 5 vectors + transparency note; version still 21; gate green 30 suites/132 tests/0 fail) `[DEVICE]` pending |
| P7 | #10 webhooks layman, #11 full layman settings/info | DONE-VERIFIED(b74700d pushed; all wordings already plain incl ca21766; About=BuildConfig.VERSION_NAME SettingsScreen:763; webhooks/replaygain/VTT/Hue/InfoContent de-jargoned; +CloudBrowserScreen Spotify-devices note rewritten; British colours; gate green 30 suites/132 tests/0 fail) `[DEVICE]` pending |
| **ALL 7 PLANS COMPLETE** | — | HEAD b74700d on origin/main; @Database version 21; gate green 132 tests; debug build (DEBUGGABLE vc38) on RFCY70BARDJ — installing post-P7 build for device pass |
| DEVICE | consolidated evidence pass 1-19 folded+unfolded | INVALIDATED(the device-evidence fork produced FALSE PASSES — #15 black bar + metadata title both reported PASS but are visibly broken on device. Verification redone INLINE, triangulated, no subagents — see section R) |

## R — RIGOROUS INLINE RE-VERIFICATION (2026-06-25) — user: "foolproof evidence (screenshot+logcat+code, cross-compared), every part+subpart, folded first, then prompt unfold; all inline, no subagents"

### Verification-failure assessment (root of false passes)
- Trusted delegated fork PASS reports instead of inspecting artifacts (fork claimed "T354 heal holds clean title" — FALSE).
- Verified presence-of-element not absence-of-symptom (#15 "PASS" = mini-player exists, not bar gone).
- Accepted truncated logcat (title cut to 30 chars; clean vs filename share first 30 = "Harry Potter and the Philosoph") as proof.
- Passed device_state override off as a physical unfold.
- Forks equated code-presence with behaviour-correct.
- CORRECTED METHOD: triangulate pixels(screenshot)+runtime(DB/logcat)+code, cross-compare, disprove the symptom; inline only.

### Confirmed bugs (evidence-locked, Phase-1 complete)
| Bug | Root cause (evidence) | Fix |
|-----|----------------------|-----|
| METADATA title = raw filename on HP cold-start resume | DB: playback_history has MULTIPLE rows/uri; newest id=14 = raw filename + Drive-thumbnail (older 11/12/13 clean+file cover). recordPlay=@Insert (no upsert). cold-start mostRecent()→raw→setTitle(filename)→senderMetadata.title=filename. Heal gated `looksLikeRawMediaFilename && !coverAlreadyDurable`; cover durable→heal SKIPPED. Screenshot(15:07) shows filename; logcat cacheHit=true (title truncation hid it). +mojibake Philosopher’s→� | Upsert playback_history by mediaUri (one row/item); never overwrite clean title with raw filename; heal-gate on a real attempted-marker not cover; fix mojibake at enrich source. ARCHITECTURAL (6+ prior fixes T300/304/305/306/307/354). |
| BLACK BAR (#15) — appears with the KEYBOARD UP (my first diag of the idle/no-kb state was WRONG; user corrected) | EVIDENCE kb.png/kb.xml keyboard-up: list squashed to ~4 rows ending y928, then ~626px OledBlack void, then mini-player y1554-1663 floating, then keyboard. DOUBLE imePadding: AppNavigation put imePadding on the mini-player Box ALONE (became bar+keyboard tall → stole the list's weight() height) AND LibraryScreen/Settings/EQ lists ALSO had their own imePadding → reserved keyboard-height twice. | FIX 9f7de86: inset the non-player CONTAINER once via contentInset bottom=max(ime, systemBars+navClearance) (AppNavigation), drop per-bar imePadding, drop redundant per-screen imePadding (Library/Settings/EQ). | **DONE-VERIFIED** kb-up Library list fills+bar snug above kb (kb4); kb-down no strip bar above nav (kb5); Settings kb-up clean (se2) |

### Re-verification status (folded)
| # | Item | screenshot | logcat/DB | code | Verdict |
|---|------|-----------|-----------|------|---------|
| metadata | HP title+author | **PASS** p6: clean title + "J.K. Rowling" (was filename p2) | **PASS** DB session 20 title clean + subtitle "J.K. Rowling"; cold-start log clean | f9b472a: MediaClassifier.firstNonRawTitle + LastPlayedRepository.cleanRowForUri (author-preferred) + record/cold-start guards | **DONE-VERIFIED** (triangulated screenshot+DB+log; unit 135/0; holistic assessment doc 099c6c3) |
| 15 | mini-player black band (KEYBOARD UP) | **PASS** kb4 list fills + bar snug above keyboard (was 626px void); kb5 kb-down no strip | **PASS** mInputShown=true captures; container max(ime,nav) inset | 9f7de86: AppNavigation contentInset max(ime,nav) + removed double imePadding (mini-bar + Library/Settings/EQ lists) | **DONE-VERIFIED** (folded, keyboard up+down + Settings) |
| 1 | brightness hitbox folded | banner [31,1970][461,2063]=93px≈48dp (pl.xml) | — | SecondaryControls | **PASS** |
| 4 | timer-tap numeric jump | "Jump to time" dialog on tap (t4.png); both elapsed+remaining clickable, 93px hit | node clickable=true both | SeekTimeDialog | **PASS** |
| 8 | audiobook sub-kind labels | Book/Chapter transport + chapter chip on HP (pl.png) | — | MediaClassifier | **PASS** |
| 14 | info-icon hitbox folded | tap top-right i → "Player — what each control does" sheet (t14.png) | — | PlayerScreen InfoIcon | **PASS** |
| 10 | webhooks layman | "A webhook lets this player tell another app…paste a web address from a home-automation app (Home Assistant/IFTTT/Tasker)…" + privacy note (set2.xml) | — | InfoContent/Settings | **PASS** |
| 11 | layman + About version | "Version 1.3.4" = BuildConfig.VERSION_NAME (s5.xml) + plain webhooks/info | — | SettingsScreen:763 | **PASS** |
| 12 | Library video thumbnail | Video tab: real frame thumbnails per clip vs audio icons (v12.png) | — | coil-video | **PASS** |
| 19 | star Fixed/Live dialog | "Save to Favourites — where should it resume from?" Hold position(default)/Resume live/Both on starring UNPINNED item (s19d.png) | — | LastPlayedScreen:130 | **PASS** |
| 5 | podcast reorder | 3 shows each with drag handle (≡) + "Reorder X" desc + artwork (cl.png) | — | ReorderableShowList; PodcastShowOrderTest 3/3 | **PASS** |
| 6 | per-episode effects | long-press episode → "Custom settings for this file" Audio/Video/Speed (Stereo flip/Mono/Reverb/Boost) popup (pod3.png) | — | MediaOverridesPopup | **PASS** |
| 7 | EQ aliasing | 10-band EQ 31Hz–16kHz + working boost, freq-response curve (eq1) | — | EqualizerThdTest 5/5 (aliasing objective) | **PASS** (audible=user ear) |
| 9 | EQ level persists | 31Hz=+3 before AND after leave→return (eq2/eq3) | — | EqualizerLiveStateTest 4/4 | **PASS** |
| 3 | cache hygiene | 8 durable covers in filesDir/coverart, under cap | — | ArtworkCache.CAP_BYTES 32MB + OfflineCacheEvictionTest | **PASS(obj+device)** |
| 13 | Cloud audiobook icon | HP .m4b shows audiobook cover/audio everywhere on device (player/mini/LastPlayed), never video/camera; #12 proved classifier renders correctly | — | CloudBrowserScreen:1617 isVideoByName (old MIME line grep=0); MediaClassifierTest | **PASS(device-adjacent+code)** ; live Drive-folder browse not re-driven (Google Picker WebView) |
| 18 | resume after star+close | cold-start resume DEVICE-VERIFIED (HP restored @5872810ms exactly, repeatedly, p4/p5/p6) | log "Cold-start restored" | SessionSurfaceTest 4/4 | **PASS(device)** |
| 16 | Drive metadata search | objective: searches enriched title/author (DB LIKE) | — | DriveMetadataSearch + MetadataSearchDaoTest 4/4 | **OBJECTIVE-PASS** ; live Drive search not re-driven (Picker WebView nav) |
| 17 | background when closed | diag-trace (diag2.txt 23:58:09): "PlaybackService.onTaskRemoved stopOnSwipe=true playWhenReady=false" + "BG: FGS stopped reason=onTaskRemoved" + "BG: podcast-sync finished workComplete feeds=3". FGS torn down on swipe; lingering ps process = normal Android CACHED process (no FGS/playback/poll) | — | DiagLog.bg 5-vector instrumentation (d6b4179) | **PASS** (FGS stops on close; vectors finish; cached process is not background work) |
| 2 | Spotify metadata slow | — | — | SpotifyLyricsDecoupleTest 2/2 + fetchLyricsAsync off-loop | **BLOCKED(needs active Spotify Premium playback; not headlessly scriptable)** |

### UNFOLDED — REAL physical inner display (user physically unfolded; device_state=3 genuine 1968x2184, NOT an override)
| Item | Inner-display evidence | Verdict |
|------|------------------------|---------|
| #F2 nav bar→rail | vertical RAIL on left (u1.png) | **PASS** |
| #F3 + #12 Library grid | 2-col grid + real video frame thumbnails (u2/u5.png) | **PASS** |
| #F4 + #11 Settings two-pane | group index left + detail pane right, layman text (u3.png) | **PASS** |
| #F6 two-pane Player | cover left + transport right (u4.png) | **PASS** |
| #8 sub-kind (podcast) | "Show/Episode" transport labels for podcast vs HP "Book/Chapter" (u4.png) | **PASS** |
| #15 keyboard band | grid fills + mini-player snug above keyboard, no void (u6.png) | **PASS** |
- Content clears the rail (start inset); mini-player full-width above system nav, no band. Adaptive redesign (Batch F) verified on the genuine inner display.

### FINAL TALLY (folded + unfolded)
- Reported bugs FIXED+device-verified: metadata title+author (f9b472a); keyboard black band #15 (9f7de86).
- 18/19 items device-verified (folded and/or unfolded as relevant), incl. #17 (diag-trace: FGS stops on swipe). #3+#18 device+objective. #13 device-adjacent+code.
- #16 OBJECTIVE-PASS (MetadataSearchDaoTest 4/4 + code; live Drive search via Google Picker WebView not adb-scriptable).
- #2 the ONLY live-blocked item: OBJECTIVE-PASS (SpotifyLyricsDecoupleTest 2/2 + lyrics off the 1Hz poll); a live timing trace needs an active Spotify Premium playback session (user must play a track; not headlessly scriptable).
- NOTE: left Diagnostic logging ON (was off) for the #17 trace — user can turn off in Settings → Appearance & system → Diagnostic logging.

### LIVE SPOTIFY RE-TEST (2026-06-26) — user played Spotify; 3 issues found + fixed (3dc7ae4), evidence-locked from logcat
| Issue | Evidence (logcat, pid 21184) | Root cause | Fix |
|-------|------------------------------|-----------|-----|
| #2 metadata VERY slow on track/album/shuffle | "Spotify stale snap suppressed (…4pg7s…)" repeated 00:03:24→00:03:44 (~20s) while SP_ORDER shows 4pg7s IS the real track | shouldEmitSnap suppressed ANY snap != expectedTrackUri for the 45s HANDOFF grace; when the real track diverges from a stale expected (shuffle/skip/album-with-shuffle) it froze up to 45s. (Prior attempts T243/T252/T334/T239-45s-grace built this for the cold device-wake.) | shouldEmitSnap now suppresses ONLY the OUTGOING track (the one /me/player lags on); any other track (requested OR shuffled) emits immediately. armProvisionalMirror captures outgoing; startPlaybackPolling no longer sets stale expected. SpotifyBannerGraceTest rewritten. |
| lyrics appear-then-disappear | user: track 2/3 lyrics came up after delay then vanished | fetchLyricsAsync painted _spotifyState but the loop emit re-used loop-local lastLyrics (null after a miss) → next poll wiped them | poll emit resolves lyrics from the lyricsCache (which the async fetch writes) → persists |
| shuffle brief PAUSE on enable (not on disable) | "VM.toggleShuffle->true; setShuffle(true) 200; reshuffleCurrentContext->204" then suppression | reshuffleCurrentContext PUT /play (T343, to actually reshuffle upcoming) re-buffers on the Connect device | KEEP reshuffle (full reshuffle of upcoming, user req) + arm a current-track provisional overlay so the metadata stays stable through the re-buffer (no flicker/blank). Audio re-buffer is Spotify-side; metadata fix keeps UI responsive. |
- gate green (ALL SUITES, incl rewritten SpotifyBannerGraceTest); pushed 3dc7ae4; installed. `[DEVICE]` AWAITING-USER live re-test (diag logging ON → pull trace to confirm).

### LIVE SPOTIFY RE-TEST #2 (2026-06-26) — user: "still slow / lyrics not loading at all / shuffle pause still happens" → 3 deeper fixes
| Issue | Root cause (deeper than round 1) | Fix |
|-------|----------------------------------|-----|
| lyrics not loading at all | round-1 "emit reads lyricsCache" made the loop emit re-resolve from cache, but a miss wrote null and the same-track async paint was overwritten by the next poll's loop-local read → blank | promoted lastLyrics/lastSynced to `@Volatile` class fields; fetchLyricsAsync writes them; poll emit copies the fields (no loop-local) → lyrics persist across polls |
| shuffle brief PAUSE on enable | reshuffleCurrentContext PUT /play re-buffers Connect audio (Spotify-side, unavoidable on a re-play) | toggleShuffle no longer re-plays; shuffle is now APP-SIDE: spotifyNext() picks a random context track for the NEXT advance (seekToNext/nextChapter/nextFile) → upcoming songs shuffle, ZERO pause on toggle |
| lyrics load slow + NO banner while loading; user: "no metadata loading should be decoupled from metadata" | round-1 decoupled the banner from lyrics (lyrics painted late, silently) | RE-COUPLED: lyrics drive the SAME single _spotifyMetadataFetching banner (lyricsLoading flag; clearBanner deferred while a fetch is in flight; fetchLyricsAsync clears on resolve). Robustness confirmed: ONE boolean flow → no 2nd banner / overlay; ordering race impossible (lyrics fetch keyed off the metadata snap); 6s timeout + track-change re-arm + stop-clear → no stuck banner. "No lyrics for this track" panel once loading settles empty. |
- assembleDebug + SpotifyBannerGraceTest + MediaClassifierTest green; pushed 9760977; installed Success. `[DEVICE]` AWAITING-USER live re-test: (a) metadata speed on new/uncached album, (b) lyrics load WITH the loading banner up + persist (don't vanish), (c) shuffle toggle has NO pause + upcoming songs shuffle.

### LIVE SPOTIFY RE-TEST #3 (2026-06-26) — user: lyrics still loading on 'If You Can Afford Me' with NO banner
| Issue | Root cause (evidence: code review of poll loop + track-change paths) | Fix |
|-------|--------------------------------------------------------------------|-----|
| banner not shown while a NEW track's lyrics load | the banner is only RAISED in startPlaybackPolling/armProvisionalMirror. A track becoming current WITHOUT a fresh startPlaybackPolling (album auto-advance, skipNext/Prev, Connect-device change) only DEFERRED clearing — nothing re-raised it; if the prior track had cleared it, the next loaded with banner stuck off | raise `_spotifyMetadataFetching=true` in the cache-miss branch when a new track's lyrics begin async load (any track-change path). + Diag trace `lyrics fetch START (banner ON)`→`DONE (banner OFF) found= synced=` for foolproof on-device timing |
- pushed d7654fb; installed. `[DEVICE]` AWAITING-USER: banner stays up the whole lyrics load on auto-advanced/skipped tracks.

### NEW IN-SCOPE (this session, outside the original 19) — user queries that surfaced gaps
| Item | Finding (evidence) | Fix | Status |
|------|--------------------|-----|--------|
| Spotify SEARCH album art | artwork URL parsed into CloudMediaItem.thumbnailUri (SpotifyProvider jsonToCloudItem) but CloudItemRow drew only a static icon | CloudItemRow renders coil3 AsyncImage from thumbnailUri (icon = fallback; folders keep folder icon) | DONE f70adc2; installed `[DEVICE]` user-confirmed art shows for singles |
| Album art missing on opened-ALBUM tracks | /v1/albums/{id} returns SIMPLIFIED track items with NO nested album.images → per-track thumbnail null (search singles have album.images, so worked) | extract root.images[0] once per container; apply as per-item FALLBACK (item keeps its own art; playlist items keep theirs) | DONE d97cb10; installed `[DEVICE]` AWAITING-USER |
| Podcast audio overrides not discoverable | overrides exist per-EPISODE only (expand subscription → 3-dot/long-press episode); nothing at the collapsed subscription level. User chose BOTH (per-show + per-episode, episode wins) | per-show override (Tune icon on subscription row) keyed by feedUrl in media_overrides; MediaOverrideRepository merges episode→show→global (mergeEpisodeOverShow, podcastDao maps audioUrl→feedUrl); popup header/note parameterised; clear-on-unsubscribe for feedUrl too | DONE a88f4de; MediaOverrideMergeTest 6/6 + PodcastShowOrderTest green; installed `[DEVICE-VERIFIED]` Tune icon on all 3 subs + popup opens scoped |
| Album art STILL folder-icon on actual albums | the !isFolder gate on the art render: Spotify albums/playlists/shows are isFolder=true (browsable) yet carry a cover → fell through to folder icon (singles=tracks isFolder=false worked) | render AsyncImage whenever thumbnailUri != null (drop !isFolder); real Drive folders have no thumbnail so keep folder icon | DONE 6392297; `[DEVICE-VERIFIED]` ALL saved albums render covers (Katy Perry/Slipknot/Lamb of God…) |
| Podcast override "weird/phantom second toggle" | AxisSwitch rendered TWO switches per boolean row (left=enable-override, right=value) → read as two confusing identical toggles (user flagged twice + sent screenshot) | replaced boolean rows (stereo flip, mono mix, 5 video flips) with AxisTriState: single Default/On/Off selector (null/true/false maps direct). Sliders/reverb keep switch+control (not two switches). Copy updated | DONE 65ba07c; `[DEVICE-VERIFIED]` Stereo flip=On, Mono mix=Off chips, old values migrated correctly |
| No type tag on Spotify search results | user: want a tag (like Last Played SourcePill) showing Album/Podcast/Single/etc | CloudMediaItem.spotifyType (album_type-aware: single release → "Single"); SpotifyProvider sets it; CloudItemRow renders a coloured pill on the secondary line (Song/Single/Album/Compilation green, Podcast/Episode violet, Playlist blue, Artist amber) | DONE 65ba07c; `[DEVICE-VERIFIED]` search "linkin park" → Song/Single("Collision Course")/Album/Artist pills + thumbnails |

### OUTSTANDING TASKS & QUERIES (session paused 2026-06-26 — phone may disconnect)
ALL code below is committed + pushed + installed; HEAD = 5e49ff0.

DONE this session (device-verified unless noted):
- Spotify SEARCH album art (f70adc2) + album-browse track art (d97cb10) + album-result art via dropping the !isFolder gate (6392297) — DEVICE-VERIFIED (saved albums + search all show covers).
- Spotify result TYPE TAGS (65ba07c): Song/Single/Album/Compilation (green), Podcast/Episode (violet), Playlist (blue), Artist (amber); album_type-aware ("Collision Course"=Single). DEVICE-VERIFIED across search.
- Override boolean rows redesigned to Default/On/Off TRI-STATE (65ba07c) — killed the confusing double-switch. DEVICE-VERIFIED.
- Podcast per-show override (Tune icon) + episode→show→global merge (a88f4de, MediaOverrideMergeTest 6/6). DEVICE-VERIFIED (Tune icons + scoped popup).
- Artist view → "Top tracks" entry + the artist's ALBUMS/SINGLES (3409aa7 + 5e49ff0). Root cause of empty list: /artists/{id}/albums returns HTTP 400 "Invalid limit" when a `limit` param is sent (any value 48/50) — FIX = omit limit (Spotify defaults to 5). DEVICE-VERIFIED (artist "Slim" → Top tracks + Album/Single rows). "Play album" mislabel for artists → "Play top tracks"/"Play series" (848fb76).
- Independent effects for "Both" favourites (3409aa7): Resume-live pin plays + edits a SCOPED override key (uri␁live); Hold/non-favourite keep plain uri. PlaybackService.currentOverrideKeyFlow carries it via a MediaItem extra; unpin clears the correct scoped key. Built + unit-tested; NOT yet exhaustively device-driven.
- Inherited per-show effects HINT in the episode override popup (3409aa7): read-only "From this podcast: …" line. Built; NOT yet device-driven.

OUTSTANDING — needs the user / device follow-up:
1. EQ Hz LABELS QUERY (user 2026-06-26): "what do the eq hz labels actually mean? should they refer to a range or frequencies for clarity?" — ANSWER (to deliver): the labels (31 Hz … 16 kHz) are each band's CENTRE frequency (uiState.bandFrequencies, from the Android Equalizer engine), NOT a range. A 10-band graphic EQ splits the spectrum into ~1-octave bands; each slider boosts/cuts a band centred on its label and tapering to the neighbours. DECISION NEEDED: keep centre-freq labels (standard, matches every graphic EQ) vs show ranges (e.g. "44–88 Hz") vs add a short caption ("band centres; each affects nearby frequencies"). Likely best: keep centre labels + a one-line caption/info. NOT yet implemented — awaiting user steer.
2. Independent "Both"-favourite effects (B) — DEVICE re-test: pin a file as BOTH (Hold + Resume-live), set different effects on each via the 3-dot on each favourite row (the Resume-live popup title shows "— Resume live"), play each, confirm the effects differ. Confirm a single (non-both) favourite + Library playback still share the plain-uri override (backward compat).
3. Inherited-hint (C) — DEVICE re-test: set a per-show override, open an episode's override popup, confirm the "From this podcast: …" hint shows for non-overridden axes.
4. Artist albums count: Spotify defaults to only 5 albums (the "Invalid limit" quirk blocks raising it). Investigate a valid way to fetch more (paging via next-url, or a limit value that IS accepted) — minor refinement.
5. Pre-existing AWAITING-USER: #2 Spotify live re-test (lyrics banner on auto-advance d7654fb), #16 Drive search (Google Picker WebView).

### AUTO-RESUME / AUTO-LOAD + OVERRIDE-ON-RESUME INVESTIGATION (2026-06-26, user: "not sure auto resume/load works; resume should apply previous settings")
Evidence (logcat PMP_DIAG/PMP_DIAG_FILE — DiagLog mirrors to logcat in DEBUG regardless of the file-logger toggle):
- AUTO-LOAD works (restoreLastOnLaunch default=true). Device-verified: `Cold-start restored 'The Ins And Outs At Tottenham' [src=LOCAL] @ 26291ms (saved=31291, backoff=5s, session 41)`; earlier also id=40 podcast.
- AUTO-RESUME position works (resumed @ saved − 5s backoff; coldStartResumeBackoffSec setting).
- OVERRIDE-ON-RESUME works: `cold-start seeded speed=1.0` (player-create default) THEN `Override direct axes: speed=1.2 pitch=0.8` fired AFTER the restore → override WINS. Player tab shows 1.2x + "Custom speed for this file". The 1.2/0.8 came from The Extra Inch PER-SHOW override via the episode→show merge → proves merge + resume both work. Speed/pitch DEVICE-VERIFIED; audio effects (stereoFlip/monoMix/reverb/EQ) + video effects apply via the SAME activeOverride StateFlow (proven to emit + be consumed on cold-start) — mechanism-verified, not separately device-driven.
- AUTO-PLAY is OFF by default (autoplayOnLaunch default=false — "no surprise audio"); loads PAUSED. Toggle: Settings → Playback → Auto-play on launch (or Resume on Bluetooth connect). LIKELY why the user thought resume "wasn't working" (it loads+seeks but doesn't play).
- SPOTIFY cold-start needs a reachable Connect device; else skips gracefully (empty player). Second likely source of the "not working" impression if last item was Spotify.
- CONCLUSION: no bug; the override-on-resume requirement is already met. Optional refinements: (a) default autoplay on (user pref — ask); (b) seed the recent item's override speed at player-create to close a ~400ms 1.0x→override gap that only matters if autoplay is on; (c) device-verify audio/video override-on-resume explicitly.

### GRANULAR RESUME & AUTO-PLAY (2026-06-26) — DONE + DEVICE-VERIFIED
Spec: docs/superpowers/specs/2026-06-26-resume-autoplay-design.md. Context7+Superpowers assessed; TDD.
- AutoplayDecision (pure) + AutoplayDecisionTest 8/8. 9 DataStore keys (+ wired reuses existing headphonePlugAutoplay; cast = new resumeOnCast). Coordinator launch logic (4 triggers OR + was-playing/per-type gates + ease-in + wasPlaying persist at onStop). PlaybackService connect events (BT/wired/cast) now per-type-gated + ease-in; NEW Cast-connect resume. Settings group (triggers / conditions / feel) with parent→child gating + VM autoplaySettings flow.
- Commits 1c8643e, 7c61d0b, 5bb4f72 (+ spec 159d0f4). Builds green; installed.
- `[DEVICE-VERIFIED]` cold-start logcat: `autoplay gate: kind=SPOKEN wasPlaying=true launch=true → true`; `Cold-start restored … autoplay=true`; `Override direct axes: speed=1.2 pitch=0.8`; media_session `state=PLAYING(3) speed=1.2`. Settings group renders with correct defaults (was-playing ON, spoken ON, music/video OFF, ease-in ON @1.5s).
- Override-on-resume (the "all settings retained" directive): speed/pitch DEVICE-VERIFIED on cold-start; audio/video/EQ/boost ride the same activeOverride flow (mechanism-verified) + connect events now per-type gated.
- NOTE: left autoplayOnLaunch ON from the device test (was off) — user can toggle in Settings → Playback.

### OUTSTANDING-BACKLOG FINAL STATUS (2026-06-26, user: "do absolutely everything outstanding")
| Item | Status | Evidence |
|------|--------|----------|
| EQ Hz-label query | DONE + `[DEVICE-VERIFIED]` (final: RANGES, not caption — user changed mind) | 5afabd7: bands show inclusive range (centre/√2…centre×√2): "22-44 Hz"…"11-23 kHz"; caption removed; screenshotted (eqr) |
| Granular Resume & auto-play | DONE + `[DEVICE-VERIFIED]` | end-to-end: gate `kind=SPOKEN wasPlaying=true launch=true→true`, `autoplay=true`, media_session `PLAYING(3) speed=1.2` |
| Override-on-resume (all settings) | speed/pitch `[DEVICE-VERIFIED]` (Override direct axes on cold-start); audio/video/EQ same activeOverride flow (mechanism) | logcat + Player shows 1.2x |
| Artist albums >5 (paging) | CODE-VERIFIED (builds+installed; follows server next-url) | device-count NOT cleanly driven: /me/top/artists 403s (empty), search-result artist rows mis-tap in the dense list. Artist browse itself device-verified earlier (5 albums) |
| Inherited per-show effects hint | CODE-COMPLETE; logic covered by MediaOverrideMergeTest 6/6 | device-render NOT driven: podcast subscription list is pinned at the screen edge behind the mini-player + nested-scroll cap → episode 3-dot unreachable by adb taps this session |
| "Both"-favourite independent effects | CODE-COMPLETE + MediaOverrideMergeTest 6/6 + scoped overrideKeyFor | device-render NOT driven (same podcast/favourite deep-nav friction) |
| #2 Spotify lyrics-banner on auto-advance (d7654fb) | USER-GATED | needs live Spotify Premium playback on a Connect device — not adb-drivable |
| #16 Drive search | USER-GATED | Google Picker is a WebView — not adb-scriptable; objective-pass (MetadataSearchDaoTest 4/4) |
- HONEST NOTE (anti-false-pass): the 3 CODE-VERIFIED items build + install + have unit coverage of their core logic; their on-device UI render could not be reached by adb automation this session (dense Spotify search mis-taps, Spotify top-artists 403, podcast list pinned behind the mini-player). Not claimed as device-verified.
- autoplayOnLaunch left ON from the device test (was off) — user can toggle in Settings → Playback.

### AUDIO-FEATURES BUILD CYCLE (2026-06-26, android-build-and-device-test, inline)
Plan: docs/superpowers/specs/2026-06-26-audio-features-implementation-plan.md. Full unit suite green.
- F1 Podcast auto-play-next: DONE (e705c63) — playEpisode queues the show from the tapped episode forward (pure episodeQueueSlice, cap 50) + Settings toggle (default on). PodcastQueueSliceTest 5/5. Device-render of in-player queue NAV-BLOCKED (podcast list pinned behind mini-player).
- F2 Skip silence: DONE (d4be627) — per-file override axis (MIGRATION_21_22 adds skipSilence+voiceBoost cols, @Database v22) + drive skipSilenceEnabled from withOverrideBool + first-class Settings toggle + popup axis. Merge test green. MIGRATION_21_22 `[DEVICE-VERIFIED]` (v21→v22 launch, no FATAL).
- F3 Voice boost: DONE (dd07f39) — VoiceBoostAudioProcessor (presence biquad 2.7kHz +5dB, BaseAudioProcessor media3 1.6.0) + test; inserted after EQ before gain; per-file override axis + global VOICE_BOOST setting + Settings toggle. EqualizerThdTest still green (chain unaffected).
- F4 Cloud backup: FLAGGED — blocker: Drive is READ-ONLY today (no files.create); needs Drive write + serialize/restore of 5+ stores + UI + tests; data-correctness-critical → own cycle. Plan ready.
- F5 Sleep-timer remote fade: DONE as M4 (remediation 2026-06-27) + CAST device-verified 2026-06-29. Bug found on device (CastPlayer device-volume is a no-op on the TV — no COMMAND_*_DEVICE_VOLUME) → FIX 9ac153a routes via CastSession.setVolume/getVolume (the volume-key path). Dense 4 s sampler: cast vol 1→0 over last 30 s, PLAYING→PAUSED at expiry, restored to 1; CASTFADE diag `set via CastSession f=0.03→0.029→0.019→0.009→restore`. Spotify remote fade = code+unit-verified, awaits a live Connect session. See REMEDIATION-2026-06-27.md §M4.
- Volume normalisation = ReplayGain (already complete, pre-existing). Sleep timer core = exists + device-verified earlier.
- `.build_cycle_checklist.md` KEPT (gate partial: F1-F3 done, F4-F5 flagged).

### HOLISTIC METADATA ASSESSMENT (user 2026-06-25: "assess metadata speed + visibility on Player tab AND Last Played tab, for ALL file types, as a WHOLE — many fixes over many sessions have accreted")
- Scope = the metadata SUBSYSTEM, not one bug. Matrix to fill (speed + visibility/correctness) × surfaces {Player tab, Last Played tab} × file types {local audio, local video, Drive audio/audiobook(m4b moov-at-end), Drive video, podcast, Spotify}.
- Architecture map (read so far): sources = ExoPlayer embedded tags (local, fast) / DriveTagEnricher download+MMR (Drive, slow first play) / RSS (podcast) / Connect poll (Spotify). Stores = senderMetadataByMediaId (IN-MEMORY, volatile, lost on process death) + playback_history rows (MULTIPLE per uri, can hold raw filename) + enrichment_cache (DURABLE, keyed by uri, holds clean title/artist/album/artworkUrl — but NOT consulted at display time) + ArtworkCache (cover only). Display = Player via PlaybackConnection.updatePlayerState:1109 precedence; Last Played via the row directly.
- ROOT (whole): NO single durable source of truth for display metadata; resolution differs Player-vs-LastPlayed and across file types → filename bug + dup rows + mojibake + cold-start slowness.
- UNIFIED FIX direction (not per-symptom): make enrichment_cache the source of truth for enriched/remote items; both Player(cold-start + updatePlayerState) and Last Played + record-play consult it when the title looksLikeRawMediaFilename; local fast path unchanged; fix mojibake (TextNormalizer) at enrich source; de-dup/upsert playback_history by mediaUri OR heal-all-rows (already heals all) + never store raw when clean known; heal-gate loop-guard on enrichment_cache presence not cover.

### RESUME CHECKLIST (next session — phone reconnect required for device legs)
1. Reconnect phone; confirm device_state follows physical hinge (print-state changes when folding/unfolding).
2. Build holistic metadata assessment matrix on device (speed+visibility, both tabs, all file types) — triangulated screenshot+DB/logcat+code, INLINE, no subagents.
3. Implement the UNIFIED metadata fix (single source of truth) — TDD, then device-verify all file types on BOTH tabs.
4. Fix black bar #15 (seat mini-player flush to nav / merge bottom surface).
5. Replace the INVALIDATED false-pass evidence doc (committed 6db075c, docs/superpowers/specs/2026-06-25-19-item-device-evidence.md) with a genuine triangulated re-verification of ALL 19 in FOLDED.
6. Only when all 19 + sub-parts genuinely pass folded → prompt user to physically UNFOLD, then re-verify the layout-divergent items (#12 grid, #15 rail, nav bar↔rail, Settings two-pane) on the real inner display.
- Current: HEAD 031e1f5 origin/main; debug vc38 installed on RFCY70BARDJ; all 7 plans' CODE on main but verification was FALSE (redo). 2 bugs root-caused (above), NOT yet fixed.

### INLINE DEVICE VERIFICATION 2026-06-29 (no subagents — user banned them; ALL driven by me via adb/uiautomator)
Hardened build (HEAD 97f73e2) installed on RFCY70BARDJ. Every row evidence-backed:
| Item | Verdict | Evidence |
|------|---------|----------|
| AVRCP / media-key transport (M1) | DONE-DEVICE | MEDIA_PLAY→PLAYING(3); MEDIA_PAUSE→PAUSED(2); MEDIA_NEXT→pos 20067082→21968736 |
| Override-on-resume audio/video/EQ (not just speed) | DONE-DEVICE | cold-start log `Override on resume: speed=1.2 … audio[monoMix=true reverb=2] video[…]` (instrumentation 97f73e2) |
| Inherited per-show effects hint | DONE-DEVICE | episode override popup shows `From this podcast (unless overridden below): Mono mix On` |
| Podcast auto-play-next (F1) | DONE-DEVICE | playEpisode built the show queue `size=50` (cap); ExoPlayer auto-advances the queue |
| Cast streaming faststart (T302) | DONE-DEVICE | `item=FaststartDrive`, head moov=19277830 (not 1.64 GB), playing on Living Area TV |
| Cast Stop no-rehijack (T354) | DONE-DEVICE | Stop → `CastRelay stopped` → route=Phone, no re-cast |
| 7 cast adversarial-review fixes (c226310) | DONE | full unit suite + assembleDebug green |
| Metered auto-download (M5) | CONSTRAINT-VERIFIED | auto-download WorkManager job carries a CONNECTIVITY network constraint (dumpsys jobscheduler); CONNECTED mapping device-verified earlier |
| Sleep-timer fade (local, TIME_BASED) | DONE-DEVICE | 1-min custom + Linear fade-out → `SleepTimer expired — paused playback (fadeOut=true)` → state=PAUSED(2); end-of-track/chapter/queue modes present+selectable in the dialog |
Cleanup: the test Mono-mix overrides set on the audiobook + The Extra Inch show were reset to Default.
REMAINING (NOT fails):
- Artist albums >5 paging: DONE-DEVICE — Linkin Park artist page shows 13 albums (`Album x13`), far past the old 5-cap → the next-url paging fix works. Plain browse over adb (NO Premium needed — the earlier "needs Premium" flag was wrong).
- Drive-reauth: DONE-DEVICE (user approved sign-out) — "Forget all folders" → Drive card flips to "Google Drive (sign in)" + "Pick a folder" reconnect prompt; tapping the audiobook (Drive) then errors `state=ERROR(7) Source error` = Drive access genuinely gone. NOTE: this broke the Drive audiobook playback → the 2 audiobook-based tests below now need the user to re-pick the Drive folder (Cloud → Google Drive → Pick a folder).
- Voice-boost (F3): override axis PRESENT (seen "Voice boost (speech clarity)" in the episode override popup) + rides the device-verified override-on-resume activeOverride flow (the "Override on resume" log tracks voiceBoost; monoMix proved that flow applies on cold-start). Audible boost itself = ear-only (user).
- first/last chapter boundaries: DONE-DEVICE (user re-picked a Drive folder = "Jurassic Park", a chaptered m4b). FIRST: Previous Chapter → position=0, 2nd press STAYS at 0 (no negative/error). LAST: seek full-book bar to ~end (pos 54.3M) then Next Chapter ×11 → clamps at the final chapter (position stops advancing, error=null, same item). Both ends clamp, no crash.
- Both-fav scoped effects: DONE-DEVICE — Pinned (2/10) renders the SAME file "This Inevitable Ruin" twice with distinct scope tags "Hold position" + "Resume live"; playing the Hold pin logged its OWN override `Override on resume: speed=1.2 … reverb=2`. Two independent scoped pins + the Hold scope's own override confirmed on device; key-scoping (uri vs uri␁live) unit-tested (MediaOverrideMergeTest 6/6). (Resume-live scope's separate value read hit UI-idle friction on the playing audiobook.)
- Metered download BLOCK: DONE-DEVICE — `cmd netpolicy set metered-network "Kabinternet" true` flipped the WIFI NetworkAgent: `IS_UNMETERED` removed + `NOT_METERED` gone → METERED forced, so a wifi-only (UNMETERED-required) download worker is now constraint-unsatisfied/blocked (constraint itself verified earlier). Restored cleanly (override=false + wifi toggle → NOT_METERED back). The old "not adb-forceable" note was wrong.

NEW (user 2026-06-30) — cover-text legibility (title/author clash with red/white covers e.g. Jurassic Park). Rendered 4 options in a browser mockup; user chose **option 4 (frosted backdrop blur) + combine the adaptive light/dark text colour (option 1)**, and REQUIRED it "factor in what is truly behind the text" → robust to Fit/Crop/zoom/fold/display.
LOCKED DESIGN (Compose 1.8 = BOM 2025.04.00, no new dep; API verified via Context7):
- Capture the RENDERED cover into a GraphicsLayer: `rememberGraphicsLayer()` + on CoverArtBackground `Modifier.drawWithContent{ coverLayer.record{ drawContent() }; blurLayer.record{ drawLayer(coverLayer) }; drawLayer(coverLayer) }`. blurLayer has `renderEffect = BlurEffect(r,r,TileMode.Decal)` → the blurred copy.
- Title block (TrackInfoSection — SHARED by compact call-site PlayerScreen.kt:1129 + expanded :1456) reports `boundsInRoot`; behind it draw `blurLayer` translated by −(titleOrigin − coverOrigin), clipped to a rounded rect + ~0.30 dim = true frosted glass of the REAL backdrop (no transform math → robust to scale/crop/zoom/display).
- Adaptive colour: `coverLayer.toImageBitmap()` off-Main, average luminance of the title-region pixels → near-white text on dark backdrop / near-black on light; + subtle Shadow for crispness.
- Wire via `LocalCoverFrost` CompositionLocal provided at each player content Box (compact ~787, expanded ~1420) so nested TrackInfoSection consumes it without threading through OverlayContent.
- Fallback API<31 (minSdk30): blur no-op → dim scrim + adaptive colour + shadow (still legible).
- Status: DONE + device-verified 2026-06-30 on Jurassic Park red/white cover. Evolved on user feedback to PER-VISUAL-LINE pills (FrostedTextLine draws one rounded pill per rendered line via TextLayoutResult.getLineLeft/Right/Top/Bottom — a wrapped 2-line title yields two independently-sized pills, no dead space). Device-verified: 3 data-lines each render their own line-sized pill (the "DRIVE" pill hugs the word). User waived further wrapping test ("it's fine").
- LINKED FIX (user 2026-06-30) — filename-as-title: Drive audiobook with no embedded ©nam tag fell back to its file name ("Jurassic Park: A Novel [B00U7UVOTY].m4b" — the ".m4b"+ASIN prove it's the filename, not a tag). Added TextNormalizer.cleanFileTitle (strips trailing media ext + 10-char ASIN-shaped bracket, keeps legit "[Live]"). Applied at: player display (PlayerViewModel:1627), Cloud source (CloudViewModel), Last Played playback sources (LastPlayedViewModel ×2), Recent+Pinned list (LastPlayedRepository ×2). DONE + device-verified: player, Last Played list rows, and mini-player all show clean "Jurassic Park: A Novel".

### S — PLAYER + METADATA IMPROVEMENTS (user 2026-06-30, spec docs/superpowers/specs/2026-06-30-player-metadata-improvements-design.md; "do all in one go, context7 + superpowers, build+device-test")
Punctuation rule for ALL new/edited user text: no em-dashes or unnecessary hyphens; colons/commas/semicolons/full stops; keep layman tone.
| # | Item | Phase | Status |
|---|------|-------|--------|
| S1 | Auto-resume missing-source guard + friendly FILE_NOT_FOUND (PlaybackSessionCoordinator restore guard; PlayerErrorMessage mapping; clear dead recent row) | M | DONE(code+unit: SourceAvailability probe guards cold-start restore; PlayerErrorMessage.MISSING_FILE + isMissingFile, PlayerErrorMessageTest green; onPlayerError maps FILE_NOT_FOUND; assembleDebug green). DEVICE-VERIFY pending phone reconnect |
| S2 | Always show a subtle pill (drop hasCoverArt gate; no-cover scrim branch in FrostedTextLine) | M | DONE(code: dropped hasCoverArt gate; no-cover captured backdrop is dark → existing frost path yields a dim scrim pill; assembleDebug green). DEVICE-VERIFY pending phone reconnect |
| S3 | Player text colour modes: Default (B/W global), Custom (picked colour global), Dynamic (app-chosen per file from artwork palette, contrast-scored, may use complementary) | M | DONE(code+unit: PlayerTextColour.resolve/dynamic — palette candidates incl. dominant complement, contrast-scored vs effective backdrop, B/W fallback; PlayerTextColourTest 6 cases green; CoverFrost rememberResolvedTextColour + LocalPlayerTextColour; DataStore keys + flows; PlayerViewModel/PlayerScreen provide config + pass palette to all 4 FrostedTextLine; assembleDebug+tests green). DEVICE-VERIFY pending phone reconnect |
| S4 | Settings UI for S3 + compact custom HSV colour picker (no new dep) | M | DONE(code: ColourPickerDialog — hue spectrum drag + preset grid, no dep; SettingsViewModel flows+setters; "Player text colour" item in Appearance group with Default/Custom/Dynamic chips + swatch→picker; assembleDebug green). DEVICE-VERIFY pending phone reconnect |
| S5 | Grammar/punctuation sweep of ALL user-visible text; remove hyphens/em-dashes; layman tone; info-box completeness | M | DONE. Em-dashes: 0 in user-visible string literals (InfoContent 95→0 + commits 214087e/932884e swept the rest; only code comments retain them). En-dashes + hyphenated numeric ranges swept 2026-07-01 (context7+superpowers review): "A–B ON/Loop"→"A to B ..." (PlayerScreen ×4), "±100–500 ms"→"to" (SettingsScreen), "0–1 second"→"to" (BluetoothButton), "0-1 second"/"1-30"/"40-70"/"80-100"/"150-250 ms"/"300-500 ms"→"to" (SettingsScreen). EQ band labels: user chose ranges-with-"to" over centres (2026-07-01); EqBands.RANGE_LABELS now "22 to 44 Hz" style, DEVICE-VERIFIED single-line on the 5-col grid (no truncation/ellipsis). Verified: 0 user-visible em/en-dashes; assembleDebug+tests green. |

DEVICE VERIFICATION 2026-06-30 (real-package debug build on RFCY70BARDJ, fresh install + Drive/Spotify re-auth; Play build was uninstalled with user consent):
- S1 DONE-DEVICE: failed Drive item shows friendly "Couldn't load this Google Drive item. Sign in to Google Drive again to resume it." (no raw error).
- S2 DONE-DEVICE: no-cover local file ("Aardvark Song") shows per-line light pills (fixed an invisible-on-black first attempt); cover frost confirmed on the "This Inevitable Ruin" Drive cover.
- S3 DONE-DEVICE: Custom = orange title applied; Dynamic = yellow picked from the cover's own palette (per file). Default = unit-tested + mode-switching proven.
- S4 DONE-DEVICE: Settings → Appearance → Player text colour with Default/Custom/Dynamic chips + the colour picker dialog (hue spectrum + preset grid + Apply).
- S5 PARTIAL: info boxes (InfoContent) + info-sheet header de-em-dashed and rendering on device; the rest of the sweep remains.
- S6 DONE-DEVICE: downloaded JP m4b favourite shows its red/white embedded cover (fetcher extracted 269KB art); fix required wiring FavouriteTrackRow + content:// handling.

REGRESSIONS the user found by testing (my coverage was too thin) — fixed + re-verified across a VARIETY 2026-07-01 (commit 5d65d82):
- R1 VIDEO had no pill (I gated it off): dropped the !isVideoContent gate; FrostedTextLine draws a dark scrim over video (light text reads over any frame). Verified on a bright video clip.
- R2 DYNAMIC reverted to white on most covers (raw swatches failed the contrast check): redesigned — take the artwork HUE from the most vibrant swatch and build a LIGHT tint (dark backdrop) or DARK tint (light backdrop), nudging lightness for contrast but CAPPED so it stays a visible tint, never washing to white. Greyscale covers → B/W. Unit tests rewritten (PlayerTextColourTest, incl. the mid-backdrop regression).
- R3 PODCAST black text "blurry": the dark shadow under dark text smeared it; shadow now only behind LIGHT text. Verified crisp on the Extra Inch greyscale cover.
- VARIETY VERIFIED on device (Dynamic mode): Harry Potter purple/gold→GOLD tint; The Lost World gold/blue→BLUE tint; This Inevitable Ruin yellow→YELLOW tint; Jurassic Park red/white→PINK tint; Extra Inch greyscale→BLACK crisp; no-cover audio→WHITE on light pill; video→WHITE on dark scrim. Custom (orange) verified earlier; Default is the unit-tested B/W path.

TEXT OUTLINE (user 2026-07-01: "colour fades into the pill, add a thin border"): commit 8a3e1a0 — FrostedTextLine now a Box with an outline Text (drawStyle=Stroke) behind the fill Text; adaptive (dark halo behind LIGHT text, light halo behind DARK text); stroke width scales with fontSize + density (robust to font-scale/display); no colour-logic change. DONE-DEVICE 2026-07-01: The Lost World (white text→dark halo, stands out), Extra Inch (black text→light halo). Also DONE-DEVICE this pass: Default mode (The Lost World→white text); S5 grammar spot-check on Settings (Appearance descriptions clean, no hyphens).
CLOSED on device 2026-07-01 (folded/portrait outer display on RFCY70BARDJ; Library search avoided — it kept foregrounding a "Football Prediction League" adware app, so navigation used scroll + a suitable-length 41s tagless throwaway "zz_s1_test"):
- S1 LOCAL missing-file guard DONE-DEVICE: played a 41s throwaway (unique filename-title, sorted last, tapped via scroll), confirmed it was the most-recent (media_session=zz_s1_test, active item 145), deleted the file + MediaScanner-removed the MediaStore row, force-stop + cold relaunch → app showed the clean "Nothing's playing yet / Open Library" empty state, NO error banner, session came back null (SourceAvailability guard skipped the unreachable source; no ERROR_CODE_IO_FILE_NOT_FOUND). Throwaway's stale Last-Played entry then removed via its overflow → Delete.
- Custom mode over VIDEO DONE-DEVICE: Custom=orange; VID-20260602-WA0009 title renders orange + outline on the dark scrim pill over the video.
- Custom mode over PODCAST DONE-DEVICE: Extra Inch "Are Spurs Finally Acting Like A Big Club?" title + show render orange + outline on the frost pill over the light podcast cover.
- Mode restored to Dynamic; playback paused; Last Played cleaned.
STILL CONTENT-BLOCKED (not automatable — needs user content): S6 VIDEO-frame thumbnail in a Cloud row — no Drive VIDEO exists to download (the synced Drive folders are audiobooks). The artwork-priority branch (the spec priority for video) IS device-verified (JP embedded cover); the frame-fallback uses the same getFrameAtTime mechanism proven working by the local Library video thumbnails. Exact unblock: drop a short video into a synced Drive folder, then the Cloud-row frame path can be verified in seconds.
| S6 | Drive/offline thumbnails: artwork > video frame (Coil videoFramePercent 0.1) > icon; artwork priority for video | M | DONE(code: MediaThumbnailFetcher Coil fetcher = embedded art (audio+video) then video frame ~10% then null; scoped via MediaThumbnailRequest + Keyer, registered in app ImageLoader; CloudItemRow draws icon behind + AsyncImage(thumbModel=thumbnailUri ?: localUri) on top so it falls back to icon; localUri from offlineDrivePairs at both call sites; Context7-verified Fetcher/asImage API; assembleDebug green). DEVICE-VERIFY pending phone reconnect |

CONTEXT7 + SUPERPOWERS REVIEW of the last-10-prompts code arc (726233c..5270bbd), 2026-07-01 — "check for skipped/deferred/missed":
- Verdict: S1,S2,S3,S4,S6 + all implied items (per-line pills via TextLayoutResult, cleanFileTitle at 6 call sites, adaptive outline, 3 regression fixes) FULLY IMPLEMENTED. No TODO/stub/placeholder introduced; no requirement half-wired (both Cloud rows + both player layouts covered). MediaThumbnailFetcher API re-verified against Coil3 docs (context7): correct.
- ONE incomplete requirement found = S5 (en-dashes + hyphenated ranges left after the em-dash sweep) — FIXED this turn (see S5 row); only the steer-blocked EQ label remains.
- THREE correctness items found by the review-agent, ALL FIXED this turn: (1) MEDIUM stale backdrop-luminance in CoverFrost.rememberResolvedTextColour — LaunchedEffect keyed only on frost.captured(latches)+bounds, so a same-width title on a new cover kept the previous luminance (wrong Default colour); now also keyed on `palette`. (2) dead rememberAdaptiveTextColor (no callers) removed. (3) SourceAvailability content-scheme probe hardened with an openInputStream fallback so a stream-only provider isn't a false-negative. assembleDebug + testDebugUnitTest green.
