# ANDROID DEEP AUDIT — Lens 2 (Context7 documentation verification), Part A: UI half

Status: IN PROGRESS (incremental writes; each section appended as finished).
Scope: app/src/main/java/com/powermediaplayer/ui/ (66 files) + MainActivity.kt (edge-to-edge mandate) + Coil fetcher registration (util/ + PowerMediaPlayerApp, mandated).
Method: every third-party/Jetpack API used by ui/ verified against Context7 version-accurate docs; findings carry [SEV] + file:line + doc citation + reasoning + interdependence + preservation pre-annotation.

## 0. Pinned dependency versions (from app/build.gradle.kts — no gradle/libs.versions.toml exists)

- compileSdk 35, targetSdk 35, minSdk 30; Kotlin JVM target 17; Compose compiler via `org.jetbrains.kotlin.plugin.compose`.
- androidx.compose BOM **2025.04.00** → Compose UI/Foundation **1.8.0**, Material3 **1.3.2**, material-icons-core/extended **1.7.8** (icons stopped being versioned past 1.7.x in the BOM).
- androidx.compose.material3:material3-window-size-class (BOM-managed, 1.3.2)
- androidx.compose.material3.adaptive:adaptive / adaptive-layout / adaptive-navigation **1.1.0**; material3-adaptive-navigation-suite (BOM-managed)
- androidx.window:window **1.3.0**
- androidx.lifecycle:lifecycle-runtime-compose / viewmodel-compose / process **2.9.0**
- androidx.activity:activity-compose **1.10.1**
- androidx.navigation:navigation-compose **2.9.0**
- androidx.hilt:hilt-navigation-compose **1.2.0**; Hilt **2.54**
- io.coil-kt.coil3:coil-compose / coil-network-okhttp / coil-video **3.1.0**
- androidx.media3:* **1.6.0** (exoplayer, session, ui, cast, common…)
- com.google.android.gms:play-services-cast-framework **21.5.0**; androidx.mediarouter:mediarouter **1.7.0**
- sh.calvin.reorderable:reorderable **2.5.0**
- androidx.palette:palette-ktx **1.0.0**; androidx.core:core-ktx **1.16.0**
- kotlinx-coroutines **1.10.1**

## 1. File coverage log (read in full unless noted)

Read so far:
- MainActivity.kt, PowerMediaPlayerApp.kt (pending), app/build.gradle.kts
- ui/theme/Theme.kt, Type.kt, Color.kt, ThemeAccent.kt
- ui/adaptive/AdaptiveInfo.kt
- ui/navigation/AppNavigation.kt, TabReselectBus.kt
- ui/components/MiniPlayerBar.kt, FloatingVideoMiniPlayer.kt, RecentSearchesDropdown.kt, DownloadProgressMini.kt
- ui/player/PlayerScreen.kt, PlayerUiState.kt
- ui/player/components/CoverArtBackground.kt, ProgressSliders.kt, PlaybackControls.kt, SecondaryControls.kt, VideoSurface.kt, CoverFrost.kt, PopupVisibilityGuard.kt, CastButton.kt

(Additional files appended below as read.)

---

## 2. Library: androidx.activity 1.10.1 — edge-to-edge (Android 15 Play-Console warning) [MANDATED ITEM]

Doc basis (Context7, developer.android.com):
- "Edge-to-edge display is automatically enabled for Android 15 devices or later if your app targets SDK 35 or later. For previous Android versions, you must manually call enableEdgeToEdge in Activity.onCreate." (develop/ui/views/layout/edge-to-edge)
- Android 15 behavior-changes: deprecated `Window#setStatusBarColor`, `Window#setNavigationBarColor` (gesture nav: deprecated AND disabled), `R.attr#navigationBarColor`, `Window#setDecorFitsSystemWindows`; display cutout forced to `LAYOUT_IN_DISPLAY_CUTOUT_MODE_ALWAYS`. (about/versions/15/behavior-changes-15)

Findings:
- F-EDGE-1 [HIGH — verified COMPLIANT, no ui/MainActivity change needed]: MainActivity.kt:157-164 calls `enableEdgeToEdge(statusBarStyle = SystemBarStyle.dark(TRANSPARENT), navigationBarStyle = SystemBarStyle.dark(TRANSPARENT))` — exactly the recommended migration; explicit dark styles are a deliberate fix (comment "Audit 6.5": app is hard-forced dark, system-light devices got dark icons on black). App targets SDK 35 → A15 auto-enforcement satisfied AND pre-15 devices get identical treatment. Immersive video uses `WindowCompat.getInsetsController(...).hide/show(WindowInsetsCompat.Type.systemBars())` + `BEHAVIOR_SHOW_TRANSIENT_BARS_BY_SWIPE` (PlayerScreen.kt:756-766) — the current non-deprecated immersive API (legacy systemUiVisibility flags nowhere in ui/). Display cutout: OverlayContent pads `WindowInsets.displayCutout` horizontally (PlayerScreen.kt:1073-1076) — compatible with A15 forced ALWAYS mode.
- F-EDGE-2 [MED — residual warning source]: the Play warning fires on *references* to deprecated bar-colour APIs anywhere in the APK, including SDKs (cast-framework 21.5.0 / mediarouter 1.7.0 are known emitters; androidx's own version-gated references are exempt in current Play scans). ui/ + MainActivity contain no direct `statusBarColor`/`navigationBarColor`/`setDecorFitsSystemWindows` writes (grep verification in §11). Conclusion: app-side migration is DONE; fully clearing the console warning needs a cast/mediarouter dependency bump — a build-file concern for the deps lens, not a ui/ code change.
- Interdependence: none — the T294 full-bleed inset system in AppNavigation/PlayerScreen already assumes transparent bars and self-insets per route.

## 3. Library: material3-window-size-class (1.3.2 via BOM) + material3-adaptive 1.1.0

Doc basis: "Compute your app's WindowSizeClass using the currentWindowAdaptiveInfo() function from the Compose Material 3 Adaptive library" (adaptive/get-started-with-adaptive-apps, use-window-size-classes) — the current recommended single source; `calculateWindowSizeClass()` from material3-window-size-class is the older, still-experimental API (`ExperimentalMaterial3WindowSizeClassApi`).

- F-WSC-1 [MED]: the app runs BOTH systems: MainActivity.kt:97+280 `calculateWindowSizeClass(this)` (threaded through AppNavigation → PlayerScreen → SettingsScreen signatures) AND AdaptiveInfo.kt:31 `currentWindowAdaptiveInfo()` (posture only; size classes come from the other system via parameter). Consolidation candidate: derive width/height classes from `currentWindowAdaptiveInfo().windowSizeClass` inside rememberAdaptiveInfo and drop the material3-window-size-class artifact + parameter threading (~6 signatures). Both APIs compute from androidx.window WindowMetricsCalculator with the same 600/840dp breakpoints → behaviour unchanged.
  PRESERVATION: (a) safe — same metrics source/thresholds; (b) defended bugs: T294 flicker fix + audits F6/F7/F8 branch on WindowWidthSizeClass values — a type swap must keep every Compact/Medium/Expanded comparison intact; (c) recurrence prevented — adaptive's WindowSizeClass exposes the same breakpoints (isWidthAtLeastBreakpoint). VERDICT: legitimate shrink, but wide signature churn for zero visible gain — defer to cross-lens arbitration; otherwise KEEP (superseded, not deprecated).
- F-WSC-2 [LOW]: raw `LocalConfiguration.current.screenWidthDp/screenHeightDp` survive at PlayerScreen.kt:357-358, :1085, :1129 (fallback), :1379-1380. Docs steer window-size decisions to size classes/window metrics; on A15 Configuration dp fields exclude system-bar space, so raw comparisons can drift near thresholds. Audit 8.1(F7) already migrated one such read to heightClass-with-raw-fallback — same pattern fits the rest. PRESERVATION: (a) mechanical, same 600dp threshold; (b) the null-AdaptiveInfo fallback branch is deliberate — keep it; (c) n/a.

## 4. Library: Coil 3.1.0 (coil-compose, coil-network-okhttp, coil-video) [MANDATED — MediaThumbnailRequest fetcher/keyer]

Doc basis (/coil-kt/coil): Keyer/Fetcher.Factory registration in ImageLoader.Builder.components{} (image_pipeline.md); SingletonImageLoader.Factory on Application (getting_started.md); custom data types passed directly as AsyncImage model; default memory-cache key = components.key(...) → null (no caching) without a Keyer; size appended to the cache key ONLY when transformations are present (MemoryCacheService.newCacheKey).

- F-COIL-1 [verified-good]: PowerMediaPlayerApp.kt:51-63 implements coil3.SingletonImageLoader.Factory; registers ArtKeyer + LocalTrackArtFetcher.Factory, coil3.video.VideoFrameDecoder.Factory, MediaThumbnailFetcher.Key + Factory — textbook. Both custom types register Keyers so memory caching works. Fetchers honour options.size (Dimension.Pixels → getScaledFrameAtTime) — correct size-resolution contract; AsyncImage in a 40dp box hands Coil constraint-resolved size (documented), so frames decode small. ImageFetchResult/SourceFetchResult + DataSource.DISK correct for 3.1.0.
- F-COIL-2 [LOW-MED]: LocalTrackArtFetcher.ArtKeyer (kt:143-146) and MediaThumbnailFetcher.Key (kt:87-91) omit size from the key while the frame branch produces size-dependent bitmaps; per docs size is only auto-appended for transformation-bearing requests → a small cached frame could serve a larger target. All call sites are uniform small rows → no visible defect today. Candidate: fold options.size into the key. PRESERVATION: (a) safe — one-time cache invalidation; (b) nothing defended by the omission; (c) n/a. Low priority.
- F-COIL-3 [LOW]: no crossfade configured anywhere — optional per docs; instant swap suits OLED design. No action.
- F-COIL-4 [KEEP-DO-NOT-TOUCH]: CoverArtBackground.kt:52-109 + MiniPlayerBar.kt:82-99 bypass Coil for embedded-art ByteArrays (BitmapFactory + produceState + inSampleSize). Coil 3 accepts ByteArray models, BUT the comment records the defended bug: "Bypasses Coil entirely (its data(ByteArray) path silently fails on Audible-style M4B JPEGs)", and project memory logs the album-art double root-cause as device-verified fixed (2026-06-14). Migration re-exposes a silent failure class for negligible gain. KEEP.
- F-COIL-5 [LOW]: CoverArtBackground.kt:141 `(result.result as? SuccessResult)` — State.Success.result is already SuccessResult in Coil 3; dead cast. Trivial, safe, defends nothing.
- F-COIL-6 [verified-good/KEEP]: allowHardware(false) (kt:131) — required for Palette pixel access, correct Coil 3 extension form. `key(artworkUri)` forcing a fresh AsyncImage defends the dangling-MediaStore-URI stale-painter bug (comment kt:118-127). KEEP.
- F-COIL-7 [verified-good]: MiniPlayerBar video branch layers a Movie icon behind AsyncImage(MediaThumbnailRequest); fetch()→null yields error state → icon shows. Matches the custom-fetcher null contract.

## 5. Library: Navigation-Compose 2.9.0

Doc basis: type-safe @Serializable routes are the documented current pattern (guide/navigation/design/type-safety); string routes remain fully supported. Canonical bottom-nav: popUpTo(findStartDestination){saveState} + launchSingleTop + restoreState.

- F-NAV-1 [LOW/KEEP]: AppNavigation.kt:55-64 string-route sealed objects — modernisation to @Serializable routes is additive only, NOT a deprecation fix. Tab-nav blocks (108-115, 192-197, 335-339) match the canonical pattern verbatim; deliberate deviations each carry regression provenance: vc32 drill-in push without popUpTo (129-133, back-exits-app fix), popBackStack(route,false)-before-navigate (328-339, Cloud-tab folder reset fix), saveState=!onDetail for the Downloads detail. PRESERVATION: any refactor must keep those three semantics. VERDICT: KEEP; migrate only in a deliberate nav overhaul.
- F-NAV-2 [verified-good]: currentBackStackEntryAsState + destination.hierarchy.any selection = documented pattern; shared LibraryViewModel via hiltViewModel() at NavHost level (activity-scoped) = documented hilt-navigation-compose cross-tab sharing.

## 6. Library: Compose runtime/foundation/ui 1.8.0 (BOM 2025.04.00)

- F-CMP-1 [HIGH → KEEP-DO-NOT-TOUCH]: PlayerScreen.kt:325-329 writes `lastReal.value = uiState` in the composable body (composition-phase state write). Legal per snapshot rules ONLY because the write precedes every read of that state in the pass (no backwards write → no loop), but it schedules redundant invalidations per cast-tick. A doc-clean SideEffect refactor would delay the hold by one frame — precisely the frame in which the defended bug ("cast handover flashes 'No media loaded'/filename", comments 306-323) becomes visible again. (a) not provably safe; (b) defends a device-verified fix; (c) recurrence under refactor can't be excluded → KEEP-DO-NOT-TOUCH, flagged for awareness.
- F-CMP-2 [MED]: TrackInfoSection collects tick-rate positionUi (PlayerScreen.kt:1784) whenever synced lyrics are visible → whole section recomposes each 500ms tick; SyncedLyricsPanel derives activeIndex via remember(lines, positionMs) (1847-1854). Doc-backed fix (side-effects#derivedstateof — use when inputs change more often than needed UI updates): hoist the positionUi collection INTO SyncedLyricsPanel and wrap the index in `remember(lines){ derivedStateOf { indexFor(lines, pos.positionMs) } }` so recomposition lands only on LINE boundaries. FrostedTextLine siblings are skippable (stable params) so today's cost is bounded → MED not HIGH. PRESERVATION: (a) pure derivation move, no effects; (b) completes Audit 3.3's tick isolation rather than fighting it; (c) LaunchedEffect(activeIndex) semantics identical (fires on line change).
- F-CMP-3 [verified-good/KEEP]: produceState off-main decode with immediate `value = null` on key change (CoverArtBackground.kt:52-86, MiniPlayerBar.kt:82-99) — documented produceState pattern; the reset defends the stale-cover-on-track-change bug (comment). KEEP.
- F-CMP-4 [LOW]: SyncedLyricsPanel LazyColumn key "$i:${timeMs}" (kt:1887) embeds the index — nullifies move tracking (docs: key = stable item identity, not position). Lines never reorder → harmless; single contentType → default fine. No action.
- F-CMP-5 [verified-good]: CoverFrost.kt GraphicsLayer capture — rememberGraphicsLayer, record/drawLayer, renderEffect=BlurEffect gated API≥31, HARDWARE→ARGB_8888 copy before getPixel, suspend toImageBitmap inside LaunchedEffect, luminance re-sample keyed (captured, bounds, palette) with stale-sample rationale documented. All current Compose 1.7/1.8 API, correctly used.
- F-CMP-6 [verified-good/KEEP]: hidden-video-controls handling (PlayerScreen.kt:885-927): graphicsLayer alpha (draw-phase only, no relayout) + clearAndSetSemantics when faded (alpha-0 ≠ gone for TalkBack) + transparent tap-catcher (alpha-0 trees stay hit-testable) — all three documented concerns handled. Defends T294 flicker. KEEP.
- F-CMP-7 [LOW]: dead `collectAsState` imports in Theme.kt:7 and ThemeAccent.kt:5 (both use collectAsStateWithLifecycle). Trivial.
- F-CMP-8 [verified-good — MANDATED, Density/fontScale override]: Theme.kt:85-104 CompositionLocalProvider(LocalDensity provides Density(base.density, base.fontScale × userScale)) is the documented app-level font-scale override; dp metrics untouched; LocalPmpScale published for touch targets; remember(baseDensity, userScale) keys correctly; 0.85..2.0 coercion sane. No misuse.
- F-CMP-9 [LOW/KEEP]: AppNavigation systemBarsIgnoringVisibility/ime insets (ExperimentalLayoutApi, opted-in; still experimental in Foundation 1.8). Constant-inset choice is load-bearing for the T294 no-relayout guarantee. KEEP.
- F-CMP-10 [LOW/INFO]: TextAutoSize (Compose 1.8 BasicText autoSize) unused; only plausible site is FrostedTextLine titles, but its per-visual-line pill geometry derives from a fixed-style TextLayoutResult — autoSize would destabilise the pill math. KEEP.
- F-CMP-11 [verified-good]: FloatingVideoMiniPlayer uses the lambda `Modifier.offset{}` form (defers state read to placement — the documented perf-correct variant) + clamped offsets on drag AND size change (Audit 6.11). Good.

## 7. Library: Material3 1.3.2 components

- F-M3-1 [verified-good]: SecondaryControls.kt:220 `menuAnchor(MenuAnchorType.PrimaryNotEditable, enabled)` — current 1.3.x overload (no-arg menuAnchor() is the deprecated form). ExposedDropdownMenuBox/Menu structure per docs.
- F-M3-2 [verified-good]: HorizontalDivider (not deprecated Divider); LinearProgressIndicator/CircularProgressIndicator use the CURRENT `progress = { }` lambda overloads (DownloadProgressMini.kt:57, PlayerScreen.kt:202) — the Float overloads are deprecated. Correct.
- F-M3-3 [LOW change candidate]: NavigationSuiteScaffold pinned to layoutType=None forever (AppNavigation.kt:156,181) + hand-built overlay bar/rail → the scaffold is a pure passthrough; suiteColors/suiteItemColors are dead config. Candidate: drop the scaffold, keep Box+overlay (~40 lines less, one layout node less). PRESERVATION: (a) safe — with None it renders no nav and adds no insets; (b) T294 kept it only to minimise diff; the overlay carries all behaviour; (c) regression impossible if overlay + per-route insets untouched.
- F-M3-4 [LOW]: AlertDialog long-content usage (LazyColumn in text slot, heightIn caps), AssistChip/FilledTonalButton/Switch/Slider with *Defaults.colors() — current API shapes, nothing deprecated.
- F-M3-5 [verified-good]: ProgressSliders Slider holds local drag state, commits on onValueChangeFinished (documented deferred-seek pattern); A-B markers drawn in overlay Canvas with remembered android.graphics.Paint (no per-frame alloc).
- F-M3-6 [LOW]: material-icons-extended frozen at 1.7.8 upstream (BOM no longer revs it); R8 shrink (enabled) strips unreferenced icons. AutoMirrored variants used where directional (QueueMusic, VolumeUp/Down, ArrowBack/Forward). No deprecated icon forms in files read.

## 8. Library: Media3 1.6.0 — UI-side usage

Doc basis: guide/topics/media/media3 — "PlayerSurface is a Composable for drawing video frames, connecting to any Player for rendering but without built-in playback controls" (media3-ui-compose, new in 1.6.0).

- F-M3UI-1 [INFO/KEEP-DO-NOT-TOUCH]: VideoSurface.kt hand-rolls PlayerSurface's job (AndroidView+TextureView+set/clearVideoTextureView) PLUS the VideoSurfaceBinding healing stack (kt:225-272) arbitrating two surfaces binding within ~30ms across PiP/mini-player transitions (loser's disposal blanked the winner — device-verified fix). media3-ui-compose 1.6.0 is alpha-grade and has no equivalent arbitration; adopting PlayerSurface re-opens that bug class. Revisit at media3 ≥1.8. KEEP.
- F-M3UI-2 [verified-good]: SubtitleView interop (kt:196-213): sibling AndroidView outside the colour-filter/transform layers; onCues listener + currentCues seed; setFractionalTextSize(DEFAULT × user scale); setUserDefaultStyle() — all current media3-ui API.
- F-M3UI-3 [LOW]: DisposableEffect(Unit) captures PlaybackService.getExoPlayer() once; a mid-composition player recreation would strand listeners. Player is a process singleton here → acceptable; note only.

## 9. Library: mediarouter 1.7.0 + play-services-cast-framework 21.5.0 (UI side)

- F-CAST-1 [verified-good/KEEP]: CastButton.kt ContextThemeWrapper(Theme_AppCompat_NoActionBar) around MediaRouteButton — standard workaround for MediaRouterThemeHelper's "background can not be translucent" crash under Compose hosting (crash documented in file comment); min-size floor + forced visibility defend the collapse-to-0 interop quirk. KEEP (each line guards a recorded failure).
- F-CAST-2 [MED — hand to deps lens]: cast-framework 21.5.0 / mediarouter 1.7.0 are the likely residual source of the Play edge-to-edge deprecated-API references (F-EDGE-2). Bumping is a gradle change outside Part A's scope.

## 10. Library: lifecycle 2.9.0 (runtime-compose)

- F-LC-1 [verified-good]: collectAsStateWithLifecycle at every flow read in files read so far (30+ sites), incl. initialValue overloads for cold flows. Documented recommended collector.
- F-LC-2 [LOW]: PlayerOfflineButton toast collector LaunchedEffect(Unit){ offlineStatus.collect{Toast} } (PlayerScreen.kt:175-179) — no lifecycle gating; composition survives STOP so background emissions enqueue Toasts (suppressed on 12+, harmless). Doc-clean form: repeatOnLifecycle(STARTED). (a) safe/standard; (b) app-context choice already defends config-change leak (comment); (c) n/a.
- F-LC-3 [LOW]: MainActivity lifecycleScope collectors (posture logger, PiP params) run without repeatOnLifecycle — deliberate (PiP params must be fresh at Home-press; posture logger is diagnostic). No change.

## 11. Whole-ui/ sweep verification (grep battery over all 66 files + targeted reads)

Method note: after the 28 full-file reads listed in §1, the remaining files' third-party API surface was enumerated exhaustively by pattern sweep (every ModalBottomSheet / FilterChip / TabRow / AsyncImage / Lazy items-key / menuAnchor / Divider / progress-overload / icon / inset / stateIn / collectAsState / reorderable / TopAppBar / ripple / swipeable / window-colour call site listed with file:line), then suspicious hits were read in context (LibraryScreen 470-530+660-830, CloudBrowserScreen 1671-1710, LastPlayedScreen 450-500, SettingsScreen 2655-2730, EqualizerScreen 135-225, ColourPicker.kt full, ChapterPickerDialog.kt full, fetchers + PowerMediaPlayerApp full). This yields complete coverage of the library-usage surface this lens audits.

Sweep results (deprecated/legacy API hunt):
- NO hits anywhere in app source for: bare `Divider(`, no-arg `menuAnchor()`, `rememberRipple`, `swipeable`, `anchoredDraggable` misuse, `systemUiVisibility`, `Window#setStatusBarColor` / `setNavigationBarColor` / platform `setDecorFitsSystemWindows`. (FullScreenAlarmActivity.kt:379 uses the ANDROIDX `WindowCompat.setDecorFitsSystemWindows(window,false)` — the supported compat API, not the deprecated platform one; outside ui/ scope, could still be modernised to enableEdgeToEdge — hand to Part B. PaletteHelper's `statusBarColor` is a local field name, not the API.)
- All ModalBottomSheet sites (10: InfoSheet, StatsSheet, HiddenFilesSheet, CloudBrowserScreen, AudioEffects/VideoEffects/Crossfade/Bluetooth/CastSwitcher/TrackContextSheet) use the current API (onDismissRequest + optional sheetState via rememberModalBottomSheetState(skipPartiallyExpanded)); matches the documented pattern (develop/ui/compose/components/bottom-sheets). Player-popup sheets pair with PopupOpenGuard (DisposableEffect counter) — bespoke but sound.
- All LinearProgressIndicator/CircularProgressIndicator determinate sites use the CURRENT `progress = { }` lambda overload (DownloadsScreen:276, DownloadProgressMini:57, PodcastsSection:835, PlayerScreen:202).
- Lazy keys: present and identity-stable in DownloadsScreen (:293,:323,:340), CloudBrowserScreen (:1580), LastPlayedScreen (:665), ReorderableShowList (:66), HiddenFilesSheet (:87), LibraryScreen (:678 "fav_${id}", :713 file.id), SettingsScreen (:2691). Index-embedding keys only in static-content dialogs (ChapterPickerDialog :83,:100; PlayerScreen lyrics :1887; SpotifyAlbumTracksButton :109 has NO key) — [LOW] static lists, no reorder → acceptable; adding stable keys is trivial but valueless.
- TopAppBar + TopAppBarDefaults.topAppBarColors used at 5 sites — current M3 API (experimental opt-in as required).
- rememberSaveable used with EXPLICIT keys where list identity shifts (SettingsScreen :1059 "grp_", :1147 "sub_" — comment shows the positional-restoration pitfall was already understood). Verified good.

- F-SWEEP-1 [MED]: 7 sites still use plain `collectAsState()` instead of `collectAsStateWithLifecycle()`: HeadphonePresetsSection.kt:57, SmartPlaylistsSection.kt:107, SmartPlaylistRail.kt:85-86, HeadphoneEqSection.kt:48, ReplayGainScanRow.kt:77-78. Doc basis: lifecycle-runtime-compose docs recommend collectAsStateWithLifecycle on Android so collection stops while STOPPED (battery). These flows are Room/DataStore-backed WhileSubscribed(5000) StateFlows — plain collectAsState keeps the subscription alive (and the upstream Room observers running) while the app is backgrounded with the composition retained. Cost is small but nonzero; the rest of the app (30+ sites) already uses the lifecycle collector, so this is an inconsistency, not a pattern. Change candidate: mechanical swap, import change only. PRESERVATION: (a) safe — identical values when RESUMED; initial value comes from the StateFlow itself; (b) no comments/git evidence any of these deliberately avoid the lifecycle collector; (c) recurrence prevented by the swap itself matching the app-wide convention.
- F-SWEEP-2 [LOW]: Deprecated directional icons still referenced (material-icons 1.7.8 deprecates them in favour of Icons.AutoMirrored.*): Icons.Filled.Sort (LibraryScreen.kt:337, MediaSelectionComponents.kt:98), Icons.Filled.VolumeUp (SettingsScreen.kt:326), Icons.Filled.QueueMusic (CloudBrowserScreen.kt:655), Icons.Filled.PlaylistPlay (SmartPlaylistsSection.kt:116). Only consequence: deprecation warnings + wrong direction under RTL locales (app ships English-only UI strings → cosmetic). Mechanical swap; (a) safe, byte-identical glyphs in LTR; (b) nothing defended; (c) n/a.
- F-SWEEP-3 [LOW]: LibraryScreen.kt:496 uses `TabRow` — not deprecated in M3 1.3.2, but PrimaryTabRow/SecondaryTabRow are the current recommended variants. Cosmetic-only difference (indicator styling). KEEP unless touched for other reasons.
- F-SWEEP-4 [LOW]: Event collectors in LaunchedEffect(Unit) without lifecycle gating: TabReselectBus.events (LibraryScreen:273, CloudBrowserScreen:111, SettingsScreen:120), snackbar/message flows (LastPlayedScreen:77,:189). Same class as F-LC-2; reselect events can only originate from foreground UI taps → no practical leak. No action.
- F-SWEEP-5 [correction to F-COIL-3]: crossfade IS used at one site — LibraryScreen.kt:789-801 video-row thumbnails: `ImageRequest.Builder(ctx).data(uri).videoFrameMillis(1000).crossfade(true)` with `error = rememberVectorPainter(...)`. This is the correct coil-video request-extension API (videoFrameMillis) + correct crossfade usage; F-COIL-3's "no crossfade anywhere" is amended to "crossfade deliberately omitted everywhere except library video thumbs". Verified good.
- F-SWEEP-6 [verified-good]: ViewModel flow exposure across ui/ (grep of all 60+ stateIn sites): uniform `stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), initial)` — the documented screen-state pattern; the handful of `Eagerly` sites (CloudViewModel provider lists, PlayerViewModel skip-seconds/rotation prefs) back always-needed player facts where late subscription would glitch controls. Consistent with lifecycle/coroutines guidance.

## 12. Library: sh.calvin.reorderable 2.5.0

- F-REORD-1 [verified-good]: Both usages (LastPlayedScreen.kt:53-54,656,666,705; ReorderableShowList.kt:33-34,56,67,107) follow the library's documented v2 pattern exactly: `rememberReorderableLazyListState(listState) { from, to -> ... }` + `ReorderableItem(reorderState, key = <same key as the lazy item>)` + `Modifier.draggableHandle()` inside the item scope. Keys match between itemsIndexed and ReorderableItem (mandatory for this library). No misuse.

## 13. Library: material3-adaptive 1.1.0 — ListDetailPaneScaffold (SettingsScreen two-pane)

- F-ADPT-1 [verified-good]: SettingsScreen.kt:2674-2729 uses `rememberListDetailPaneScaffoldNavigator<String>()` + `ListDetailPaneScaffold(directive = navigator.scaffoldDirective, value = navigator.scaffoldValue, listPane = { AnimatedPane { ... } }, detailPane = { AnimatedPane { ... } })` with `scope.launch { navigator.navigateTo(Detail, key) }` — navigateTo became suspend in adaptive-navigation 1.1.0 and the code correctly wraps it in a coroutine. Matches the current documented adaptive list-detail pattern (develop/ui/compose/layouts/adaptive). NavigationSuiteScaffold is the only other adaptive component used (see F-M3-3).

## 14. Coverage list — libraries verified via Context7 + resolved versions

| Library | Version pinned | Context7 source | Verdict |
|---|---|---|---|
| androidx.activity (enableEdgeToEdge/SystemBarStyle) | 1.10.1 | /websites/developer_android (edge-to-edge + A15 behavior-changes) | Compliant; residual Play warning is dependency-side (F-EDGE-1/2) |
| Compose UI/Foundation/Runtime | 1.8.0 (BOM 2025.04.00) | /websites/developer_android_develop_ui_compose (bestpractices, lists, side-effects) | Sound; 1 KEEP-flag (F-CMP-1), 1 MED perf candidate (F-CMP-2) |
| Material3 | 1.3.2 (BOM) | same + components/bottom-sheets | Current APIs throughout (menuAnchor, HorizontalDivider, lambda progress); 1 LOW shrink (F-M3-3) |
| material3-window-size-class | 1.3.2 (BOM) | adaptive/get-started, use-window-size-classes | Superseded by adaptive — consolidation candidate (F-WSC-1) |
| material3-adaptive (+navigation-suite, adaptive-navigation) | 1.1.0 | adaptive docs | Correct usage (posture, ListDetailPaneScaffold); NavigationSuiteScaffold now vestigial (F-M3-3) |
| material-icons core/extended | 1.7.8 (frozen) | icons deprecation set | 4 deprecated directional icons (F-SWEEP-2, LOW) |
| Coil (coil-compose, coil-network-okhttp, coil-video) | 3.1.0 | /coil-kt/coil (image_pipeline, getting_started, MemoryCacheService) | Fetcher/Keyer registration textbook; keyer-size nuance (F-COIL-2); ByteArray bypass = KEEP (F-COIL-4) |
| navigation-compose | 2.9.0 | /websites/developer_android_guide (type-safety, navigate) | Canonical patterns; string routes fine (F-NAV-1 KEEP) |
| hilt-navigation-compose | 1.2.0 | same | hiltViewModel scoping correct |
| lifecycle-runtime-compose | 2.9.0 | compose docs | collectAsStateWithLifecycle app-wide except 7 sites (F-SWEEP-1 MED) |
| media3 (media3-ui interop; media3-ui-compose awareness) | 1.6.0 | /websites/developer_android (media3 topic) | SubtitleView interop correct; PlayerSurface adoption deferred (F-M3UI-1 KEEP) |
| mediarouter / cast-framework | 1.7.0 / 21.5.0 | (interop crash documented in-code) | Workarounds KEEP; version bump handed to deps lens (F-CAST-2) |
| sh.calvin.reorderable | 2.5.0 | (v2 API pattern) | Exact documented usage (F-REORD-1) |
| androidx.window | 1.3.0 | adaptive docs | Used only for the F8 posture diagnostic in MainActivity — correct |
| palette-ktx | 1.0.0 | (allowHardware constraint via Coil docs) | Off-main quantisation + software bitmaps — correct |

STATUS: COMPLETE. Highest-value actionable items: F-SWEEP-1 (7× collectAsState swap), F-CMP-2 (lyrics derivedStateOf isolation), F-M3-3 (delete vestigial NavigationSuiteScaffold), F-COIL-5 (dead cast), F-CMP-7 (dead imports), F-SWEEP-2 (icon deprecations). Everything edge-to-edge-related in ui/+MainActivity is already compliant; the Play warning remnant is dependency-side. All KEEP-DO-NOT-TOUCH annotations above defend device-verified fixes and must survive cross-lens arbitration.
---
# Lens 2 — Context7 documentation verification, part B (non-UI half)

- Date: 2026-07-04. Read-only audit; no code edited, no build run.
- Version pins from `app/build.gradle.kts`: compileSdk/targetSdk 35, minSdk 30; Media3 1.6.0; Room 2.7.1; DataStore 1.1.7; WorkManager 2.10.0 + hilt-work 1.2.0; Hilt 2.54; kotlinx-coroutines 1.10.1; OkHttp 4.12.0; AppAuth 0.11.1; NanoHTTPD 2.3.1; BouncyCastle bctls/bcprov-jdk18on 1.78.1; play-services-cast-framework 21.5.0; mediarouter 1.7.0; play-services-auth 21.3.0; security-crypto 1.1.0-alpha06; Gson 2.12.1; Guava 33.4.0-android; startup-runtime 1.1.1 (debug only).
- Severity scale: CRITICAL / HIGH / MED / LOW / INFO / VERIFIED-CORRECT.

---

## 1. Media3 1.6.0 (`/androidx/media`)

### 1.1 [LOW] PlaybackService.kt:379-387 — deprecated `COMMAND_SET_DEVICE_VOLUME` reference (the known warning)
- Doc (api.txt, /androidx/media): `setDeviceVolume(int)` and `setDeviceMuted(boolean)` are `@Deprecated`; the `VolumeFlags` overloads replace them. `COMMAND_SET_DEVICE_VOLUME_WITH_FLAGS` was added in 1.1.0 (RELEASENOTES 2023-07-05).
- Code reality: the actual mutator call is the **two-arg, non-deprecated** `p.setDeviceVolume(target, 0)` at :387. The deprecation warning at :380 arises solely from referencing the deprecated *constant* `Player.COMMAND_SET_DEVICE_VOLUME` inside a defensive availability check that accepts either the new or legacy command.
- Reasoning: usage is doc-correct where it matters (the setter). The legacy-constant check is defensive compatibility: a `Player` implementation (remote controller, older wrapper) may advertise only the legacy command; dropping the check could silently disable the cast fade on such players.
- Interdependence: this is the CASTFADE sleep-timer fade path — device-verified behaviour (remediation 2026-06-27). CastSession path (:370-376) is primary; CastPlayer path is already the fallback.
- PRESERVATION: **KEEP-DO-NOT-TOUCH** the dual check. Cosmetic option only: `@Suppress("DEPRECATION")` on the enclosing function to silence the compile warning. (a) suppression is a zero-behaviour change; (b) the dual check defends against players advertising only the legacy command — removing it risks a silent fade no-op; (c) with suppression the warning goes away without touching logic, so no recurrence path.

### 1.2 [MED] audio/AudioDelayProcessor.kt — missing `onFlush()`; ring buffer survives seek/flush
- Doc (BaseAudioProcessor.java source via Context7): `BaseAudioProcessor` "handles configuration/flush/reset state management. Subclasses override … `onFlush()` for state resets." The base `flush()` clears only the *output* buffer; any subclass-held buffered PCM must be dropped by the subclass in `onFlush()`.
- Code reality: `AudioDelayProcessor` holds up to 2000 ms of PCM in `ring` (`writePos`/`readPos`/`filled`, :40-46) and overrides **no** `onFlush`/`onReset` (grep over `audio/*.kt`: Equalizer :201/:209, Reverb :187/:192, VoiceBoost :110 and HueAnalyserAudioProcessor :197 all override `onFlush`; AudioDelayProcessor does not; Gain and StereoTransform are stateless so correctly need none).
- Consequence: with a non-zero A/V delay configured, a seek (which flushes the sink) leaves pre-seek audio in the ring; the first post-seek output emits up to `delayMs` of stale audio from the old position — audible glitch, worst on BT-video-offset users (the processor's whole audience).
- Interdependence: only active when `btVideoAudioOffsetMs > 0` and PCM16 (onConfigure gate :54); pass-through path (:79-84) drains `filled` first, so the stale tail also leaks out of delay→0 transitions after a seek.
- PRESERVATION pre-annotation (change candidate: add `override fun onFlush() { writePos = 0; readPos = 0; filled = 0 }`):
  - (a) Safe: flush happens exactly when ExoPlayer discards its own buffered audio (seek, format change, stop); dropping ring content then matches what every sibling processor in this codebase already does.
  - (b) Prior-bug defence: none identified — vc29.26/vc29.27 comments cover allocation reuse, not flush semantics; the scratch-buffer reset lives in `onConfigure` and is untouched.
  - (c) No recurrence: resetting the three indices reproduces the exact post-`onConfigure` state (:62), which is already proven-good.

### 1.3 [VERIFIED-CORRECT] BaseAudioProcessor buffer-ownership contract across audio/ + hue/
- Doc: `replaceOutputBuffer(size)` returns the internal (reused, direct, native-order) buffer; caller must write then `flip()`; one output buffer per processor — calling it twice per `queueInput` would clobber output.
- Verified: every processor (`AudioDelayProcessor`, `EqualizerAudioProcessor`:156-168, `GainAudioProcessor`:38-55, `ReverbAudioProcessor`:96-127, `StereoTransformProcessor`:52-61, `VoiceBoostAudioProcessor`:76-86, `HueAnalyserAudioProcessor`) calls `replaceOutputBuffer` at most once per `queueInput`, fully consumes the input buffer, and flips. `replaceOutputBuffer(0).flip()` during delay warm-up (:107) is legal (empty output, input consumed). `onConfigure` returning `AudioFormat.NOT_SET` for non-PCM16 (:54) is the documented deactivation idiom.

### 1.4 [MED] PlaybackService.kt:1273 — `setWakeMode(C.WAKE_MODE_LOCAL)` while the app streams over network
- Doc: Media3 wake modes — `WAKE_MODE_LOCAL` holds a partial wake lock only; `WAKE_MODE_NETWORK` additionally holds a WifiLock and is the recommended mode when playing remote streams. This app registers HLS/DASH/RTSP modules and streams Drive over HTTPS (`ResolvingDataSource`, :1178), so screen-off Wi-Fi playback relies on the OS not idling Wi-Fi.
- Consequence: on aggressive-doze OEM builds, a long screen-off Drive/HLS stream can stall when Wi-Fi power-saves; symptom would be mid-track rebuffer/stop with screen off, local files unaffected.
- PRESERVATION pre-annotation (change candidate: `WAKE_MODE_NETWORK`):
  - (a) Safe: strict superset of LOCAL (docs); locks are held only while `playWhenReady && state != idle`, so no battery cost when paused.
  - (b) Prior-bug defence of current usage: possible deliberate battery choice for a mostly-local library; no in-code comment claims that, but this project's rules demand evidence before player changes (no-guesswork policy) — no user-reported screen-off-stall symptom is on file.
  - (c) Recurrence: n/a — the mode is declarative.
  - Verdict: legitimate candidate but **evidence-gated** — only apply if a screen-off network-stall symptom is ever logged; otherwise KEEP.

### 1.5 [VERIFIED-CORRECT] ResolvingDataSource (PlaybackService.kt:1178-1191)
- Doc: the resolver is invoked on the loading thread before `open()`; blocking work is the intended use. `fetchAccessTokenBlocking()` for googleapis.com hosts is a canonical use (auth-header stamping). Pass-through for non-Drive URIs is correct.

### 1.6 [VERIFIED-CORRECT] MediaSessionService manifest + lifecycle
- Doc (developer.android.com/media/media3/session/background-playback): service must declare `foregroundServiceType="mediaPlayback"`, exported, `MediaSessionService` intent filter, with FOREGROUND_SERVICE + FOREGROUND_SERVICE_MEDIA_PLAYBACK permissions. All present (AndroidManifest.xml:25-26, :159-166).
- INFO: docs also suggest adding `android.media.browse.MediaBrowserService` to the intent filter for legacy/platform clients. Absent here; only needed for MediaBrowser-based clients (e.g. some car/watch surfaces). No defect for current feature set — note only.
- `onTaskRemoved` (:3258-3311) implements the documented stop-if-not-playing pattern with an explicit user setting; matches guidance.

### 1.7 [VERIFIED-CORRECT] CastPlayer construction + SessionAvailabilityListener (PlaybackService.kt:1914-1945)
- Doc-conformant: lazy `CastContext.getSharedInstance` in try/catch for missing GMS, `CastPlayer(castContext)`, `setSessionAvailabilityListener` switching the session player. The suppression windows are app logic layered on top (device-verified hijack fix) — KEEP-DO-NOT-TOUCH.

---

## 2. Room 2.7.1 (`/websites/developer_android_topic`)

### 2.1 [MED] data/db/AppDatabase.kt:104-105 — `exportSchema = false` with 15 hand-written migrations
- Doc: Room migration guidance is to export schemas (`room.schemaLocation` KSP arg) and keep them in VCS so `MigrationTestHelper` can round-trip every version pair. (Context7 surfaced the "Testing Room migrations is essential" architecture note; the exportSchema mechanics are stable, long-standing Room doc.)
- Reality: 22 schema versions, migrations 7→22 all hand-written SQL (AppDatabase.kt:133-381), zero migration tests possible without exported schemas. One bad `ALTER TABLE` in a future migration would brick upgrades and be undetectable pre-release.
- PRESERVATION pre-annotation (change candidate: set `exportSchema = true` + KSP `room.schemaLocation`):
  - (a) Safe: compile-time artefact generation only; zero runtime behaviour change; APK unchanged.
  - (b) Prior-bug defence of current setting: none — `false` merely silences the Room build warning.
  - (c) Recurrence: n/a. Note historical schemas before v22 cannot be back-generated; testing starts from the first exported version.

### 2.2 [VERIFIED-CORRECT / KEEP-DO-NOT-TOUCH] `fallbackToDestructiveMigrationFrom(false, 1..6)` (di/AppModule.kt:61)
- Deliberate, documented master-plan policy ("beta data does not migrate"). The `false` first arg (don't drop all tables on downgrade) is the 2.7 signature. Correct API use.

### 2.3 [LOW] Room 2.7.1 vs current 2.8.4 — hygiene-only; no doc-verified defect in 2.7.1 usage found. DAO Flow invalidation usage (observed `observeEpisodes(...).first()` inside worker, PodcastSyncWorker.kt:109) is the documented one-shot-read-from-Flow idiom.

---

## 3. DataStore preferences 1.1.7 (`/websites/developer_android_topic`)

### 3.1 [LOW-MED / KEEP-DO-NOT-TOUCH] The single ~127-key "settings" store (data/preferences/SettingsDataStore.kt:13)
- Doc: the binding rules are (i) exactly one DataStore instance per file per process (top-level `by preferencesDataStore(name = "settings")` — complied, single delegate, singleton `SettingsDataStore` via Hilt); (ii) `edit`/`updateData` is transactional. Docs impose **no key-count limit** and give no store-splitting mandate; the practical costs are whole-file rewrite per edit and one cold parse.
- Observed costs are already known and mitigated in-code: cold reads 300-500 ms under load (PlaybackService.kt:1298-1329 comment), bounded `runBlocking + withTimeoutOrNull(200/600)` seeds with defaults-on-miss.
- Why KEEP: splitting the store would require a key-migration and would re-tune the cold-start timing that the 600 ms seed window was *specifically enlarged for* (the "half-second un-pitched audio" tester bug). The current single-store + bounded-seed design is a defence against that prior bug; a split could shift latencies in either direction and reintroduce it. No doc-backed gain justifies that risk.
- Sub-note [INFO]: the `runBlocking` seeds are a documented anti-pattern in general but are deliberate, bounded, StrictMode-acknowledged (PowerMediaPlayerApp.kt:67-76 penaltyLog-only comment names them). Doc-conformant compromise.

---

## 4. WorkManager 2.10.0 (`/websites/developer_android_topic`)

### 4.1 [HIGH] podcast/PodcastSyncWorker.kt:141-145 — `ExistingPeriodicWorkPolicy.KEEP` + constraints changed across releases under an unchanged unique name → stale constraints persist on upgraded installs
- Doc (workmanager/how-to/managing-work): "`KEEP` (ignores new work)" — if unfinished unique periodic work exists, the new request (including its Constraints) is discarded. `UPDATE` (added 2.8) exists precisely to replace the spec of existing periodic work while preserving its enqueue time and period.
- Evidence chain:
  - `UNIQUE_NAME = "podcast_sync_periodic_v1"` (:127) has never changed since introduction (git: only 88239d8 touches the string).
  - Constraints DID change: 50c19a6 "fix(podcast): Wi-Fi-only auto-sync" set UNMETERED; the current code sets `NetworkType.CONNECTED` per the explicit user directive of 2026-06-26 (comment at :134-138); fc3fb29 (M5 download gate) also reshaped download behaviour.
  - `enqueueIfNeeded` runs every process start (PowerMediaPlayerApp.kt:127-129) but with KEEP it is a permanent no-op on any install that enqueued under the old spec. No `cancelUniqueWork` exists anywhere (grep: zero hits).
- Consequence: every install that ran a Wi-Fi-only build keeps the UNMETERED constraint forever → periodic podcast sync silently never runs on mobile data, directly contradicting the shipped 2026-06-26 behaviour. Fresh installs behave correctly — which is why device tests on a clean install would not catch it.
- PRESERVATION pre-annotation (change candidate: `ExistingPeriodicWorkPolicy.UPDATE`):
  - (a) Safe: UPDATE is the doc-designed mechanism for exactly this; it preserves the period start/next-run time, so calling it on every app start does not reset or thunder the schedule; when the spec is identical it is effectively idempotent.
  - (b) What KEEP defended against: re-enqueue churn from `enqueueIfNeeded` firing every onCreate. UPDATE retains that protection (no schedule reset), so the defence carries over.
  - (c) Recurrence: with UPDATE, all future constraint/interval edits propagate automatically; the class of bug is eliminated, not patched. Double-safety: mobile-data spend is independently gated *inside* the worker (`downloadsAllowed` + MobileDataPolicy), so widening the run-constraint cannot cause unwanted downloads.

### 4.2 [VERIFIED-CORRECT] On-demand initializer + Hilt wiring
- Manifest removes `androidx.work.WorkManagerInitializer` from the merged `InitializationProvider` via `tools:node="remove"` (AndroidManifest.xml:83-92) while the app implements `Configuration.Provider` with `HiltWorkerFactory` (PowerMediaPlayerApp.kt:39-43). This is the exact documented pattern for @HiltWorker; the debug-only DeepLogger startup entry is unaffected because only the WorkManager meta-data node is removed.
- `PeriodicWorkRequestBuilder<PodcastSyncWorker>(6, HOURS)` ≥ MIN_PERIODIC_INTERVAL (15 min) — valid. The A4 `noBackupFilesDir` touch (:122) is a harmless defensive fix for a real field crash.

---

## 5. OkHttp 4.12.0 (`/websites/square_github_io_okhttp`)

### 5.1 [VERIFIED-CORRECT] util/SharedHttp.kt — shared base client + `newBuilder()` derivations
- Doc (recipes, Per-call Configuration): `newBuilder()` "returns a builder that shares the same connection pool, dispatcher, and configuration with the original client." The SharedHttp design comment claims exactly this and is doc-accurate. 12 call sites derive from `SharedHttp.base`; zero bare `OkHttpClient()` constructions remain in scope (grep verified).
- `callTimeout(30 s)` default with `callTimeout(0)` overrides for large transfers (cloud/DriveOAuthProvider.kt:78 with explicit rationale; podcast/PodcastDownloader.kt:36) is correct: 0 disables the whole-call cap while `readTimeout(20/60 s)` still bounds stalls — the documented pattern for long downloads. SpotifyProvider's 6 s callTimeout (:192) is a deliberate UI-latency bound.
- 10 MB disk `Cache` installed once from Application.onCreate before first use (`installCacheDir`, PowerMediaPlayerApp.kt:94) — ordering guarantee satisfied by the lazy `base`.

---

## 6. Hilt 2.54 + App Startup 1.1.1

### 6.1 [VERIFIED-CORRECT] di/AppModule.kt — @Singleton scoping
- All DAOs/providers/@ApplicationScope CoroutineScope bound in `SingletonComponent`; the never-cancelled `SupervisorJob + Main.immediate` app scope is the documented process-lifetime pattern for session side effects. `@HiltAndroidApp` + `@AndroidEntryPoint` on the boot receiver are standard. No doc deviation found. (Interplay with App Startup: DeepLogger initializer is debug-only and does not touch Hilt — no ordering hazard, Hilt initialises via the Application base class, before any ContentProvider-driven initializer's `create()` completes its dependents' use.)

---

## 7. kotlinx.coroutines 1.10.1 (`/kotlin/kotlinx.coroutines`)

### 7.1 [VERIFIED-CORRECT] Semaphore fairness — podcast/PodcastSyncWorker.kt:53 `Semaphore(3)`, hue/HueProvider.kt:976 `Semaphore(4)`
- Doc (Semaphore.kt source): `release()` "resumes waiters in FIFO order … demonstrates the fairness guarantee." Both throttles therefore starve nothing; correct primitive for bounded parallel feed/bridge fan-out.

### 7.2 [VERIFIED-CORRECT] `combine` arity — all in-scope combines are 2-4 flows (PlaybackService.kt:1072, 1625, 1648, 1661, 1697, 2427; repositories) — within the typed overloads; no vararg edge. The Hue collector's 3-source combine including `hueSelectedArea` is the T253 reconnect fix — KEEP-DO-NOT-TOUCH.

### 7.3 [LOW] `callbackFlow` + `trySend` for `playingFlow` (PlaybackService.kt:1681-1691)
- `callbackFlow` has a default 64-element buffer; `trySend` of a boolean toggle can in theory drop on burst, but a dropped intermediate is self-healing (next `onIsPlayingChanged` restores truth) and `distinctUntilChanged()` downstream makes ordering benign. No change warranted — KEEP.

### 7.4 [MED, cross-ref §10.1] BootCompletedReceiver launches on `Dispatchers.Default` scope with no `CoroutineExceptionHandler` and try/**finally** only — an exception from the DataStore read crashes the process (details in §10.1; the coroutine-side fix is a `runCatching` around the body).

---

## 8. Google Cast framework 21.5.0 + mediarouter 1.7.0

### 8.1 [MED — store/dependency] Residual Android 15 edge-to-edge Play warning is dependency-side (handed from Lens 2 part A)
- Cross-checked: the Play Console warning ("deprecated APIs or parameters for edge-to-edge") flags `Window.setStatusBarColor` / `setNavigationBarColor` / `setDecorFitsSystemWindows` call *sites inside libraries*; community threads confirm warnings persist until the offending dependencies update, and `play-services-cast-framework` ↔ `androidx.mediarouter` are coupled (cast-framework pulls mediarouter transitively; app pins 21.5.0 + 1.7.0, both pre-Android-15).
- Current versions: Cast SDK release notes (developers.google.com/cast/docs/release-notes) list the 22.x line as current for the Android Sender; mediarouter has a 1.8.x line. No authoritative changelog line states "edge-to-edge deprecated-API use removed in exactly version X" — the fix version is **not doc-provable**, only that 21.5.0/1.7.0 predate the Android 15 window-API migration.
- PRESERVATION pre-annotation (change candidate: bump cast-framework → latest 22.x + mediarouter → latest 1.8.x):
  - (a) Why safe in principle: sender API surface is stable; app uses only `CastContext.getSharedInstance`, `CastOptions`/`OptionsProvider`, `SessionManager` volume, and Media3's `CastPlayer` adapter (media3-cast 1.6.0 declares a cast-framework dependency range that tolerates newer versions).
  - (b) What the current pin defends: the device-verified cast behaviour matrix — TV→speaker hijack suppression, CastRelay streaming, cast fade — is fragile and was hard-won (project memory: cast verification is unreliable via dumpsys and needs ear/log verification). 22.x also changed `getSharedInstance` threading expectations in some releases.
  - (c) Recurrence guard: the bump must ride a dedicated pass with the full cast test protocol (Living Area TV / Kabir Stereo, relay GET-log evidence, fade + disconnect cycle). Until that pass runs, KEEP the pins; the warning is informational-only and does not block Closed testing (project memory note).
- Sources: [flutter/flutter#183372](https://github.com/flutter/flutter/issues/183372), [dotnet/maui#26788](https://github.com/dotnet/maui/issues/26788), [Google Cast SDK Release Notes](https://developers.google.com/cast/docs/release-notes), [Play dev community thread](https://support.google.com/googleplay/android-developer/thread/349318074).

### 8.2 [VERIFIED-CORRECT] cast/CastOptionsProviderImpl.kt — OPTIONS_PROVIDER_CLASS_NAME meta-data (manifest :95-97) + Styled Media Receiver `CC1AD845` + default NotificationOptions: the canonical minimal sender configuration.

---

## 9. NanoHTTPD 2.3.1 (`/nanohttpd/nanohttpd` + web verification)

### 9.1 [INFO] Maintenance status: dormant. Last release 2.3.1 = 2016-08-12; no maintained successor line.
- Known CVEs target modules this app does **not** ship or use: CVE-2020-13697 (reflected XSS in `RouterNanoHTTPD.GeneralHandler` — app never uses RouterNanoHTTPD; `CastRelayServer` extends core `NanoHTTPD` and overrides `serve()` wholesale) and CVE-2022-21230 (nanolets module, not a dependency). The core `org.nanohttpd:nanohttpd` artefact used here has no direct CVE.
- Store posture: an embedded LAN HTTP relay is Play-acceptable; the server is started per cast session and stopped (`stop()` override :114). Acceptable to keep, with 9.2 addressed.
- Sources: [NVD CVE-2020-13697](https://nvd.nist.gov/vuln/detail/CVE-2020-13697), [NVD CVE-2022-21230](https://nvd.nist.gov/vuln/detail/CVE-2022-21230), [GHSA-pr5m-4w22-8483](https://github.com/advisories/GHSA-pr5m-4w22-8483), [mvnrepository 2.3.1](https://mvnrepository.com/artifact/org.nanohttpd/nanohttpd-webserver/2.3.1).

### 9.2 [MED — security] service/CastRelayServer.kt:45,101 — predictable relay tokens on a wildcard-bound server
- `tokenCounter = AtomicLong` → `incrementAndGet().toString(16)` yields tokens "1", "2", "3"… on a server deliberately bound to all interfaces (:40-44, required so the cast receiver can reach it). Any device on the LAN can enumerate `http://<phone>:<port>/1..n` while a cast session is live and fetch the user's media (local files, SAF docs, Drive content relayed with the user's OAuth).
- Threat is bounded (LAN-only, relay alive only during casting, ephemeral port) but the fix is nearly free.
- PRESERVATION pre-annotation (change candidate: token = 16-byte `SecureRandom` hex):
  - (a) Why safe: the token is opaque end-to-end — generated in `register()`, embedded in the URL handed to the receiver, looked up in a `ConcurrentHashMap` (:46-48). No component parses or orders it; length/format change is invisible to the receiver.
  - (b) What the counter may defend: nothing functional — the in-code comment covers only map visibility across NanoHTTPD worker threads, which is a property of the ConcurrentHashMap, not of the token scheme.
  - (c) Recurrence: impossible by construction — random tokens keep the identical URL contract; the CastRelay GET diagnostic logs (used as playback-verification evidence) change cosmetically only (longer token string).

---

## 10. Platform APIs (targetSdk 35) (`/websites/developer_android`)

### 10.1 [HIGH] alarm/BootCompletedReceiver.kt:30-50 + AndroidManifest.xml:263-272 — Direct Boot: `LOCKED_BOOT_COMPLETED` handler reads credential-encrypted DataStore → crash before first unlock
- Doc: `directBootAware` components run before user unlock and may access **only device-protected storage**; credential-encrypted (default) app storage is unavailable until unlock. The receiver is `directBootAware="true"` and registered for `LOCKED_BOOT_COMPLETED`, but its handler's first act is `settingsDataStore.scheduledAlarms.first()` — a read of `filesDir`-backed (CE) Preferences DataStore.
- Failure path: at locked boot the DataStore file open throws; the exception propagates out of `first()` inside a `try { … } finally { pendingResult.finish() }` — **no catch** — into a `CoroutineScope(SupervisorJob() + Dispatchers.Default)` with no `CoroutineExceptionHandler` → uncaught → process crash on every reboot of a Direct-Boot device. Alarms are still re-armed later by the second delivery (`BOOT_COMPLETED`, post-unlock), so the feature partially self-heals — which hides the crash from casual testing.
- Second-order defect: the *purpose* of handling `LOCKED_BOOT_COMPLETED` (ring even if the user never unlocked after an overnight reboot) is not achieved, because the alarm records live in CE storage.
- PRESERVATION pre-annotation (two-tier change candidate):
  - Tier 1 (minimal, unambiguously safe): wrap the body in `runCatching` (keep `finally { finish() }`). (a) Cannot regress anything — it converts a crash into the exact same no-op the crash produced, and BOOT_COMPLETED still re-arms; (b) current code defends nothing — the try/finally exists only to finish the async receiver; (c) recurrence prevented for the crash class.
  - Tier 2 (full fix, larger): persist `scheduledAlarms` in `context.createDeviceProtectedStorageContext()`-backed storage so pre-unlock re-arm actually works. This moves data between encryption domains — needs a one-time migration and a deliberate decision that alarm times are OK in device-protected storage (they are non-sensitive; the platform's own AlarmClock apps do this). Do not attempt casually; Tier 1 first.
- Interdependence: `AlarmScheduler.schedule` itself (setAlarmClock + PendingIntent) needs no storage and would work pre-unlock; only the read blocks.

### 10.2 [MED — manifest pattern + Play policy] AndroidManifest.xml:49-50 — both `SCHEDULE_EXACT_ALARM` and `USE_EXACT_ALARM` declared unbounded
- Doc (Manifest.permission reference): "only one of `USE_EXACT_ALARM` or `SCHEDULE_EXACT_ALARM` should be requested on a device. If your app … needs `USE_EXACT_ALARM` on SDK 33+, then `SCHEDULE_EXACT_ALARM` should be declared with a max-sdk attribute": `<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" android:maxSdkVersion="32" />`. The in-code comment (AlarmScheduler.kt:14-16) already understands this split but the manifest doesn't implement it.
- Play-policy note (same doc): USE_EXACT_ALARM is auditable; intended for apps where exact alarms are **core** functionality (alarm-clock class). A media player with a wake-up-alarm feature is defensible (it IS an alarm clock feature ringing media) but should have a prepared justification for Play review. Also note the side effect: holders of USE_EXACT_ALARM are pinned to the WORKING_SET standby bucket or lower — a real (documented) battery/standby cost of keeping this permission.
- PRESERVATION pre-annotation (change candidate: add `android:maxSdkVersion="32"` to SCHEDULE_EXACT_ALARM):
  - (a) Safe: this is the doc-verbatim pattern; on API 30-32 SCHEDULE_EXACT_ALARM continues to apply (min SDK 30), on 33+ USE_EXACT_ALARM auto-grants — every supported API level keeps a valid exact-alarm path; `setAlarmClock` (AlarmScheduler.kt:37,75) is the correct doc-recommended API for user-visible alarms and needs no change.
  - (b) What the double declaration defends: nothing beyond belt-and-braces; the system resolves precedence anyway. No prior bug attached.
  - (c) Recurrence: n/a — declarative.

### 10.3 [MED — store acceptance, external action] AndroidManifest.xml:135-142 — `specialUse` FGS (SpotifyBounceService)
- Manifest side is complete and doc-correct: `foregroundServiceType="specialUse"` + `PROPERTY_SPECIAL_USE_FGS_SUBTYPE` property ("spotify_bounce_back"). Doc (Service.startForeground behaviour changes): targetSdk 34+ FGS must have valid types; special-use additionally requires a **Play Console declaration** (use-case description, possibly video) at release review. This cannot be verified from the repo — genuine external blocker: confirm the FGS special-use declaration is filed in Play Console for the release track before the next Production push. `mediaPlayback` FGS needs no extra declaration beyond its type (present).
- POST_NOTIFICATIONS declared (:42); runtime grant is a UI-layer concern (part A scope); the media notification itself is posted by MediaSessionService as required.

### 10.4 [VERIFIED-CORRECT] MediaMetadataRetriever lifecycle in scope
- All non-UI construction sites pair with cleanup: util/MediaMetadataHelper.kt:88,127 (`release()` in finally-equivalent), replaygain/ReplayGainScanner.kt:113 (`runCatching { mmr.release() }`), cloud/DriveTagEnricher.kt:269 (`.use {}` — MMR implements AutoCloseable since API 29; minSdk 30, so `use` is valid everywhere this app runs). No leak path found in scope.

### 10.5 [VERIFIED-CORRECT] PlaybackService.kt:1161-1165 — `registerReceiver(..., RECEIVER_NOT_EXPORTED)` for `ACTION_HEADSET_PLUG`: protected system broadcasts are deliverable to non-exported runtime receivers on 13+; correct flag choice, and this broadcast is runtime-register-only as the comment says.

---

## 11. BouncyCastle bctls/bcprov 1.78.1 (hue/) — knowledge-flagged (no Context7 corpus for BC TLS)

### 11.1 [VERIFIED-CORRECT] hue/HueEntertainment.kt:515-546 — DTLS 1.2 PSK client
- Uses BC's documented extension points precisely: `PSKTlsClient` subclass overriding `getSupportedVersions()` → `{DTLSv12}` (Hue bridges speak DTLS 1.2 only) and `getSupportedCipherSuites()` → `{TLS_PSK_WITH_AES_128_GCM_SHA256}` (the only suite the Hue Entertainment endpoint accepts — the in-code comment records the handshake_failure(40) evidence). `UDPTransport(sock, 1500)` MTU, `BcTlsCrypto(SecureRandom())`, fd-leak close on failed handshake — all correct.
- KEEP-DO-NOT-TOUCH: the version/cipher constraints are device-verified against the physical bridge and are part of the resolved Hue reconnect regression (T253). Any BC upgrade (1.78.1 → 1.8x, LOW hygiene, no known CVE against this client path in 1.78.1) must re-run the Hue reactive-frames verification and keep the `META-INF/versions/9/OSGI-INF/**` packaging excludes (build.gradle.kts:157-158), which remain necessary on the jdk18on artefacts.

---

## 12. Other in-scope libraries — assessed, no plausible issue / no query spent

- **AppAuth 0.11.1** (`/openid/appauth-android`, queried): `manifestPlaceholders["appAuthRedirectScheme"] = "powermediaplayer"` (build.gradle.kts:55) is the README-verbatim custom-scheme pattern; PKCE verifier/state auto-generated by `AuthorizationRequest.Builder`. 0.11.1 is the latest published release — VERIFIED-CORRECT.
- **security-crypto 1.1.0-alpha06** (subtitles/OpenSubsCredStore): [LOW-MED, knowledge-flagged] Jetpack security-crypto was deprecated by Google in 2024 with no stable 1.1.0 ever shipped; EncryptedSharedPreferences has no maintained successor (guidance: Keystore + own encryption, or accept the frozen artefact). The frozen alpha keeps working; migrating credentials is high-risk (silent credential loss on scheme change) for zero functional gain — KEEP-DO-NOT-TOUCH, revisit only if Play flags it.
- **Gson 2.12.1, Guava 33.4.0-android, play-services-auth 21.3.0, documentfile 1.0.1, coroutines-guava/play-services 1.10.1**: version-current or near-current; usage in scope is plain serialisation / ListenableFuture bridging / sign-in; no doc-sensitive API surface touched — no queries spent (coordinator's skip rule).
- **MediaCodec/MediaExtractor (audio/ReverseAudio.kt)**: standard dequeue/queue loop with 10 ms timeouts observed; platform-stable API, no version-sensitive behaviour in scope — not queried.

---

## Coverage list (library → resolved Context7 ID / method → pinned version → verdict)

| Library | Verified via | Version | Outcome |
|---|---|---|---|
| Media3 (exoplayer/session/cast/common) | `/androidx/media` (2 queries) | 1.6.0 | 1 MED (AudioDelay onFlush), 1 MED-conditional (wake mode), 1 LOW (deprecation constant), rest verified |
| Room | `/websites/developer_android_topic` | 2.7.1 | 1 MED (exportSchema=false), migrations verified |
| DataStore preferences | `/websites/developer_android_topic` | 1.1.7 | KEEP (single store compliant; splitting rejected) |
| WorkManager | `/websites/developer_android_topic` | 2.10.0 | 1 HIGH (KEEP policy vs changed constraints) |
| OkHttp | `/websites/square_github_io_okhttp` | 4.12.0 | verified-correct (sharing + callTimeout pattern) |
| Hilt (+ App Startup interplay) | pattern vs WorkManager on-demand-init docs | 2.54 / 1.1.1 | verified-correct |
| kotlinx.coroutines | `/kotlin/kotlinx.coroutines` | 1.10.1 | verified (Semaphore FIFO-fair, combine arity, SharedFlow) |
| Google Cast framework + mediarouter | web (release notes + Play threads); Context7 has no sender corpus | 21.5.0 / 1.7.0 | 1 MED (edge-to-edge dependency warning; bump gated on cast re-verification) |
| AppAuth | `/openid/appauth-android` | 0.11.1 | verified-correct; 0.11.1 is latest |
| NanoHTTPD | `/nanohttpd/nanohttpd` + NVD/GHSA | 2.3.1 | dormant since 2016; CVEs in unused modules; 1 MED (predictable relay token) |
| BouncyCastle bctls/bcprov | code-vs-BC-API review (knowledge-flagged) | 1.78.1 | verified-correct DTLS-PSK; KEEP pins |
| Platform: FGS types / POST_NOTIFICATIONS | `/websites/developer_android` | targetSdk 35 | manifest correct; specialUse Play declaration = external action |
| Platform: AlarmManager exact alarms | `/websites/developer_android` | targetSdk 35 | 1 MED (maxSdkVersion split missing; policy note), setAlarmClock correct |
| Platform: Direct Boot | `/websites/developer_android` (behaviour doc) | targetSdk 35 | 1 HIGH (LOCKED_BOOT_COMPLETED CE-storage crash) |
| Platform: MediaMetadataRetriever | reference docs (AutoCloseable API 29+) | minSdk 30 | verified-correct lifecycle |
| security-crypto | knowledge-flagged (deprecated library) | 1.1.0-alpha06 | KEEP, monitor |
| Gson / Guava / play-services-auth / documentfile | skip rule (no plausible issue) | 2.12.1 / 33.4.0 / 21.3.0 / 1.0.1 | not queried |

### Severity roll-up
- HIGH: 2 — WorkManager KEEP-policy stale constraints (§4.1); Direct Boot crash in BootCompletedReceiver (§10.1 Tier 1 fix is trivial).
- MED: 6 — AudioDelayProcessor onFlush (§1.2); wake mode (§1.4, evidence-gated); Room exportSchema (§2.1); cast/mediarouter edge-to-edge bump (§8.1, gated); CastRelay token predictability (§9.2); exact-alarm maxSdk split + policy (§10.2); specialUse Play declaration (§10.3, external).
- LOW/INFO: deprecation-constant warning (§1.1), Room/BC version hygiene, callbackFlow trySend, security-crypto status, MediaBrowserService filter note.
- No CRITICAL found: no doc-contract violation that corrupts data or breaks playback on the happy path.
