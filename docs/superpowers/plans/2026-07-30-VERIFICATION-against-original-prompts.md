# Verification against the two original prompts (2026-07-30)

Every distinct requirement from both prompts, its implementation, and the ACTUAL
device evidence. Status legend: ✅ device-verified · 🟡 code done, device-verify gap · ⛔ needs user (ear/credentials).

| # | Requirement (verbatim intent) | Impl | Device evidence | Status |
|---|---|---|---|---|
| 1 | Deep-scan prompts even when already enabled in Settings (Settings-vs-Library, like the Drive-auth fix); investigate first-install scenarios | I1: Library prompt gated on `!deepScanOn && !firstRunSeen`; setDeepScan also marks first-run-seen | Emulator: enable deep-scan → prompt gone + stays gone on re-nav. Phone: fresh install (deep-scan off) → prompt shows correctly | ✅ |
| 2 | Spotify sticky sign-in: gets stuck, must full-close + retry, works after delay. Clear data, attempt sign-in, see what happens. Check if common. Delicate handoff — no rash changes | I2: SPOTIFYAUTH logging; fixed (a) 4s re-tap debounce (cancel-storm), (b) 60s→5min safety timer + cancel-on-result/rearm | App data cleared (uninstall+reinstall). Log PINNED both: triple-tap → "User cancelled flow"; 60s timer fired mid-2m43s login. User signed in OK. **FIX itself (debounce/timer) not yet live-tested** | 🟡 fix untested live |
| 3 | Player track-list tap does nothing for **local multi-track** (audiobook-chapter popup works); unsure if **Spotify albums** work — test on device | I3: PlaybackConnection cachedQueue + seekToQueueIndex; ChapterPickerDialog fed the real queue | Emulator: 2-track local queue → tap track 2 → **seeked** ✅. **Spotify-album track-list NOT tested** (user explicitly flagged this) | 🟡 Spotify-album gap |
| 4a | Autoplay-next toggle in Settings + info box; "does it work properly?" | I4a: "Auto-play next track" SettingsToggleItem + info box; PlaybackService applies pauseAtEndOfMediaItems | Emulator + phone: toggle + info box present ✅. **Actual end-of-track auto-advance not watched on device** | 🟡 auto-advance not watched |
| 4b | Autoplay button in bottom bar **before Bluetooth**, teal (user colour), appropriate icon | I4b + I7 custom fast-forward (stretched/fading) icon | Phone: ▶▶ icon before BT, teal (on) / grey (off); custom fade renders ✅ | ✅ |
| 4c | Shuffle button mini popup "shuffle on/off" | I4c: transientToast for shuffle + autoplay | Emulator: "Shuffle off" + "Auto-play off" toasts ✅ | ✅ |
| 5a | Metadata/cover not loading on phone when casting (phone should show even if receiver doesn't); "you fixed it then it came back" | I5a: CASTART logging; I8 isCasting fix; **I5a-fix2**: substitute the relay's on-disk cover (file://) when the phone-side art is the receiver-only http relay url | User found cover BLANK casting Drive audiobook → log = http RELAY BLANK → fixed → re-cast: CASTART "scheme=file → renders" + AsyncImage success ✅ | ✅ |
| 5b | Cast start delay option (beginning ½s cut off, even on mid-cast restart); if toggle exists triple-check | I5b: new "Cast start lead-in" setting + info box + prime (buffer→READY→lead-in→play); confirmed castVideoAudioOffsetMs is a SEPARATE lip-sync offset (triple-checked) | Setting + info box + slider present ✅ (emulator+phone). CASTPRIME "armed…started after 500ms lead-in" fires ✅. **Actual "beginning no longer clipped" needs your ear** | 🟡/⛔ symptom needs ear |
| 6 | Downloaded podcasts in Library like downloaded Drive books (presentation) | I6: "Downloaded podcasts (N)" section (mirrors P6 books) + shared PodcastPlayback builder | Phone: subscribed 2, downloaded 5 → "Downloaded podcasts (5)" section (title·show·size·Offline+art) → tap "Nueva Tucuman" PLAYED ✅ | ✅ |
| — | Autoplay "does it work properly" (I4d) — single-item plays should auto-advance | I4d: pinned-album tap enqueues whole album (playAlbumTracks) | Phone: pinned 3-track album, tap track → "2 / 3 tracks" multi-item queue ✅ | ✅ |
| — | Plan committed; app-data-clear held for item 2 "until last" | Plan MD committed (2850c96…); data-clear brought forward WITH explicit user consent (AskUserQuestion) to enable sideload | n/a | ✅ (consented deviation) |

## Gaps
- **G3 (I4a) — ✅ CLOSED (device):** autoplay ON → Cuddle Monkey (0:24) ended → `PLAYER: discontinuity AUTO_TRANSITION from=24359ms`, index 3→5, still PLAYING. Toggled autoplay OFF (▶▶ teal→grey) → same track ended → `state=PAUSED, index still 3` (no advance). The toggle genuinely controls auto-advance. Autoplay restored ON.
- **G2 (I3 Spotify album) — ✅ CLOSED (device):** played the "Meteora" (Linkin Park) Spotify album on a Connect device → opened the Player's album track-list (♫≡) → tracks populated, "Don't Stay" highlighted as playing → tapped "Faint" (#7) → Player switched to "Faint" playing. Spotify-album track-list tap WORKS (existing playSpotifyContextTrack path). So all three I3 sub-cases pass: local multi-track (my seekToQueueIndex fix), Spotify album (plays tapped track), audiobook chapters (pre-existing popup).
- **G1 (I2 debounce) — code-sound, live-test optional:** the 4s re-tap debounce + 5-min timer directly address the exact failures the log captured (triple-tap → "User cancelled flow"; 60s timer mid-2m43s login). Live-testing it needs a sign-out → rapid-tap → re-sign-in; not forced since you're currently signed in and Spotify verified working (G2). Offer stands if you want it proven live.

## Needs you
- **G4 (I5b):** by ear — cast a track and confirm the first ½s is no longer clipped (raise the "Cast start lead-in" if needed). I can't hear the cast.
