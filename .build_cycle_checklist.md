# Build-cycle checklist — audio/podcast features (2026-06-26)
Plan: docs/superpowers/specs/2026-06-26-audio-features-implementation-plan.md
Installed: media3 1.6.0, Room 2.7.1, Drive REST via OkHttp (drive.file). Device RFCY70BARDJ.
Rules: inline, sequential, no defer/stub, TDD pure units first, build+device-test+commit each feature.

## F1 — Podcast auto-play next episode + toggle  ✅ DONE (e705c63 + test)
- [x] DataStore key `podcastAutoplayNext` (default true) + flow + setter
- [x] PodcastDao.episodesForFeedOrdered (ordered snapshot)
- [x] PodcastsViewModel.playEpisode → queue via pure episodeQueueSlice (cap 50) when on; single when off; buildEpisodeItem per episode (mediaId=audioUrl → per-episode resume/override)
- [x] Settings row "Auto-play next podcast episode" toggle (autoplaySettings flow)
- [x] BUILD green + PodcastQueueSliceTest 5/5
- [~] device-test: queue-count render NAV-BLOCKED (podcast list pinned behind mini-player + segment ambiguity; repeated adb tap-misses → flagged per skill). Logic unit-tested instead.

## F2 — Skip silence (surface + per-file axis)  ✅ DONE
- [x] MediaOverrideEntity + skipSilence:Boolean? + voiceBoost:Boolean? ; AppDatabase v22 + MIGRATION_21_22 (+ AppModule register)
- [x] mergeEpisodeOverShow + MediaOverrideMergeTest cover new axes (skipSilence/voiceBoost merge test)
- [x] PlaybackService: skipSilenceEnabled from mediaOverrideRepo.withOverrideBool(crossfadeSkipSilence){skipSilence}
- [x] First-class Settings "Skip silence" toggle + override axis (AudioTab tri-state) in MediaOverridesPopup
- [x] BUILD green + merge test green; `[DEVICE-VERIFIED]` MIGRATION_21_22 applies cleanly (v21→v22, app launches visible, no FATAL)

## F3 — Voice boost processor + axis  ✅ DONE
- [x] audio/VoiceBoostAudioProcessor.kt — presence biquad (2.7 kHz, +5 dB, Q1) + headroom + tanh; BaseAudioProcessor media3 1.6.0
- [x] VoiceBoostAudioProcessorTest (off = exact pass-through; presence boosted relative to low) GREEN
- [x] Inserted AFTER EQ, BEFORE audioDelay/gain in PlaybackService chain; EqualizerThdTest still GREEN
- [x] voiceBoost override axis (popup) + global VOICE_BOOST setting + per-file drive (withOverrideBool) + Settings toggle
- [x] BUILD green
- [~] device-test: processor active is hard to A/B via adb; logic unit-tested + chain verified. Settings/popup toggles reachable.

## F4 — Cloud settings/cache backup & restore (Drive)
- [ ] backup/BackupModels.kt + BackupManager (sets: Settings / Overrides+EQ presets / Library data / Caches)
- [ ] DriveOAuthProvider upload (files.create multipart) + download + list (Context7 REST)
- [ ] Settings "Backup & restore" section (per-set checkboxes, Back up now, Restore)
- [ ] BUILD + unit (round-trip) + device-test + COMMIT

## F5 — Sleep-timer remote fade
- [ ] PlayerViewModel sleep fade: route-aware (Spotify/cast volume) when remote
- [ ] BUILD + device-test + COMMIT

## Gate
- [ ] Full unit suite green; delete this file
