# Build-cycle checklist — audio/podcast features (2026-06-26)
Plan: docs/superpowers/specs/2026-06-26-audio-features-implementation-plan.md
Installed: media3 1.6.0, Room 2.7.1, Drive REST via OkHttp (drive.file). Device RFCY70BARDJ.
Rules: inline, sequential, no defer/stub, TDD pure units first, build+device-test+commit each feature.

## F1 — Podcast auto-play next episode + toggle
- [ ] DataStore key `podcastAutoplayNext` (default true) + flow + setter
- [ ] PodcastDao: ordered episodes-for-feed query for queueing
- [ ] PodcastsViewModel.playEpisode → queue (tapped episode forward, cap 50) when toggle on; single when off; each item carries pmpOverrideKey + metadata/art
- [ ] Settings row "Auto-play next podcast episode" toggle
- [ ] BUILD + unit (queue builder) + device-test (advances at end) + COMMIT

## F2 — Skip silence (surface + per-file axis)  [migration 21->22 batch with F3]
- [ ] MediaOverrideEntity + skipSilence:Boolean? + voiceBoost:Int? ; AppDatabase v22 + MIGRATION_21_22
- [ ] mergeEpisodeOverShow + MediaOverrideMergeTest cover new axes
- [ ] PlaybackService: drive player.skipSilenceEnabled from activeOverride.skipSilence ?: global
- [ ] First-class Settings toggle (keep crossfade mirror) + override axis in MediaOverridesPopup
- [ ] BUILD + unit + device-test + COMMIT

## F3 — Voice boost processor + axis
- [ ] audio/VoiceBoostAudioProcessor.kt (presence-band lift + soft compression), BaseAudioProcessor media3 1.6.0 (mirror EqualizerAudioProcessor)
- [ ] VoiceBoostAudioProcessorTest (gain at presence band; off = pass-through)
- [ ] Insert AFTER EQ, BEFORE gain in PlaybackService chain
- [ ] voiceBoost override axis + global toggle + flow wiring + MediaOverridesPopup axis
- [ ] BUILD + unit (+ existing EQ/reverb THD still pass) + device-test + COMMIT

## F4 — Cloud settings/cache backup & restore (Drive)
- [ ] backup/BackupModels.kt + BackupManager (sets: Settings / Overrides+EQ presets / Library data / Caches)
- [ ] DriveOAuthProvider upload (files.create multipart) + download + list (Context7 REST)
- [ ] Settings "Backup & restore" section (per-set checkboxes, Back up now, Restore)
- [ ] BUILD + unit (round-trip) + device-test + COMMIT

## F5 — Sleep-timer remote fade
- [ ] PlayerViewModel sleep fade: route-aware (Spotify/cast volume) when remote
- [ ] BUILD + device-test + COMMIT

## Gate
- [ ] Full unit suite green; delete this file
