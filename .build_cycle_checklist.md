# Build-cycle checklist — 19-item execution (inline, no-skip)

Durable execution state (android-build-and-device-test skill). Read before every
new action. Mark a box complete ONLY after work is executed + verified.

- **Stage 1 (research):** DONE — investigation `docs/superpowers/investigation/2026-06-24-19-item-investigation.md`.
- **Stage 2 (plan):** DONE — 7 plans + MASTER-INDEX (`docs/superpowers/plans/2026-06-24-*`).
- **Stage 3 (implement):** IN PROGRESS — this checklist.
- **Stage 4 (device test):** device = Galaxy Z Fold6, serial `RFCY70BARDJ`. adb = `$LOCALAPPDATA/Android/Sdk/platform-tools/adb.exe`. NOTE: phone holds RELEASE 1.3.4/38 → debug install may need uninstall (data wipe) — flag before wiping.

Build cmds: `cmd /c gradlew.bat :app:assembleDebug -q` (empty+exit0 = success);
`cmd /c gradlew.bat :app:testDebugUnitTest --tests "<FQCN>"`. Commit per task.
Context7 before writing any unfamiliar library/API (installed versions).

Execution order + migration coordination: **P2 → P1 → P3(v19) → P4 → P5(v20) → P6(v21) → P7**.

---

## P2 — EQ quality (#7) + live-state guard (#9)  [no migration]  ✅ CODE DONE
- [x] A1 — `EqualizerThdTest.kt` Goertzel harness; RED captured (oneBand 11.1%, allBands 16.0%); commit 8c81e9c
- [x] A2 — `EqualizerAudioProcessor.kt`: accurate-headroom (|H(f)| at centres) + tanh limiter; THD GREEN; flat byte-exact; commit 6d42e31
- [x] A3 — predicate: +12dB-all-bands RED 9.3% → accurate headroom 0.0035% (oversampling N/A, evidence-recorded); commit 6d42e31
- [x] A4 — `assembleDebug` green (exit 0)
- [x] B1 — `EqualizerLiveStateTest.kt` (4 tests) GREEN; commit 302502d
- [x] B2 — `EqualizerEffectController.kt`: EqSource + setLive + restoreIfUntouched; commit 302502d
- [x] B3 — `EqualizerViewModel.kt`: restoreLastPreset→restoreIfUntouched, pushLevels(source), collector `.drop(1)`; commit 302502d
- [x] B4 — `EqualizerOverrideRouter.kt`: setLive(LIVE_OVERRIDE); commit 302502d
- [x] B5 — `assembleDebug` green + grep: only in-controller `bandLevels.value =` writes
- [x] C1 — audio suite green (THD 5, live-state 4, reverb 5; exit 0)
- [ ] DEVICE #7 — loud music + heavy preset → no robotic edge; flat = clean (stage-4 physical listen)
- [ ] DEVICE #9 — playback + expand Audio settings → no EQ shift (stage-4 physical listen)

## P1 — folded hitboxes (#1,#14,#15) + timer popup (#4)  [no migration]  ✅ CODE DONE (commit afa8051)
- [x] T1 (#14) InfoIcon declared-last hit-test fix + EQ flag-only comment
- [x] T2 (#1) brightness clickable-before-padding + 48dp (heightIn/wrapContentHeight)
- [x] T3-5 (#4) SeekTimeDialog + parseTimeToMs (11 tests green) + tappable times + PositionSection wiring
- [x] T6 (#15) isImeVisible gate on mini-player imePadding
- [x] T7 app-wide sweep — carried from investigation (3 fixes complete set; EQ flagged)
- [x] build green (assembleDebug + parser test exit 0)
- [ ] DEVICE folded/unfolded hitbox pass (Fold6 cover + main) — stage-4

## P3 — Spotify perf (#2) + Drive search + favourite-enrich (#16)  [NO migration — index dropped]  ✅ CODE DONE (commits + f7c45af)
- [x] A: #2 lyrics decouple — trackResolveStep seam + test, off-loop async fetch + LRU + 6s client, banner clears on snap; A4 album parallelise
- [x] B: #16 search — DAO LIKE (history title/subtitle, offline displayName, enrichment title/artist/album/genre); pure merge/dedup; CloudViewModel wiring; MetadataSearchDaoTest(3 Robolectric). **Index migration DROPPED** (leading-wildcard LIKE can't use it; tables tiny — D5). B7 info note corrected.
- [x] C: #16 favourite-enrich (always-on, no gate) — FavouriteEnrichPlanner(4 tests); enrichmentCacheDao write (provider 'drive-fav-tags', no Recents pollution) mapped back to playable search hits; dedup + onDone; C4 status hint + enrichingIds StateFlow
- [x] build green; tests: SpotifyLyricsDecouple(2), DriveMetadataSearchMerge(2), FavouriteEnrichPlanner(4), MetadataSearchDao(3) all green
- [ ] DEVICE #2 banner ≤1s + #16 "Matt" search + favourite-enrich no-freeze — stage-4

**MIGRATION CHAIN REVISED:** P3 adds NO migration → P5 = MIGRATION_18_19 (v19, displayOrder), P6 = MIGRATION_19_20 (v20, followLive). Final DB version = 20.

## P4 — media-type icons + classification (#13,#12,#8)  [no migration]  ✅ CODE DONE (commit 0b9c2f7)
- [x] 1.x MediaClassifier (3 pure fns) + 15 JUnit tests green
- [x] 2.x (#13) CloudBrowserScreen icon uses isVideoByName (m4b → audio icon)
- [x] 3.x (#8) PlayerViewModel.inferMediaKind → classifier; PODCAST reachable (PodcastDao membership); BT untouched
- [x] 4.x (#12) coil-video:3.1.0 + VideoFrameDecoder + LibraryScreen gated thumbnail (m4b excluded, icon fallback)
- [x] build green (assembleDebug + MediaClassifier 15/15)
- [ ] DEVICE icons/thumbnails (Drive m4b=audio, local mp4=frame, m4b≠frame, podcast label) — stage-4

## P5 — podcast reorder (#5) + effects (#6)  [MIGRATION_18_19 = v19 — P3 dropped its migration, so P5 keeps 18→19]  ✅ CODE DONE (commit b8fb68c)
- [x] A: displayOrder column + MIGRATION_18_19 (v19, back-fill by title) + DAO order + ReorderableShowList + reorderShow + append-new
- [x] B: per-episode MediaOverridesPopup (audioUrl key) via long-press + 3-dot; auto-clear on unsubscribe; MediaOverrideDao injected
- [x] PodcastShowOrderTest(3 Robolectric) + assembleDebug + full suite green
- [ ] DEVICE reorder-persist + live-effect + auto-clear — stage-4

NOTE: P5 = v19. **P6 followLive must be MIGRATION_19_20 (v20)** — renumber from the plan's 18→19. Final DB version = 20.

## P6 — resume/star (#19,#18) + storage (#3) + background (#17)  [MIGRATION_20_21 = v21 — RENUMBERED from plan's 18→19]
- [ ] 19.1-19.9 followLive (v21 migration) + StarPositionResolver + DAO positionForUri + repo/VM wiring + Fixed/Follow-live dialog + tests
- [ ] 18.1-18.4 repro doc + SessionSurfaceTest + UNCONDITIONAL warm-reopen surfacing fix (+18.3b iff audio stopped)
- [ ] 3.1-3.5 ArtworkCache evict+32MB cap + ChapterCache evict + deleteLocal wiring + OfflineCacheEvictionTest
- [ ] 17.1-17.5 vector doc + DiagLog.bg instrumentation all 5 vectors + prove termination + hard-stop safeguard + UNCONDITIONAL transparency note
- [ ] build green + DB v21 migration clean + DEVICE (follow-live resume; reopen surfacing; cache evict; bg trace)

## P7 — layman settings/info text (#10,#11)  [no migration, strings only — LAST]
- [ ] Webhooks rewrite + stale placeholder removal
- [ ] Full catalogue: every flagged string → new wording; About 1.0.0→BuildConfig.VERSION_NAME; "colors"→"colours"; strip raw class names
- [ ] build green + DEVICE settings/info read-through

---

## Cross-cutting gates
- [ ] Each plan: per-task commit; push + adb-install after commits (auto-push memory)
- [ ] Final: full `:app:testDebugUnitTest` 0 failures; `assembleDebug` green
- [ ] DB final version = 21; single coherent migration chain 18→19→20→21
- [ ] TASKS.md T347-T353 + the per-item rows updated with evidence
- [ ] Delete this checklist when every box checked + tests passed/flagged
